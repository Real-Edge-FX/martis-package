# Martis Ecosystem Audit — consolidated report

**Generated:** 2026-06-28 (recovered from the workflow synth transcript after the synth agent stalled; adversarially verified findings only).
**Scope:** martis-package (PHP + React) + docs + martis-docs mirror. Source of truth = martis-package.

## Executive summary

**158 confirmed findings** (false positives already dropped by the per-finding adversarial verifier; 1 verifier-invalidated finding excluded).

| Severity | Count |
|---|---|
| Critical | 3 |
| High | 28 |
| Medium | 57 |
| Low | 70 |

| Category | Count |
|---|---|
| bug | 54 |
| security | 51 |
| doc-drift | 20 |
| convention | 18 |
| implementation | 14 |
| perf | 1 |

| Subsystem | Count |
|---|---|
| docs-sync-package-vs-code | 12 |
| fe-components | 10 |
| fe-fields | 10 |
| fields-base-textlike | 9 |
| notif-pref-profile | 7 |
| actions-filters-lenses-cards | 6 |
| cache | 6 |
| console | 6 |
| core-registry-manager-provider | 6 |
| http-resource | 6 |
| metrics-rest | 6 |
| docs-sync-pkg-vs-mdx | 6 |
| fields-special | 5 |
| fields-media | 5 |
| fields-relation | 5 |
| http-auth-sso-ctrl | 5 |
| http-misc | 5 |
| sso | 5 |
| sec-secrets-sweep | 5 |
| authz-gates-impersonation | 4 |
| auth | 4 |
| sec-injection-sweep | 4 |
| enums-contracts | 4 |
| fe-pages-lib | 4 |
| http-relationships | 4 |
| mcp | 4 |
| menu-layout-dashboards | 3 |
| sec-authz-sweep | 2 |

> **Already fixed this session (dedupe):** `?sort=` whitelist on BelongsToMany/MorphToMany (commit d28121eae) and TrendMetric cross-driver DATE_FORMAT (commit ced5c5055). Matching findings below are tagged **[FIXED]**.

## Critical (3)

### Pivot action execution skips per-model canRun and resource-level authorization checks
- **Severity:** critical · **Category:** security · **Subsystem:** actions-filters-lenses-cards · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ActionController.php:534-642`
- **Evidence:** if (! $actionInstance->authorizedToSee($request)) {     return JsonErrorResponse::forbidden('This action is unauthorized.')->toResponse(); }  // ... no authorizedToRun($request, $model) loop, no authorizedToRunAction / authorizedToRunDestructiveAction check
- **Suggested fix:** Mirror the per-model authorization loop from `execute()` (lines 154-171) inside `executePivot()`. After resolving `$models`, iterate and call `$actionInstance->authorizedToRun($request, $model)` and `$resourceForModel->authorizedToRunAction()` / `authorizedToRunDestructiveAction()` exactly as the non-pivot path does. A destructive pivot action currently bypasses the delete-policy gate entirely.
- **Verifier:** The code confirms the finding. executePivot (lines 534-642) performs only one authorization check: authorizedToSee at line 566. After that guard passes, it resolves related models via the parent relationship (line 590) and immediately executes the action (lines 613-618) with no per-model authorizati …
- **Verifier fix note:** The fix is straightforward: after line 596 (the isSole check) and before field validation, add the same loop that execute() uses at lines 154-171. Guard it with if (! $actionInstance->isStandalone()), then iterate $models, call $actionInstance->authorizedToRun($request, $model), construct $resourceForModel = new $resourceClass($model), and branch on $actionInstance->isDestructive() to call authorizedToRunDestructiveAction or authorizedToRunAction. The suggested fix in the report is correct.

### Impersonation stop() endpoint missing authorization check — impersonated target can escalate to operator identity
- **Severity:** critical · **Category:** security · **Subsystem:** authz-gates-impersonation · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ImpersonationController.php:98-107`
- **Evidence:** public function stop(Request $request): JsonResponse {     if (! $this->impersonation->enabled()) {         return response()->json(['message' => 'Impersonation is disabled.'], 503);     }      $this->impersonation->stop();      return response()->json($this->impersonation->snapshot()); }
- **Suggested fix:** During an active impersonation session, the currently authenticated user (from the auth guard's perspective) is the TARGET, not the operator. The stop() endpoint has no check confirming the caller is the ORIGINAL operator. Because the endpoint only requires martis.auth (any authenticated user), the impersonated target can POST /stop. ImpersonationManager::stop() then reads stashed['original'] from …
- **Verifier:** Confirmed by reading ImpersonationController.php:98-107, ImpersonationManager.php:99-132, and routes/martis.php:232-237.  The `stop()` route sits inside the `martis.auth` + `martis.impersonation.duration` middleware group with no per-route gate check (routes/martis.php:236-237). The `stop()` control …
- **Verifier fix note:** The simplest correct fix is to add `Gate::allows('martis-impersonate')` to `stop()` exactly as done in `start()`. However, during impersonation the current guard user is the TARGET, who will not hold that gate — so the gate check will deny the stop correctly for the target while allowing the operator (who is not impersonating anyone and retains their original gate) to stop from their own session. Alternatively: stash the operator's session ID alongside `original` at start time and reject any `stop()` call arriving on a session ID that does not match. Either approach closes the escalation. The gate check is simpler and consistent with the stated two-layer design in the class docblock.

### MarkdownField: unsanitized marked.parse() output fed to dangerouslySetInnerHTML — stored XSS
- **Severity:** critical · **Category:** security · **Subsystem:** fields-special · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/MarkdownField.tsx:11-26, 65, 100`
- **Evidence:** function renderMarkdown(content: string, preset: string): string {   // ...   return marked.parse(content, { async: false }) as string } // ... <div   className="prose dark:prose-invert max-w-none text-sm"   dangerouslySetInnerHTML={{ __html: html }} />
- **Suggested fix:** Pass the output through DOMPurify (or isomorphic-dompurify) before injecting it. Add `dompurify` to package.json and wrap the return: `return DOMPurify.sanitize(marked.parse(content, { async: false }) as string)`. This applies to both the display path (line 65) and the preview path (line 100). `marked` v17 removed the deprecated `sanitize` option and does NOT strip `<script>`, `onerror=`, or other …
- **Verifier:** Direct code inspection confirms the finding on every cited line. `renderMarkdown()` (lines 11-26) passes `marked.parse(content, { async: false })` unsanitized for the `'default'` and `'commonmark'` presets; only the `'zero'` preset performs HTML-escaping. The raw return value is assigned to `html` v …
- **Verifier fix note:** The fix applies to `renderMarkdown()` itself so both call sites are covered in one change: `return DOMPurify.sanitize(marked.parse(content, { async: false }) as string)`. Add `dompurify` and `@types/dompurify` to `package.json` and import at the top of the file. The `'zero'` branch is already safe (manual HTML-escaping) and does not need DOMPurify. Also audit the other unsanitized `dangerouslySetInnerHTML` usages found in the same sweep: `TrixField.tsx` (raw DB value), `MartisTooltip.tsx` (author-controlled but still rendered in admin context), `GateModal.tsx` (`message`), and `FieldWrapper.tsx` (`help`) — these are lower-severity but share the same pattern.

## High (28)

### ExecuteAction job double-encodes original/changes columns — JSON stored as string instead of object
- **Severity:** high · **Category:** bug · **Subsystem:** actions-filters-lenses-cards · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Actions/Jobs/ExecuteAction.php:114-125`
- **Evidence:** ActionEvent::where(...)     ->update([         'status' => $status,         'exception' => $exception ?? '',         'original' => json_encode($originalDiff),         'changes' => json_encode($changesDiff),     ]);
- **Suggested fix:** Remove the `json_encode()` calls. `ActionEvent` declares `'original' => 'array'` and `'changes' => 'array'` casts. However, `Builder::update()` bypasses Eloquent casting, so the JSON-encoded string is stored literally. The `ActionController::logActionEvent()` path (line 463-478) passes raw arrays to `ActionEvent::create()`, which correctly triggers the cast. Either pass raw arrays to `update()` (a …
- **Verifier:** Confirmed by reading the actual code. ActionEvent declares `'original' => 'array'` and `'changes' => 'array'` casts (ActionEvent.php lines 56-58). The columns are `text` in the migration. At ExecuteAction.php lines 123-124, `json_encode($originalDiff)` and `json_encode($changesDiff)` are passed to ` …
- **Verifier fix note:** The simplest fix is to remove the explicit `json_encode()` calls at lines 123-124 and pass the raw PHP arrays directly to `Builder::update()`. However, note that `Builder::update()` still bypasses casts on write even without the manual `json_encode`, so the raw array would be passed to PDO as-is and PHP would serialize it as `Array` (lossy). The correct fix is to use model instances rather than a mass-update query: load the matching `ActionEvent` record, set its attributes, and call `save()`, which routes through `setAttribute()` and correctly applies the `'array'` cast. Alternatively, call `json_encode()` explicitly and remove the `'original'` and `'changes'` entries from the `casts()` array so the encoding contract is uniform across both create and update paths.

### Action execute() resolveModels() bypasses indexQuery, scopes, and authorizedToViewAny — IDOR on action target models
- **Severity:** high · **Category:** security · **Subsystem:** actions-filters-lenses-cards · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ActionController.php:300-323`
- **Evidence:** $result = $modelInstance->newQuery()->whereIn(     $modelInstance->getKeyName(),     $ids, )->get();
- **Suggested fix:** Apply `$resourceClass::applyScopes($request, $query)` and `$resourceClass::indexQuery($request, $query)` to the query inside `resolveModels()` before the `whereIn`, exactly as `ResourceController` does. Also add an `authorizedToViewAny` guard before loading models, matching the check in `LensController::index` line 50.
- **Verifier:** The finding is confirmed by reading the actual code.  resolveModels() at lines 300-323 of ActionController.php executes: `$modelInstance->newQuery()->whereIn($modelInstance->getKeyName(), $ids)->get()` with no applyScopes, no indexQuery, and no authorizedToViewAny guard. The IDs come directly from ` …
- **Verifier fix note:** The suggested fix is correct in substance. One precision: authorizedToViewAny should be called on `new $resourceClass` (a blank instance, no model) before resolveModels(), exactly as ResourceController line 89 does: `$instance->authorizedToViewAny($request)`. Inside resolveModels(), the query should be built as `$resourceClass::applyScopes($request, $modelInstance->newQuery())` followed by `$resourceClass::indexQuery($request, $query)` before the whereIn, to honour both global-scope injections and the developer's custom visibility hook. The severity is adjusted to high rather than critical because exploitation requires a valid authenticated session with access to the resource's action routes; unauthenticated access is not possible. In a multi-tenant or row-level-security deployment the impact is severe but the attack surface is narrower than a fully unauthenticated IDOR.

### LensRequest::withOrdering passes raw HTTP sort column to orderBy() with no whitelist validation
- **Severity:** high · **Category:** security · **Subsystem:** actions-filters-lenses-cards · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Requests/LensRequest.php:70-83`
- **Evidence:** public function withOrdering(Builder $query, ?Closure $default = null): Builder {     if ($this->sortColumn !== null && $this->sortColumn !== '') {         return $query->orderBy($this->sortColumn, $this->sortDirection->value);     }
- **Suggested fix:** Validate `$this->sortColumn` against the lens's declared sortable fields before injecting it into the query, exactly as `ResourceController::applySorting()` does (lines 1606-1616: builds `$sortableAttributes` from fields that return `isSortable() === true` and checks `in_array($sort, $sortableAttributes, true)` before calling `orderBy`). `LensController` should resolve the lens fields and pass the …
- **Verifier:** Reading the full call chain confirms the vulnerability exists with no mitigating guard:  1. `LensRequest::fromRequest` (LensRequest.php:113-114) assigns the raw HTTP `sort` query parameter to `$req->sortColumn` with no validation: `$req->sortColumn = is_string($rawSort) && $rawSort !== '' ? $rawSort …
- **Verifier fix note:** The fix should mirror exactly what ResourceController::applySorting does: resolve the lens's declared fields, build an allowlist of attributes where isSortable() returns true, and check the incoming sort column against that list before calling orderBy. The cleanest approach is to add a withOrdering overload (or a new method) on LensRequest that accepts an explicit $allowedColumns list, and have LensController build that list from the resolved lens fields before constructing the LensRequest — the same fields array that is already resolved at LensController.php:114 via resolveLensFields(). Developers who call withOrdering with no allowlist in their own Lens::query() implementations would need to update those callsites.

### MagicLinkController::autoRegister uses model constructor with potential mass-assignment silent failure
- **Severity:** high · **Category:** security · **Subsystem:** auth · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/MagicLinkController.php:133-138`
- **Evidence:** $user = new $userClass([     'email' => $email,     'name' => $email,     'password' => bcrypt(Str::random(40)), ]); $user->save();
- **Suggested fix:** The constructor path respects $fillable / $guarded on the consumer's User model. Laravel 11+ ships apps with $guarded = ['*'] by default (all attributes guarded). When auto_register=true and the consumer's model has strict guarding, all three attributes (email, name, password) are silently dropped, save() persists an empty/partial row, and the account is created with null email, null name, and nul …
- **Verifier:** The finding is confirmed by direct code inspection.  `MagicLinkController::autoRegister()` at lines 133-138 uses the Eloquent mass-assignment constructor (`new $userClass([...])`) which internally calls `fill()`, respecting the consumer model's `$fillable` / `$guarded`. If the consumer's User model  …
- **Verifier fix note:** The reporter's suggested fix is correct: replace `new $userClass([...])` with `(new $userClass)->forceFill([...])` to match `DefaultRegistersUsers::register()` at line 44. In addition, dispatch `event(new Registered($user))` after save (same as line 80 of DefaultRegistersUsers), and gate the method on `config('martis.auth.registration.enabled', true)` before creating the row. The severity is adjusted to high rather than medium because a null password can silently persist in the database and downstream password-authentication paths may behave unpredictably against a null hash; the only mitigating factor keeping this from critical is that `auto_register` defaults to false.

### LensController cache key missing user identity — cross-user data leak
- **Severity:** high · **Category:** security · **Subsystem:** cache · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/LensController.php:260-279`
- **Evidence:** private function buildCacheKey(     Lens $lensInstance,     LensRequest $lensRequest,     int $perPage,     int $page,     string $trashed = '',     string $tableVersion = '', ): string {     $parts = [         'martis-lens',         static::classHash($lensInstance),         $lensInstance->uriKey(),         $tableVersion,         $lensRequest->search,         $lensRequest->sortColumn ?? '',         $lensRequest->sortDirection->value,         md5(json_encode($lensRequest->selectedFilters) ?: ''), …
- **Suggested fix:** Add the authenticated user's identity to the $parts array: `(string) ($lensRequest->user()?->getAuthIdentifier() ?? 'guest')`. The Lens::query() receives the full LensRequest (which wraps the HTTP request), so user-scoped lens implementations (e.g. 'my orders') will have their results cached globally and served to every user who hits the same filter/sort/page combination. The fix is a one-liner ad …
- **Verifier:** Confirmed by reading both files directly.  In /Users/lmoura/projects/martis/martis-package/src/Lenses/Lens.php:69, `cacheTtlSeconds` defaults to `0`. The controller at /Users/lmoura/projects/martis/martis-package/src/Http/Controllers/LensController.php:145 only enters the cache path when `$ttl > 0`: …
- **Verifier fix note:** The fix is correct as stated. One additional hardening: consider whether "guest" (unauthenticated) lenses should be cacheable at all — if the controller already enforces authentication before this code path, the `?? 'guest'` fallback is dead code and could be omitted to avoid confusion. Verify the auth middleware on the martis API routes to confirm whether unauthenticated requests can ever reach LensController::index.

### Default manage-martis-cache gate allows any authenticated user to wipe/disable caches
- **Severity:** high · **Category:** security · **Subsystem:** cache · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/MartisServiceProvider.php:321-325`
- **Evidence:** Gate::define('manage-martis-cache', fn ($user) => $user !== null);
- **Suggested fix:** The default should be restrictive, not permissive. Replace with a deny-by-default that requires the host to explicitly grant: `Gate::define('manage-martis-cache', fn ($user) => false);`. The CacheController docblock already warns hosts to tighten this, but shipping a permissive default means every Martis install that does not override it exposes cache clear/disable/enable to any logged-in user. An …
- **Verifier:** The gate definition at MartisServiceProvider.php:325 is confirmed: `Gate::define('manage-martis-cache', fn ($user) => $user !== null);`. The cache routes (GET /api/cache, POST /api/cache/clear, POST /api/cache/disable, POST /api/cache/enable, POST /api/cache/reset-override) are registered inside the …
- **Verifier fix note:** The suggested fix (deny-by-default: `fn ($user) => false`) is correct and sufficient. An alternative that preserves usefulness out of the box would be to require a host-defined admin check, e.g. checking for a conventional `is_admin` boolean or a Martis-managed role if the package ships one. Either way, the default must not grant access to all authenticated users. The CacheController docblock and the stub in the published MartisServiceProvider should also be updated to show the override pattern prominently.

### InstallCommand::runMigrations() omits --force, causing interactive prompt in production and CI hang
- **Severity:** high · **Category:** bug · **Subsystem:** console · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Console/InstallCommand.php:1193-1195`
- **Evidence:** protected function runMigrations(): void     {         $this->call('migrate');         $this->components->twoColumnDetail('<fg=green>Executed</> migrations', 'Martis database changes applied');     }
- **Suggested fix:** Pass `['--force' => true]` to the migrate call, matching the pattern already used in RolesScaffoldCommand and SsoMakeCommand: `$this->call('migrate', ['--force' => true]);`. In production environments Laravel's `migrate` command requires explicit `--force` confirmation or it will interactively prompt 'Do you really want to run this command?' and block forever in CI or hang a Docker-compose exec in …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/src/Console/InstallCommand.php:1194 reads `$this->call('migrate');` with no arguments. In Laravel, when `APP_ENV=production` the `migrate` command guards itself with `confirmToProceed()`, which either throws an exception (in non-interactive/CI …
- **Verifier fix note:** Pass `['--force' => true]` to the migrate call: `$this->call('migrate', ['--force' => true]);`. This matches the pattern already used in RolesScaffoldCommand and SsoMakeCommand and is the standard Laravel idiom for installer commands that are expected to run non-interactively in production and CI pipelines.

### UserCommand accepts plaintext --password via CLI option, exposing credential in process list and shell history
- **Severity:** high · **Category:** security · **Subsystem:** console · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Console/UserCommand.php:14,27`
- **Evidence:** {--password= : The password for the admin user}';}     ...         $password = (string) ($this->option('password') ?? $this->secret('Password'));
- **Suggested fix:** The `--password=` option is a documented convenience that leaks the plaintext credential in `ps aux`, `/proc/*/cmdline`, and shell history on any multi-user system. Remove the option from the signature or mark it `@deprecated` and add a clear warning if it is used: `if ($this->option('password') !== null) { $this->components->warn('Passing --password on the CLI exposes it in the process list. Use  …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/src/Console/UserCommand.php:14 declares `{--password= : The password for the admin user}` in the command signature, and line 27 reads it with `$this->option('password')` with no guard, warning, or deprecation notice. A caller can run `php arti …
- **Verifier fix note:** The suggested fix is correct. The minimal safe change is to add a `$this->components->warn(...)` call when `$this->option('password') !== null`, directing users to the interactive prompt instead. Removing the option entirely is the safest path but may break existing automation scripts; a deprecation warning preserves backward compatibility while signaling the risk. The interactive `$this->secret()` path is already the right implementation.

### checkPolicy and checkRelationalPolicy ignore the passed $request and use the global request() helper for user resolution
- **Severity:** high · **Category:** security · **Subsystem:** core-registry-manager-provider · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Resource.php:788, 840`
- **Evidence:** // checkRelationalPolicy (line 788): $user = request()->user();  // checkPolicy (line 840): $user = request()->user();  // But callers pass $request and never thread it through: public function authorizedToViewAny(Request $request): bool {     return $this->checkPolicy('viewAny', null); // $request silently dropped }
- **Suggested fix:** Add `$request` as a parameter to both `checkPolicy(string $ability, ?Model $model, Request $request)` and `checkRelationalPolicy(string $ability, string $relatedModelClass, ?Model $relatedModel, Request $request)`, and pass `$request` from every caller. Replace `request()->user()` with `$request->user()` in both methods. This makes the auth check respect whichever request the caller has in scope ( …
- **Verifier:** The finding is confirmed by reading the actual code. Both `checkPolicy` (line 840) and `checkRelationalPolicy` (line 788) call `request()->user()` with no guard argument. The Laravel chain is: `Request::user(null)` -> `Auth::userResolver()(null)` -> `AuthManager::guard(null)->user()` -> resolves the …
- **Verifier fix note:** The fix should use `$request->user(config('martis.guard'))` rather than just `$request->user()`, because threading `$request` alone still resolves the default guard. The guard name must also be passed. Alternatively, a private helper `resolveAuthUser(Request $request): ?Authenticatable` centralizing `auth()->guard(config('martis.guard'))->user()` can replace both call sites consistently.

### Duplicate 'search' top-level key in config/martis.php silently drops default_limit and min_query
- **Severity:** high · **Category:** bug · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/config/martis.php:474-478 and 705-710`
- **Evidence:** Line 474: 'search' => [     'default_limit' => (int) env('MARTIS_SEARCH_DEFAULT_LIMIT', 5),     'min_query' => (int) env('MARTIS_SEARCH_MIN_QUERY', 2), ],  ... (231 lines later) ...  Line 705: 'search' => [     'enabled' => true,     'placeholder' => null,     'mode' => env('MARTIS_SEARCH_MODE', 'bar'),     'mobileMode' => env('MARTIS_SEARCH_MOBILE_MODE', 'icon'), ],
- **Suggested fix:** Merge both 'search' blocks into one. The final array should include all six keys: default_limit, min_query, enabled, placeholder, mode, mobileMode. The SearchController at src/Http/Controllers/SearchController.php:169-170 reads config('martis.search.default_limit') and config('martis.search.min_query') — because PHP replaces the first 'search' block with the second, these keys are absent at runtim …
- **Verifier:** The config file is a single PHP array returned at line 3. Two top-level 'search' keys exist: line 474 (default_limit, min_query) and line 705 (enabled, placeholder, mode, mobileMode). PHP silently discards the first duplicate key, so at runtime config('martis.search') contains only the four keys fro …
- **Verifier fix note:** Merge both 'search' blocks into one at either location. The merged block must include all six keys: default_limit, min_query, enabled, placeholder, mode, mobileMode. Remove the now-redundant standalone block. After merging, config('martis.search.default_limit') and config('martis.search.min_query') will resolve correctly from the env vars. The config() fallback defaults in SearchController can stay as safety floors but will no longer be the primary source.

### XSS: UrlField.tsx renders href without scheme allowlist — stored javascript:// executes on click
- **Severity:** high · **Category:** security · **Subsystem:** fields-base-textlike · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/UrlField.tsx:15`
- **Evidence:** <a   href={url}   target="_blank"   rel="noopener noreferrer" >
- **Suggested fix:** Either sanitise on the backend (the correct fix — see Url.php finding above) or add a frontend guard: const safeUrl = /^https?:\/\//i.test(url) ? url : '#'; then use href={safeUrl}. Both layers should be hardened; defence in depth applies here.
- **Verifier:** Confirmed by reading the actual code. UrlField.tsx line 15 renders `href={url}` with no scheme guard. The PHP backend (Url.php) does not block javascript: URLs: normaliseUrl() uses the regex `#^[a-z][a-z0-9+\-.]*://#i` (line 97), which matches `javascript://` as a valid RFC 3986 scheme and returns i …
- **Verifier fix note:** The correct fix is a scheme allowlist on the backend in Url.php, not just the frontend. In buildRules(), after FILTER_VALIDATE_URL passes, add a check that the stored scheme is http or https: parse_url($normalised, PHP_URL_SCHEME) must be in ['http', 'https']. The fill() method should also reject or strip non-http(s) schemes. The frontend guard (const safeUrl = /^https?:\/\//i.test(url) ? url : '#') is defence in depth but not a substitute for the backend fix, because values can be set programmatically (resolveUsing callbacks, seeders, API writes) bypassing form validation entirely.

### fill() ignores closure-based readonly — writes proceed when readonly(Closure) is set
- **Severity:** high · **Category:** bug · **Subsystem:** fields-base-textlike · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Field.php:312-316`
- **Evidence:** public function fill(Model $model, mixed $value): void {     if ($this->readonly) {         return;     }
- **Suggested fix:** Replace `if ($this->readonly)` with `if ($this->isReadonly())`. The `isReadonly()` method already handles the Closure path correctly (lines 440-449). The same bug exists verbatim in Password::fill() at line 148 — fix both. Slug::fill() calls parent::fill() so it inherits the fix automatically.
- **Verifier:** Confirmed by reading the source directly.  `readonly(bool|\Closure $value)` at Field.php line 422-433 only mirrors the value into `$this->readonly` when `is_bool($value)` is true (line 428-429). When a Closure is passed, only `$this->readonlyResolver` is set; `$this->readonly` remains `false` (its i …
- **Verifier fix note:** The fix is correct as stated. Two files need the one-line change: `Field.php:314` and `Password.php:148`. No other fill() overrides share the pattern — Slug delegates to parent so it inherits the fix automatically. After the fix, also confirm there are no remaining direct reads of `$this->readonly` outside of `isReadonly()` itself (the grep above shows line 341 in `toArray()` already calls `$this->isReadonly()`, and line 448 inside `isReadonly()` is the correct terminal read — both are fine).

### XSS: Url field accepts javascript:// and data:// schemes — rendered as raw href
- **Severity:** high · **Category:** security · **Subsystem:** fields-base-textlike · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Url.php:75`
- **Evidence:** if (filter_var($normalised, FILTER_VALIDATE_URL) === false) { ... $fail(...); }
- **Suggested fix:** Before FILTER_VALIDATE_URL, add an explicit scheme allowlist: $allowed = ['http', 'https', 'ftp', 'ftps']; $scheme = parse_url($normalised, PHP_URL_SCHEME); if ($scheme === null || !in_array(strtolower($scheme), $allowed, true)) { $fail(...); return; } — The regex at normaliseUrl() line 97 already lets 'javascript://' through unchanged because it has the '://' pattern. FILTER_VALIDATE_URL returns  …
- **Verifier:** Direct PHP execution confirmed: `filter_var('javascript://alert(document.cookie)', FILTER_VALIDATE_URL)` returns the input string (truthy), not `false`. PHP's FILTER_VALIDATE_URL follows RFC 3986, which considers `javascript:` a valid scheme and `//alert(document.cookie)` a valid authority component …
- **Verifier fix note:** The suggested fix is correct in principle but needs two adjustments. First, apply the scheme allowlist check inside `normaliseUrl` (or as a separate guard called first in `buildRules`) before `FILTER_VALIDATE_URL`, not after — the regex at line 97 passes all `://` forms through unchanged so the allowlist must run before that path returns. Second, the allowlist should use `strtolower(parse_url($trimmed, PHP_URL_SCHEME))` on the original trimmed value (not on `$normalised` post-prepend) because `normaliseUrl` can change the string. Recommended allowlist: `['http', 'https', 'ftp', 'ftps']`. Also fix `fill()` at Url.php:104: it calls `normaliseUrl` but does not re-validate the scheme, so a `javascript://` value that passed validation can still reach the model via `fill()`. On the TSX side, add a runtime guard in `UrlFieldDisplay` before rendering the href: `const safe = /^https?:\/\//i.test(url)` and render a plain `<span>` instead of an `<a>` when the check fails, as a defence-in-depth measure independent of server-side validation.

### fillMultiple keepPaths not validated against model-owned paths — data corruption + file deletion
- **Severity:** high · **Category:** security · **Subsystem:** fields-media · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/File.php:325-350`
- **Evidence:** $keepPaths = []; foreach ($rawExisting as $p) {     if (is_string($p) && $p !== '') {         $keepPaths[] = $p;     } }  // Delete files that are no longer kept $removedPaths = array_diff($existingPaths, $keepPaths); foreach ($removedPaths as $path) {     $this->deletePathFromDisk($path); }  $allPaths = array_merge($keepPaths, $newPaths); $model->setAttribute($this->attribute, json_encode($allPaths));
- **Suggested fix:** Replace the keepPaths collection with an intersection check so only paths already stored in the model can be retained: `$keepPaths = array_values(array_intersect($rawExisting, $existingPaths));`. The same fix is needed in Image::fillMultiple (Image.php lines 210-238). Without this, an authenticated user can send arbitrary strings in `{attr}_keep[]` via a direct API POST. This causes (a) the model' …
- **Verifier:** Read /Users/lmoura/projects/martis/martis-package/src/Fields/File.php lines 306-351 directly. The `fillMultiple` method at line 325-330 builds `$keepPaths` by iterating over `$rawExisting`, which is `$value['existing']`. At line 333, `$removedPaths = array_diff($existingPaths, $keepPaths)` — everyth …
- **Verifier fix note:** The suggested fix is correct: replace the `$keepPaths` collection loop with `$keepPaths = array_values(array_intersect($rawExisting, $existingPaths))`. This ensures only paths already stored on the model can be retained, so an attacker-supplied list of arbitrary strings cannot (a) cause stored files to be deleted by exclusion or (b) inject foreign paths into the JSON column. The same fix must be applied to Image::fillMultiple at /Users/lmoura/projects/martis/martis-package/src/Fields/Image.php lines 209-238, which is an exact structural copy of the vulnerable pattern.

### Closure-based ->readonly() is never evaluated in fill() across all relation fields
- **Severity:** high · **Category:** bug · **Subsystem:** fields-relation · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Tag.php:250`
- **Evidence:** public function fill(Model $model, mixed $value): void {     if ($this->readonly) {         return;     }
- **Suggested fix:** Replace `if ($this->readonly)` with `if ($this->isReadonly())` in every fill() override that copies this pattern: Tag::fill (line 250), BelongsTo::fill (line 311), MorphTo::fill (line 286), MultiSelect::fill (line 195), and the base Field::fill (line 314). The Field::readonly() setter at lines 422-430 already documents that 'Closure values defer until isReadonly() is called' — the fill() implement …
- **Verifier:** The bug is confirmed by direct code reading. `Field::readonly()` at Field.php:422-430 only sets `$this->readonly = $value` when `is_bool($value)`; when a Closure is passed it stores it in `$readonlyResolver` only, leaving `$this->readonly` at its default `false`. The comment on line 427 explicitly d …

### DateTime extends Date but never sets withTime=true, so resolve() strips the time portion from DateTimeInterface values
- **Severity:** high · **Category:** bug · **Subsystem:** fields-special · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/DateTime.php:1-20`
- **Evidence:** class DateTime extends Date {     public function type(): string     {         return 'datetime';     } }
- **Suggested fix:** Add an initializer that sets the correct defaults, e.g. override `make()` to call `->withTime(true)`, or add a constructor that calls `parent::__construct($attribute, $label)` and then `$this->withTime(true)`. Without this, `Date::resolve()` formats any `\DateTimeInterface` value with `displayFormat = 'Y-m-d'`, silently dropping the time portion. `extraAttributes()` also emits `withTime: false` an …
- **Verifier:** DateTime.php (/Users/lmoura/projects/martis/martis-package/src/Fields/DateTime.php) is exactly 20 lines and only overrides `type()` to return `'datetime'`. It has no constructor, no `make()` override, and no property initializer. It inherits Date's defaults verbatim: `protected bool $withTime = fals …

### AuthController::user() 2FA pending check uses === false, misses the null initial session state — SPA bootstrap does not redirect to challenge for users who logged in via SSO or magic link
- **Severity:** high · **Category:** bug · **Subsystem:** http-auth-sso-ctrl · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/AuthController.php:53-59`
- **Evidence:** $twoFactorPassed = $request->session()->get('martis_two_factor_passed'); $twoFactor = app(TwoFactorService::class); if ($twoFactor->isEnabled($user) && $twoFactorPassed === false) {     return response()->json([         'two_factor_pending' => true,         ...     ]); }
- **Suggested fix:** The session key is absent (null) for users who logged in via SSO or magic link, and also on any session that predates the 2FA flag introduction. `null === false` is `false`, so the pending check is silently skipped. The EnsureTwoFactorChallenge middleware correctly gates with `!$request->session()->get(...)` (truthiness), so the two surfaces are asymmetric.  Change the condition to: ```php if ($tw …
- **Verifier:** The defect is confirmed by reading the actual code. At AuthController.php:55 the condition is `$twoFactorPassed === false` (strict). Session key `martis_two_factor_passed` is only ever explicitly put to `false` by the password-login path (AuthController::login() line 131). SSO login (SsoController)  …
- **Verifier fix note:** The suggested fix is correct. One additional note: after applying `!== true`, also verify that the `two_factor_pending` response shape sent by `user()` is handled by the SPA the same way as the `two_factor_required` shape sent by `login()`. The login flow returns `two_factor_required` (line 133) while the user-bootstrap flow returns `two_factor_pending` (line 57); confirm the React bootstrap handler checks for `two_factor_pending` specifically, not just `two_factor_required`.

### LensController cache key excludes user identity, enabling cross-user data leakage when TTL > 0
- **Severity:** high · **Category:** security · **Subsystem:** http-misc · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/LensController.php:252-280`
- **Evidence:** private function buildCacheKey(     Lens $lensInstance,     LensRequest $lensRequest,     int $perPage,     int $page,     string $trashed = '',     string $tableVersion = '', ): string {     $parts = [         'martis-lens',         static::classHash($lensInstance),         $lensInstance->uriKey(),         $tableVersion,         $lensRequest->search,         $lensRequest->sortColumn ?? '',         $lensRequest->sortDirection->value,         md5(json_encode($lensRequest->selectedFilters) ?: ''), …
- **Suggested fix:** Add `$userKey` to the cache key parts. The user identifier must be part of the key whenever `cacheTtl() > 0` because lenses filter records via `authorizedToViewAny` and consumer-defined `indexQuery` scoping — different users legitimately see different row sets. Pattern to follow: `$request->user()?->getAuthIdentifier() ?? 'guest'` as used in NavigationController and ResourceController.
- **Verifier:** The defect is confirmed by reading the actual code.  buildCacheKey() at LensController.php:252-280 assembles 12 parts (lens class hash, uriKey, tableVersion, search, sortColumn, sortDirection, filter hash, trashed, perPage, page, locale) and does NOT include any user identifier. The $request object  …
- **Verifier fix note:** The fix must add the user identifier to buildCacheKey() and pass $request (or the resolved identifier) through from index(). The private signature should become buildCacheKey(Lens, LensRequest, int, int, string, string, string $userKey): string, where $userKey = (string) ($request->user()?->getAuthIdentifier() ?? 'guest') is resolved in index() and appended to $parts. This matches the pattern already used in NavigationController and ResourceController. No additional guard (e.g. "only add user key when cacheTtl > 0") is needed because buildCacheKey() is only called unconditionally from index() and the Cache::remember() branch is already gated on $ttl > 0 — an unused cache key is harmless.

### syncField context=update checks wrong gate — view-only user can probe update-field metadata and reactive callbacks
- **Severity:** high · **Category:** security · **Subsystem:** http-resource · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ResourceController.php:728`
- **Evidence:** if ($context === 'update' && ! $instance->authorizedToCreate($request) && ! $instance->authorizedToViewAny($request)) {
- **Suggested fix:** The condition uses an OR-conjunction: it blocks only when BOTH authorizedToCreate AND authorizedToViewAny are false. A user who has viewAny but not update (read-only role) passes the gate and can invoke syncField with context=update to trigger reactive callbacks and enumerate update-field definitions. Replace with `! $instance->authorizedToUpdate($request)` — or for context=create, keep `! $instan …
- **Verifier:** The finding is confirmed by direct code inspection.  Line 728 of /Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ResourceController.php:  ```php if ($context === 'update' && ! $instance->authorizedToCreate($request) && ! $instance->authorizedToViewAny($request)) { ```  By De Morgan …
- **Verifier fix note:** The fix direction is correct. One nuance: `authorizedToUpdate` calls `checkPolicy('update', $this->model)` and since the resource is constructed as `new $resourceClass` with no model, `$this->model` is null. `checkPolicy` then calls `$policy->update($user)` with no model argument. Consumer policies whose `update()` signature requires a `Model $model` argument will throw a TypeError at runtime. The safe fix is to either (a) pass a fresh model instance (`new (static::model())`) so the policy call is valid, or (b) add a dedicated `authorizedToUpdateAny()` helper analogous to `authorizedToViewAny()` that checks the `update` ability without a model. The `authorizedToCreate` call on line 731 uses the same null-model pattern and would have the same issue, so this is a pre-existing systemic concern across the syncField gate, not introduced by the fix.

### show?context=update returns raw update-field values to users with view but not update permission
- **Severity:** high · **Category:** security · **Subsystem:** http-resource · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ResourceController.php:213-228`
- **Evidence:** if (! $res->authorizedToView($request)) {     return JsonErrorResponse::forbidden('This action is unauthorized.')->toResponse(); }  $context = $request->query('context', 'detail'); $forDisplay = true; if ($context === 'update') {     $fields = Field::filterForContext($res->fieldsForUpdate($request), FieldContext::UPDATE);     $forDisplay = false; } elseif ($context === 'create') {     $fields = Field::filterForContext($res->fieldsForCreate($request), FieldContext::CREATE);     $forDisplay = fals …
- **Suggested fix:** The show endpoint checks only authorizedToView regardless of the requested context. When context=update it should additionally check authorizedToUpdate, and when context=create it should check authorizedToCreate. A view-only user can currently call GET /api/resources/{resource}/{id}?context=update and receive unformatted raw field values that are only meant for the edit form. Add a context-specifi …
- **Verifier:** Confirmed by reading the actual code. The `show` action in `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ResourceController.php` at lines 213-228 performs a single authorization gate: `authorizedToView`. After that gate passes, it branches on the `?context` query parameter with  …
- **Verifier fix note:** The suggested fix is correct and sufficient. Insert both checks immediately after the `authorizedToView` guard (after line 215) and before the `$context = $request->query(...)` line. No other compensating mechanism exists in the route layer, middleware, or policy resolver that would catch this. The `context=create` branch needs its own `authorizedToCreate` check for the same reason.

### TrendMetric uses MySQL-only DATE_FORMAT — breaks on Postgres and SQLite **[FIXED]**
- **Severity:** high · **Category:** bug · **Subsystem:** metrics-rest · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Metrics/TrendMetric.php:153`
- **Evidence:** ->select(DB::raw("DATE_FORMAT({$dateColumn}, '{$dateFormat}') as date_key"), $expression)
- **Suggested fix:** Branch on the DB driver and use the driver-appropriate expression: MySQL → DATE_FORMAT, Postgres → TO_CHAR with its own format tokens (%Y→YYYY, %m→MM, etc.), SQLite → strftime. A clean approach is a private helper `dateFormatExpression(string $column, TrendPeriod $unit): string` that switches on `DB::getDriverName()`. Day: MySQL `'%Y-%m-%d'`, Postgres `'YYYY-MM-DD'`, SQLite `'%Y-%m-%d'`. Month: My …
- **Verifier:** The cited code exists exactly as described. At /Users/lmoura/projects/martis/martis-package/src/Metrics/TrendMetric.php line 153, the only SELECT on date bucketing is:    ->select(DB::raw("DATE_FORMAT({$dateColumn}, '{$dateFormat}') as date_key"), $expression)  The $dateFormat values are pure MySQL  …
- **Verifier fix note:** The suggested fix is correct. A private helper dateFormatExpression(string $column, TrendPeriod $unit): string that branches on DB::getDriverName() and emits the driver-appropriate SQL expression is the right approach. The week case deserves extra care on PostgreSQL: %x-%v has no direct TO_CHAR equivalent; the cleanest portable alternative is to cast to the ISO week-start date using date_trunc('week', col)::date and format that as YYYY-MM-DD, then align the PHP CarbonPeriod key format accordingly.

### TrendMetric casts named range strings ('TODAY', 'MTD', 'QTD', 'YTD') to int 0 — silently returns empty chart
- **Severity:** high · **Category:** bug · **Subsystem:** metrics-rest · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Metrics/TrendMetric.php:131`
- **Evidence:** $range = (int) $request->query('range', '30');
- **Suggested fix:** The base Metric::ranges() includes 'TODAY', 'MTD', 'QTD', 'YTD' as valid range keys, and TrendMetric does not override ranges(). When a user selects any named range, (int)'TODAY' evaluates to 0, making $startDate == $now (subDays(0)), and the query window collapses to a single instant returning no rows and an empty or single-point series. Fix: either (a) override ranges() in TrendMetric to return  …
- **Verifier:** Confirmed by reading the source. Metric::ranges() (Metric.php:191-215) returns 'TODAY', 'MTD', 'QTD', 'YTD' as valid keys alongside numeric ones, and TrendMetric does not override ranges() — those four options are therefore presented to the user in every trend card. TrendMetric::aggregateByPeriod()  …
- **Verifier fix note:** The simplest fix mirrors the ValueMetric pattern exactly: in aggregateByPeriod(), read the raw string first (`$rangeRaw = $request->query('range', '30');`), resolve via `MetricRange::tryFrom($rangeRaw)`, then derive a `$range` integer only for the null (numeric) branch. For the named presets, map to day counts before feeding the existing subDays/subWeeks/subMonths match — or add named-range branches to that match directly (e.g. MetricRange::Today maps to subDays(1) for a day-granularity chart). An alternative is to override ranges() in TrendMetric to exclude the four named presets entirely, but that removes legitimate functionality. The reporter's option (b) — resolve via MetricRange::tryFrom() — is the correct approach.

### 2FA setup silently overwrites confirmed secret — no password/OTP guard
- **Severity:** high · **Category:** security · **Subsystem:** notif-pref-profile · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Profile/TwoFactorService.php:45-48`
- **Evidence:** // Store pending secret on user (not confirmed yet) assert($user instanceof Model); $user->two_factor_secret = encrypt($secret); $user->save();
- **Suggested fix:** In `generateSetup()`, check whether the user already has a confirmed 2FA (`$this->isEnabled($user)`) and refuse (throw `\InvalidArgumentException`) unless the caller supplies a valid current password or current OTP. The corresponding `twoFactorSetup` route in `ProfileController` should then validate `current_password` (via `current_password` rule) before delegating — identical to how `twoFactorDis …
- **Verifier:** Confirmed by reading the actual code. `ProfileController::twoFactorSetup()` (line 119-125) calls `TwoFactorService::generateSetup()` with no prior credential check. The method unconditionally overwrites `two_factor_secret` and saves (TwoFactorService.php:47-48). The route at martis.php:309 sits insi …
- **Verifier fix note:** The severity is high rather than critical because exploitation requires an already-authenticated session (not just any unauthenticated request) — the attacker must first steal a valid session where 2FA has already been passed. A critical rating would apply if the route were unauthenticated. The fix is exactly as suggested: in `ProfileController::twoFactorSetup`, add `$request->validate(['current_password' => ['required', 'string', 'current_password']]);` before delegating to `generateSetup()`, mirroring the guard already present on `twoFactorDisable`. No change is required inside `TwoFactorService::generateSetup()` itself — the guard belongs at the controller layer where HTTP context is available.

### IDOR: executePivot loads parent record without authorizedToView check
- **Severity:** high · **Category:** security · **Subsystem:** sec-authz-sweep · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ActionController.php:546-568`
- **Evidence:** $modelClass = $resourceClass::model(); $parentModel = $modelClass::find($id); if ($parentModel === null) {     return JsonErrorResponse::notFound("Parent record [{$id}] not found.")->toResponse(); }  if (! method_exists($parentModel, $relationship)) { ... }  $instance = new $resourceClass; $actionInstance = $this->findAction($instance, $action, $request); ... if (! $actionInstance->authorizedToSee($request)) {     return JsonErrorResponse::forbidden('This action is unauthorized.')->toResponse(); …
- **Suggested fix:** After instantiating $parentInstance = new $resourceClass($parentModel) (line 571), add: if (! $parentInstance->authorizedToView($request)) { return JsonErrorResponse::forbidden('This action is unauthorized.')->toResponse(); }  Also add the authorizedToRun per-model loop from execute() (lines 154-170) for non-standalone pivot actions, matching the pattern in execute(). Compare with BelongsToManyCon …
- **Verifier:** The code at ActionController.php:546-571 confirms the gap. After loading $parentModel = $modelClass::find($id) at line 547, the method constructs $parentInstance = new $resourceClass($parentModel) at line 571 but never calls $parentInstance->authorizedToView($request) before proceeding. The only aut …
- **Verifier fix note:** The suggested fix is correct: after line 571 ($parentInstance = new $resourceClass($parentModel)), add the authorizedToView guard before resolvePivotColumns. Additionally, consider adding an authorizedToRun loop over $models after line 590 (once the related models are loaded), mirroring the execute() pattern at lines 154-170, to cover per-related-record authorization for non-standalone pivot actions.

### Unsanitized sort column in BelongsToManyController — arbitrary column enumeration **[FIXED]**
- **Severity:** high · **Category:** security · **Subsystem:** sec-injection-sweep · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/BelongsToManyController.php:82-83`
- **Evidence:** if (is_string($rawSort) && $rawSort !== '') {     $query->orderBy($rawSort, $direction->value); }
- **Suggested fix:** Mirror the pattern used in HasManyController::applySorting() (lines 475-507): extract sortable attributes via Field::flattenLayoutFields() on the related resource, then guard with in_array($sort, $sortableAttributes, true) before calling orderBy(). The $relatedResourceClass variable is already in scope at this block.
- **Verifier:** The cited code at BelongsToManyController.php:82-83 is confirmed real. Lines 77-84 of the `index` method read `$rawSort` directly from `$request->query('sort')` and pass it to `$query->orderBy($rawSort, $direction->value)` with no allowlist check. The only guard is `is_string($rawSort) && $rawSort ! …
- **Verifier fix note:** The fix is correct as described. Use `$relatedResourceClass` (already in scope from `$ctx`) to instantiate the related resource, call `Field::flattenLayoutFields($instance->fields($request))`, extract sortable attribute names, and guard with `in_array($rawSort, $sortableAttributes, true)` before calling `orderBy`. Also check MorphToManyController — it extends the same pattern and may carry the same omission.

### TrendMetric uses MySQL-only DATE_FORMAT — fatal on SQLite/Postgres **[FIXED]**
- **Severity:** high · **Category:** implementation · **Subsystem:** sec-injection-sweep · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Metrics/TrendMetric.php:153`
- **Evidence:** ->select(DB::raw("DATE_FORMAT({$dateColumn}, '{$dateFormat}') as date_key"), $expression)
- **Suggested fix:** Detect the driver from $model::query()->getConnection()->getDriverName() (the same pattern SearchResolver already uses at line 110-113). Branch the raw expression: MySQL keeps DATE_FORMAT; SQLite uses strftime(); Postgres uses TO_CHAR(). The SearchResolver already shows the safe driver-detection idiom. The week format '%x-%v' needs per-dialect equivalents (SQLite: strftime('%Y-W%W'); Postgres: TO_ …
- **Verifier:** Confirmed by direct code reading. At /Users/lmoura/projects/martis/martis-package/src/Metrics/TrendMetric.php:153, the `aggregateByPeriod` method unconditionally emits:    ->select(DB::raw("DATE_FORMAT({$dateColumn}, '{$dateFormat}') as date_key"), $expression)  There is no driver-detection guard an …
- **Verifier fix note:** The fix requires three dialect branches for the raw SELECT expression. MySQL keeps `DATE_FORMAT(col, fmt)`. SQLite uses `strftime(fmt, col)` with format remapping: `%Y-%m-%d` stays the same, `%Y-%m` stays the same, and `%x-%v` has no direct equivalent — the safest SQLite approximation is `strftime('%Y', col) || '-W' || printf('%02d', strftime('%W', col))` (uses ISO-week-of-year via `%W`). PostgreSQL uses `TO_CHAR(col, fmt)` with remapping: `%Y-%m-%d` becomes `YYYY-MM-DD`, `%Y-%m` becomes `YYYY-MM`, `%x-%v` becomes `IYYY-IW`. Driver detection should follow the `SearchResolver` idiom at lines 110-113: `$model::query()->getConnection()->getDriverName()`. The `groupBy('date_key')` and `pluck('aggregate', 'date_key')` calls are portable and require no change.

### on_no_role_match hook always fires even when strategy = 'deny', allowing callback to bypass deny
- **Severity:** high · **Category:** security · **Subsystem:** sso · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/SsoController.php:92-107`
- **Evidence:** if ($roles->isEmpty()) {     $cfg = config("martis.auth.sso.providers.{$provider}", []);     $strategy = (string) ($cfg['on_no_role_match'] ?? 'deny');      $hookResult = $this->manager->fireNoRoleMatch($identity, $provider);     if ($hookResult instanceof RedirectResponse) {         return $hookResult;     }      if ($strategy === 'deny') {         return redirect()->route('martis.login')->withErrors(...);     }     // strategy === 'guest' falls through }
- **Suggested fix:** The `fireNoRoleMatch` hook is fired unconditionally regardless of `strategy`. The docblock on `SsoManager::onNoRoleMatchUsing` says the callback is active only when `on_no_role_match` is `'callable'`, but the controller does not enforce that. A hook that returns a non-RedirectResponse value (e.g. null) is silently ignored, and control falls through to the `strategy` check — but a hook that returns …
- **Verifier:** The bug is confirmed by reading both files directly.  In SsoController.php lines 92-99, `fireNoRoleMatch` is called unconditionally whenever `$roles->isEmpty()`, before the `$strategy` check:  ```php $hookResult = $this->manager->fireNoRoleMatch($identity, $provider); if ($hookResult instanceof Redi …
- **Verifier fix note:** The fix must be applied in SsoController.php at the call site. Replace the unconditional `fireNoRoleMatch` call with a strategy-gated one:

```php
if ($strategy === 'callable') {
    $hookResult = $this->manager->fireNoRoleMatch($identity, $provider);
    if ($hookResult instanceof RedirectResponse) {
        return $hookResult;
    }
}

if ($strategy === 'deny') {
    return redirect()->route('martis.login')->withErrors(['sso' => __('martis::messages.sso_no_role_match')]);
}
```

No change needed inside SsoManager — the enforcement belongs at the call site where the strategy is already known. After the fix, the docblock on `onNoRoleMatchUsing` will accurately describe both the intention and the runtime behaviour.

### Dead code branch in accessToken resolution always produces null for method_exists check
- **Severity:** high · **Category:** bug · **Subsystem:** sso · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Sso/Providers/AzureProvider.php:62`
- **Evidence:** $accessToken = method_exists($user, 'token') ? null : null;
- **Suggested fix:** Both branches of the ternary return `null`, so `$accessToken` is always initialized to `null` regardless of whether `token` is a method on the Socialite user object. The real token is then picked up only if `property_exists($user, 'token')` is true (line 63-65). The `method_exists` ternary is dead code that adds confusion but, more critically, if the Socialite driver exposes the token as a method  …
- **Verifier:** Line 62 of /Users/lmoura/projects/martis/martis-package/src/Sso/Providers/AzureProvider.php reads:      $accessToken = method_exists($user, 'token') ? null : null;  Both branches of the ternary return `null`, so the `method_exists` check is pure dead code — the result is always `null` regardless of  …
- **Verifier fix note:** The simplest correct fix is to collapse the dead ternary and the property_exists branch into a single expression that also handles method-based access. For the `socialiteproviders/microsoft` driver specifically, `token` is a public property on the AbstractUser base class, so the property_exists path happens to work in practice for that driver. However the code as written is fragile and will silently break for any driver that exposes the token differently. Recommended replacement for lines 62-66:

    $accessToken = $user->token                                    // public property (most drivers)
        ?? (method_exists($user, 'getToken') ? $user->getToken() : null)  // method fallback
        ?? (method_exists($user, 'token')    ? $user->token()    : null); // callable fallback

Remove the dead `method_exists($user, 'token') ? null : null` ternary entirely. The `$accessToken ?? ''` on line 68 and the empty-string guard on line 88 can stay as-is.

## Medium (57)

### ActionController returns HTTP 404 instead of 403 for authorization failures on individual models
- **Severity:** medium · **Category:** security · **Subsystem:** actions-filters-lenses-cards · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ActionController.php:157-168`
- **Evidence:** if (! $actionInstance->authorizedToRun($request, $model)) {     return JsonErrorResponse::notFound('You are not authorized to run this action on one or more selected resources.')->toResponse(); } // ... if (! $resourceForModel->authorizedToRunDestructiveAction($request)) {     return JsonErrorResponse::notFound('You are not authorized to run this destructive action.')->toResponse(); } if (! $resourceForModel->authorizedToRunAction($request)) {     return JsonErrorResponse::notFound('You are not  …
- **Suggested fix:** Return `JsonErrorResponse::forbidden(...)` (HTTP 403) for all three authorization failure branches. Using 404 for authorization failures is an intentional obfuscation pattern in some APIs, but here the message body explicitly says 'not authorized', which negates the security benefit while confusing API consumers. If the team decides to keep 404 for security-through-obscurity, the message should be …
- **Verifier:** Confirmed by reading the actual code. In `ActionController::execute()` (lines 156-168), three per-model authorization branches all call `JsonErrorResponse::notFound(...)`, which the `JsonErrorResponse` class (line 86-89) maps to HTTP 404. The message bodies are "You are not authorized to run this ac …
- **Verifier fix note:** Replace all three `JsonErrorResponse::notFound(...)` calls in the per-model authorization loop (ActionController.php lines 157, 163, 167) with `JsonErrorResponse::forbidden(...)`. The `forbidden()` factory is already imported and used correctly at line 143 in the same method. No other changes are needed — the message strings can stay as-is once the status code is corrected.

### LensRequest::withFilters does not guard against null/empty values before calling filter->apply()
- **Severity:** medium · **Category:** bug · **Subsystem:** actions-filters-lenses-cards · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Requests/LensRequest.php:44-62`
- **Evidence:** foreach ($this->selectedFilters as $uriKey => $value) {     $filter = $this->availableFilters[$uriKey] ?? null;     if ($filter === null) {         continue;     }     if (method_exists($filter, 'apply')) {         $filter->apply($this, $generic, $value); // no null/empty guard     } }
- **Suggested fix:** Add the same null/empty guard used in `ResourceController::applyFilters()` (line 1570): `if ($value === null || $value === '') { continue; }` before calling `$filter->apply()`. Without this guard, a `DateFilter::apply()` receiving `null` passes `null` directly to `whereDate($column, $op, null)`, which produces `WHERE date_col = ''` or a driver-specific error. `DateRangeFilter::apply()` is protecte …
- **Verifier:** The finding is confirmed by direct code reading.  LensRequest::fromRequest() (lines 98-108) stores the raw JSON-decoded filter map into $req->selectedFilters with no null/empty filtering at intake. A payload like {"created_at": null} or {"created_at": ""} is valid JSON and produces an array entry wi …
- **Verifier fix note:** The fix is correct as stated: add `if ($value === null || $value === '') { continue; }` in LensRequest::withFilters() before the method_exists/apply call, mirroring ResourceController::applyFilters() line 1570. No change to DateFilter itself is needed since the guard belongs at the dispatch site.

### 2FA bypass on /api/auth/user: strict === false check misses null (session key absent)
- **Severity:** medium · **Category:** security · **Subsystem:** auth · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/AuthController.php:53-55`
- **Evidence:** $twoFactorPassed = $request->session()->get('martis_two_factor_passed'); $twoFactor = app(TwoFactorService::class); if ($twoFactor->isEnabled($user) && $twoFactorPassed === false) {
- **Suggested fix:** The /api/auth/user endpoint is intentionally public (outside the martis.2fa middleware group) and performs its own 2FA gate. The session key martis_two_factor_passed is set to false only by AuthController::login() when a password login triggers 2FA. When a user arrives via magic-link (MagicLinkController::consume), SSO, or any other path that never calls login(), the key is absent from the session …
- **Verifier:** The defect is confirmed by reading the actual code.  AuthController::user() at line 55 uses `$twoFactorPassed === false` (strict equality). When a user authenticates via MagicLinkController::consume() (lines 107-108) or SsoController::callback() (lines 119-127), neither controller writes `martis_two …
- **Verifier fix note:** The suggested fix is correct. Change line 55 from `$twoFactorPassed === false` to `!$twoFactorPassed` (or equivalently `!$request->session()->get('martis_two_factor_passed')`). This aligns AuthController::user() with the semantics already used by EnsureTwoFactorChallenge::requiresChallenge() at line 78 of the middleware. Severity is medium rather than high because EnsureTwoFactorChallenge still blocks all protected data routes with 423; the bypass is limited to the user info payload and SPA routing, not arbitrary data access.

### Per-request nocache bypass (?nocache=1) is unrestricted — DoS vector for any authenticated user
- **Severity:** medium · **Category:** security · **Subsystem:** cache · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Cache/MartisCache.php:298-313`
- **Evidence:** public function bypassed(?Request $request = null): bool {     $request ??= $this->currentRequest();      if (! $request instanceof Request) {         return false;     }      if ($request->headers->get('X-Martis-No-Cache') === '1') {         return true;     }      $param = $request->query('nocache');      return $param === '1' || $param === 'true'; }
- **Suggested fix:** Gate-check the bypass before honoring it. Add a permission check, e.g. `Gate::forUser($request->user())->allows('bypass-martis-cache')` (defaulting to false), so only privileged users (developers/admins) can send ?nocache=1. Any authenticated user can currently append ?nocache=1 to any Martis API call to force every expensive metric/navigation/schema computation on every request, bypassing all cac …
- **Verifier:** The code was read in full and the claim is confirmed by four converging observations.  1. `bypassed()` at MartisCache.php:298-313 accepts `?nocache=1`, `?nocache=true`, or the `X-Martis-No-Cache: 1` header and returns `true` with no authorization check of any kind inside the method.  2. The sole cal …
- **Verifier fix note:** The suggested fix (adding a Gate check inside `bypassed()`) is the right approach, but the default gate (`fn ($user) => $user !== null`) would not actually restrict access unless the host application overrides it. The fix should either: (a) introduce a dedicated `bypass-martis-cache` gate that defaults to `false` (deny-by-default), or (b) add a config flag `martis.cache.allow_nocache_bypass` that defaults to `false` and is only enabled in developer/staging environments. Option (a) aligns better with the existing gate pattern. The `manage-martis-cache` gate should NOT be reused here because its own default allows any authenticated user.

### ThemeMakeCommand::handle() calls bare $this->confirm() without a TTY guard, blocking piped/non-PTY invocations
- **Severity:** medium · **Category:** bug · **Subsystem:** console · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Console/ThemeMakeCommand.php:29`
- **Evidence:** if (file_exists($path) && ! $this->confirm("Theme '{$name}.css' already exists. Overwrite?")) {
- **Suggested fix:** InstallCommand implements the dual-gate pattern `$this->input->isInteractive() && $this->stdinIsTty()` precisely because `docker-compose exec -T` and CI pipes set `isInteractive()=true` while stdin has no TTY. ThemeMakeCommand uses the bare Symfony `$this->confirm()` without this gate. Add a check before the confirm: if not interactive or running unit tests, treat an existing file as blocking and  …
- **Verifier:** The code at ThemeMakeCommand.php:29 is exactly as cited: `if (file_exists($path) && ! $this->confirm("Theme '{$name}.css' already exists. Overwrite?"))` with no interactivity guard of any kind. The command's $signature declares only `{name?}` — there is no `--force` option. Every other command in th …
- **Verifier fix note:** The fix needs two changes: (1) add `{--force : Overwrite an existing theme file without prompting}` to $signature, and (2) replace the bare confirm with `! $this->option('force') && $this->input->isInteractive() && ! app()->runningUnitTests() && $this->stdinIsTty()` before calling confirm — matching the InstallCommand dual-gate pattern. Without --force, automation has no way to bypass the prompt even after the guard is added.

### UserCommand does not check for existing email before creating the admin user, causing an unhandled DB unique-constraint exception on re-run
- **Severity:** medium · **Category:** bug · **Subsystem:** console · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Console/UserCommand.php:38-43`
- **Evidence:** $user = new $modelClass;         $user->setAttribute('name', $name);         $user->setAttribute('email', $email);         $user->setAttribute('password', Hash::make($password));         $user->setAttribute('email_verified_at', now());         $user->save();
- **Suggested fix:** Before calling `save()`, check whether a user with the given email already exists and abort with a clear message: `if ($modelClass::query()->where('email', $email)->exists()) { $this->components->error("A user with email [{$email}] already exists."); return self::FAILURE; }`. Without this guard, running `martis:user` twice with the same email throws a PDO/QueryException with a cryptic UNIQUE const …
- **Verifier:** I read /Users/lmoura/projects/martis/martis-package/src/Console/UserCommand.php in full (51 lines). The handle() method performs exactly one guard before persisting: an empty-password check at lines 29-33. There is no duplicate-email check anywhere in the file — no ->exists() query, no firstOrCreate …
- **Verifier fix note:** The suggested fix is correct. Add `if ($modelClass::query()->where('email', $email)->exists()) { $this->components->error("A user with email [{$email}] already exists."); return self::FAILURE; }` immediately before line 38. Alternatively, wrapping save() in a try/catch for QueryException would also work but is less informative to the operator. The explicit guard is the cleaner approach.

### Resource policy cache ($resolvedPolicies) is a process-lifetime static array with no flush hook for Octane or queue workers, causing stale policy resolution across requests
- **Severity:** medium · **Category:** implementation · **Subsystem:** core-registry-manager-provider · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Resource.php:69, 591-594`
- **Evidence:** private static array $resolvedPolicies = [];  public static function flushPolicyCache(): void {     self::$resolvedPolicies = []; }
- **Suggested fix:** Register a flush hook in MartisServiceProvider::boot() for Laravel Octane's request lifecycle event so the cache is cleared between requests in persistent-process deployments: `if (class_exists(\Laravel\Octane\Events\RequestTerminated::class)) { Event::listen(\Laravel\Octane\Events\RequestTerminated::class, fn () => Resource::flushPolicyCache()); }`. The same pattern applies to HasPolicy's `$resol …
- **Verifier:** The code confirms all three parts of the claim. First, `Resource::$resolvedPolicies` is declared as `private static array $resolvedPolicies = []` at line 69 — a static property that lives for the lifetime of the PHP process. Second, `Resource::flushPolicyCache()` (lines 591-594) and `HasPolicy::flus …
- **Verifier fix note:** The suggested fix is correct in shape. One addition: `HasPolicy::$resolvedHasPolicyPolicies` (used by Dashboard and Tool) needs the same treatment. A single `Event::listen(RequestTerminated::class, ...)` call in `MartisServiceProvider::boot()` should flush both `Resource::flushPolicyCache()` and `HasPolicy::flushPolicyCache()` (calling the trait flush once on any class that uses it, e.g. a registered Dashboard class, resets the shared static). Also consider `WorkerStarting` / `WorkerStopping` for queue worker processes.

### configuration.md 'Locales and i18n' section shows wrong top-level flat keys instead of nested 'locales' block
- **Severity:** medium · **Category:** doc-drift · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:1183-1196`
- **Evidence:** Docs show: 'locale_fallback_chain' => env('MARTIS_LOCALE_FALLBACK_CHAIN', 'en'), 'app_locale_namespaces' => env('MARTIS_APP_LOCALE_NAMESPACES', ''), 'rtl_locales'           => env('MARTIS_RTL_LOCALES', 'ar,fa,he,ur'),  Actual config (martis.php lines 442-455): 'locales' => [     'app_namespaces' => array_filter(...),     'fallback_chain' => array_filter(...),     'rtl_locales' => array_filter(...), ],  Actual code reads: config('martis.locales.fallback_chain') and config('martis.locales.app_name …
- **Suggested fix:** Replace the 'Locales and i18n' code block in docs to match the actual nested structure: 'locales' => ['fallback_chain' => ..., 'app_namespaces' => ..., 'rtl_locales' => ...]. Also fix the key name: the doc says 'app_locale_namespaces' but the config key is 'app_namespaces' (inside the 'locales' group).
- **Verifier:** Confirmed by direct code reading. The docs at configuration.md:1185-1196 show three flat top-level config keys (`locale_fallback_chain`, `app_locale_namespaces`, `rtl_locales`) with raw env string values. The actual martis.php:442-455 nests all three under a `'locales'` group, with different inner k …
- **Verifier fix note:** Replace the flat three-key block in the 'Locales and i18n' section with the actual nested structure: 'locales' => ['fallback_chain' => env('MARTIS_LOCALE_FALLBACK_CHAIN', 'en'), 'app_namespaces' => env('MARTIS_APP_LOCALE_NAMESPACES', ''), 'rtl_locales' => env('MARTIS_RTL_LOCALES', 'ar,fa,he,ur')]. Update the table to use 'locales.fallback_chain' and 'locales.app_namespaces' as the key column values. Note that the actual runtime values are arrays (split on comma), not raw strings, but showing the env var defaults in the doc snippet is acceptable for readability as long as the nesting and key names are correct.

### configuration.md 'Password reset' section shows wrong config structure: uses snake_case and wrong nesting
- **Severity:** medium · **Category:** doc-drift · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:1024-1034`
- **Evidence:** Docs show: 'auth' => [     'password_reset' => [         'enabled' => env('MARTIS_AUTH_PASSWORD_RESET_ENABLED', false),         'url'     => env('MARTIS_AUTH_PASSWORD_RESET_URL'),     ],     'password_broker' => env('MARTIS_AUTH_PASSWORD_BROKER', 'users'), ],  Actual config (martis.php lines 850-861): 'passwordReset' => [     'enabled' => env('MARTIS_AUTH_PASSWORD_RESET_ENABLED', false),     'url' => env('MARTIS_AUTH_PASSWORD_RESET_URL'),     'broker' => env('MARTIS_AUTH_PASSWORD_BROKER', 'users …
- **Suggested fix:** Change the 'Password reset' code block to use camelCase 'passwordReset' key (not 'password_reset') and move 'broker' inside the 'passwordReset' subkey (not as a sibling 'password_broker'). The correct shape is auth.passwordReset.broker. authentication.md (line 124) already shows the correct shape.
- **Verifier:** Confirmed by direct code reading. docs/configuration.md lines 1024-1034 shows 'password_reset' (snake_case) and 'password_broker' as a sibling key under 'auth'. The actual config/martis.php lines 850-861 uses 'passwordReset' (camelCase) with 'broker' nested inside it. All three callers (AuthControll …
- **Verifier fix note:** The fix is to update docs/configuration.md lines 1026-1031 to match the actual config shape: rename 'password_reset' to 'passwordReset' and move 'broker' (not 'password_broker') inside that subkey. No code changes are needed — only the documentation is wrong.

### configuration.md 'Login throttle' section shows auth.login_throttle nesting that does not exist — config/code uses throttle.login_attempts
- **Severity:** medium · **Category:** doc-drift · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:1051-1061`
- **Evidence:** Docs show: 'auth' => [     'login_throttle' => [         'attempts' => (int) env('MARTIS_LOGIN_THROTTLE_ATTEMPTS', 20),         'minutes'  => (int) env('MARTIS_LOGIN_THROTTLE_MINUTES', 1),     ], ],  Actual config (martis.php lines 496-497): 'login_attempts' => (int) env('MARTIS_LOGIN_THROTTLE_ATTEMPTS', 20), 'login_minutes' => (int) env('MARTIS_LOGIN_THROTTLE_MINUTES', 1),  (these are flat keys under the 'throttle' block, not nested under 'auth')  Code reads: config('martis.throttle.login_attem …
- **Suggested fix:** Change the 'Login throttle' code block to show the actual location: they are flat keys inside the 'throttle' block (not nested under 'auth'). The correct path is martis.throttle.login_attempts and martis.throttle.login_minutes.
- **Verifier:** Verified by reading the three authoritative sources directly.  1. docs/configuration.md lines 1053-1058 shows the "Login throttle" code block as:    'auth' => ['login_throttle' => ['attempts' => ..., 'minutes' => ...]]  2. config/martis.php lines 492-498 shows the actual structure:    'throttle' =>  …
- **Verifier fix note:** The doc block should be replaced with the actual structure: 'throttle' => ['login_attempts' => (int) env('MARTIS_LOGIN_THROTTLE_ATTEMPTS', 20), 'login_minutes' => (int) env('MARTIS_LOGIN_THROTTLE_MINUTES', 1)]. The surrounding prose should also change "Independent from the global panel throttle" to note these keys live inside the same 'throttle' block as the global settings, not in a separate 'auth' block.

### configuration.md 'Dev tools' section shows top-level 'dev_tools' key — actual config/code uses nested 'dev.tools_enabled'
- **Severity:** medium · **Category:** doc-drift · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:1197-1203`
- **Evidence:** Docs show: 'dev_tools' => env('MARTIS_DEV_TOOLS', in_array(env('APP_ENV', 'production'), ['local', 'testing'], true)),  Actual config (martis.php lines 302-307): 'dev' => [     'tools_enabled' => env(         'MARTIS_DEV_TOOLS',         in_array(env('APP_ENV', 'production'), ['local', 'testing'], true),     ), ],  Code reads: config('martis.dev.tools_enabled', false) — resources/views/app.blade.php:111.
- **Suggested fix:** Change the 'Dev tools' code block to show the correct nested structure: 'dev' => ['tools_enabled' => env('MARTIS_DEV_TOOLS', ...)]. The key name is 'tools_enabled' inside the 'dev' subkey, not a top-level 'dev_tools'.
- **Verifier:** The finding is confirmed by direct code reading. Three sources were checked:  1. /Users/lmoura/projects/martis/martis-package/config/martis.php lines 302-307: the actual config uses a nested structure `'dev' => ['tools_enabled' => env('MARTIS_DEV_TOOLS', ...)]`. The key is `tools_enabled` inside a ` …
- **Verifier fix note:** The doc code block at configuration.md line 1199-1201 must be replaced with the correct nested structure: 'dev' => ['tools_enabled' => env('MARTIS_DEV_TOOLS', in_array(env('APP_ENV', 'production'), ['local', 'testing'], true))]. The severity is medium rather than high because the MARTIS_DEV_TOOLS env var path still works correctly for the vast majority of users; only developers overriding via a published config PHP file using the documented flat key are affected.

### configuration.md user_menu section documents 'showNotifications' key that does not exist in config or code
- **Severity:** medium · **Category:** doc-drift · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:504-517`
- **Evidence:** Docs show: 'user_menu' => [     'showThemeToggle' => true,     'showProfile' => true,     'showNotifications' => true,     // 'customItems' => [], ],  Actual config (martis.php lines 693-697): 'user_menu' => [     'showThemeToggle' => true,     'showProfile' => env('MARTIS_SHOW_PROFILE_MENU', true),     // 'customItems' => [], ],  'showNotifications' is absent from the config file and produces no grep hits in src/.
- **Suggested fix:** Remove 'showNotifications' from the user_menu code block and table in configuration.md. If notifications visibility IS intended to be togglable via this key, it must first be added to config/martis.php and wired into the blade/frontend. Until then the docs should not document it.
- **Verifier:** Confirmed real. Evidence:  1. config/martis.php (lines 693-697): the published `user_menu` array contains only `showThemeToggle` and `showProfile`. `showNotifications` is absent.  2. resources/js/lib/config.ts (lines 23-32): `MartisUserMenuConfig` interface declares only `showThemeToggle?: boolean`, …
- **Verifier fix note:** The blade fallback on line 83 of app.blade.php also needs to be cleaned up: the default array passed to `json_encode` includes `showNotifications: true` and should be updated to match the actual published config (`['showThemeToggle' => true, 'showProfile' => true]`). Additionally, `authentication.md` line 846 contains the same phantom key and should be corrected at the same time as `configuration.md`.

### configuration.md sticky_views 'scope' table documents 'request' as a valid value; only 'session' and 'local' are supported
- **Severity:** medium · **Category:** doc-drift · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:1139`
- **Evidence:** Docs (line 1139): "| `scope` | `session` | `session` keeps the memory until the session expires; `request` is a no-op for one navigation only. |"  The authoritative sticky_views.md (lines 63-64) lists only 'session' and 'local' as valid: - session (default) — sessionStorage. Wipes when the tab closes. - local — localStorage. Survives tab close.  The config comment in martis.php lines 651-658 also lists only: 'session', 'local', 'server' (reserved future). 'request' is not mentioned anywhere in c …
- **Suggested fix:** Change the scope description in the duplicate Sticky views section (line 1127-1139) to match sticky_views.md: valid values are 'session' (sessionStorage, wipes on tab close) and 'local' (localStorage, survives). Remove the erroneous 'request' value.
- **Verifier:** Confirmed by reading four authoritative sources:  1. `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:1139` - The offending line reads: "`scope` | `session` | `session` keeps the memory until the session expires; `request` is a no-op for one navigation only." The value `request` a …
- **Verifier fix note:** The description column in configuration.md:1139 should be corrected to match the other authoritative sources. Replace the erroneous `request` mention with a description that lists the two real supported values: `session` (sessionStorage, wipes on tab close) and `local` (localStorage, survives tab close). The corrected row should read: "`scope` | `session` | Where state is persisted: `session` — sessionStorage, wipes on tab close; `local` — localStorage, survives tab close." This aligns with sticky_views.md:63-64, config/martis.php:651-654, and the TypeScript type at config.ts:484.

### configuration.md env var table lists 'MARTIS_EXTENSIONS_PATH' which has no matching entry in config/martis.php or any source file
- **Severity:** medium · **Category:** doc-drift · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:1297`
- **Evidence:** Docs (line 1297): "| `MARTIS_EXTENSIONS_PATH` | `'martis-extensions'` |"  Grepping config/martis.php and src/ for 'MARTIS_EXTENSIONS_PATH' or 'extensions_path' returns zero hits. The real config has 'extensions' (a CSV list of ESM URLs via MARTIS_EXTENSIONS), not a path. The claim that 'martis-extensions' is the default path for anything has no code backing.
- **Suggested fix:** Remove MARTIS_EXTENSIONS_PATH from the env var table in configuration.md. If there was an 'extensions_path' config key that was removed, it should be mentioned in a deprecation note, not listed as active. The actual runtime extensions config is the 'extensions' key (MARTIS_EXTENSIONS CSV env var) at martis.php line 1204.
- **Verifier:** The finding is confirmed by direct code inspection.  1. `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:1297` lists `MARTIS_EXTENSIONS_PATH` with default `'martis-extensions'` in the env var reference table.  2. A grep of the entire package tree (all PHP, TS, TSX, JS files) for ` …
- **Verifier fix note:** Two files need updating, not one. Remove `MARTIS_EXTENSIONS_PATH` from the env var table at `docs/configuration.md:1297` and remove the `extensions_path` bullet from `docs/installation-guide.md:123`. Both should be replaced with a note pointing at the current mechanism: the `extensions` config key (env var `MARTIS_EXTENSIONS`, a comma-separated list of ESM URLs, added in v1.8.19). A brief "removed in v1.10.0" note in a migration section would help consumers who are upgrading from older versions and still have `MARTIS_EXTENSIONS_PATH` in their `.env`.

### overrides.mdx has stale build instruction replacing pkg's correct one
- **Severity:** medium · **Category:** doc-drift · **Subsystem:** docs-sync-pkg-vs-mdx · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-docs/src/content/customization/overrides.mdx:660-671`
- **Evidence:** The pkg source at `/Users/lmoura/projects/martis/martis-package/docs/overrides.md:654-660` reads: `After creating a component, rebuild the consumer extension bundle:\n\`\`\`bash\nnpm run build:extensions\n\`\`\`\nThat runs \`vite build --config vite.extensions.config.ts\`, which is published into your app by \`martis:install\`. The bundle is emitted at \`public/vendor/martis-user/extensions.js\`...`. The mdx version replaces this with a two-command block (`npm run build` first, then `npm run bui …
- **Suggested fix:** The mdx has been updated with current v1.9.x guidance (plain `npm run build` + auto-discovery) but the pkg source still describes the older `npm run build:extensions` path with `public/vendor/martis-user/extensions.js`. Decide which is canonical and sync both. If v1.9.x auto-discovery via `npm run build` is current, update the pkg source to match the mdx.
- **Verifier:** Both files were read directly. The pkg source at martis-package/docs/overrides.md:654-660 shows a single `npm run build:extensions` command and describes the output as `public/vendor/martis-user/extensions.js`, with no mention of `npm run build` or `MARTIS_USER_DIR`. The mdx at martis-docs/src/conte …
- **Verifier fix note:** The mdx at line 671 contradicts its own code block: the block shows two commands suggesting `npm run build` is a valid primary path, but the prose immediately after declares `npm run build:extensions` is "the only path." Before syncing the pkg source to the mdx, resolve this internal contradiction in the mdx first. If v1.9.x auto-discovery via plain `npm run build` is genuinely the current path, remove the "only path" prose and update the pkg source to match. If `npm run build:extensions` is still the canonical consumer command, remove the two-command block from the mdx and align its prose with the pkg source.

### DrawerSlot::Quick is orphaned — no corresponding contract method or overrides() key
- **Severity:** medium · **Category:** implementation · **Subsystem:** enums-contracts · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Enums/DrawerSlot.php:39`
- **Evidence:** case Quick = 'quick';
- **Suggested fix:** Add `overrideQuick(): ?OverrideContract` to ResourceContract (and Resource.php with a null default), then include `'quick' => $this->overrideQuick()?->toArray()` in the `overrides()` return array and update the @return annotation at ResourceContract line 553. The DrawerOverride::quick() factory and the tests for the enum value and factory already exist — the missing piece is the contract entry poi …
- **Verifier:** The finding is confirmed by direct code inspection. `DrawerSlot::Quick = 'quick'` exists at /Users/lmoura/projects/martis/martis-package/src/Enums/DrawerSlot.php:39 and `DrawerOverride::quick()` exists at DrawerOverride.php:58, and the `DrawerQuick` TSX component is registered in app.tsx:38. However …
- **Verifier fix note:** The suggested fix is correct as far as it goes (add overrideQuick() to ResourceContract and Resource, add 'quick' to overrides()). However the fix is incomplete without also adding a TSX consumer: no page currently reads schema.overrides?.quick to actually mount the DrawerQuick component. Without a frontend caller, adding the PHP contract entry point still leaves DrawerQuick as dead code. The full fix must also identify which surface (ResourceIndex row action, a future peek affordance, etc.) should read schema.overrides?.quick and render the component.

### NotificationBell action_url: internal-looking paths use hard full-page reload instead of SPA navigation
- **Severity:** medium · **Category:** implementation · **Subsystem:** fe-components · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/NotificationBell.tsx:178-181`
- **Evidence:** if (item.action_url.startsWith('/')) {   window.location.href = item.action_url } else {   window.open(item.action_url, '_blank', 'noopener,noreferrer') }
- **Suggested fix:** Paths starting with `/` inside a Martis SPA are almost always in-app routes. Using `window.location.href` causes a full page reload and discards all React Query caches. Use `navigate(item.action_url)` (from `useNavigate`) for paths that start with `/martis` or the configured prefix, or simply always use `navigate` for relative paths and `window.open` only for absolute `http(s)://` URLs.
- **Verifier:** The code at NotificationBell.tsx lines 178-181 is exactly as reported. The component uses window.location.href for any action_url starting with "/" and window.open for all others — there is no SPA navigation path at all.  Three independent confirmations make this real:  1. The component has no impor …
- **Verifier fix note:** The fix proposed by the reporter is correct in direction. The cleanest implementation is to add `import { useNavigate } from 'react-router-dom'` at the top of the file, call `const navigate = useNavigate()` inside the component, then replace the branch with: if `item.action_url.startsWith('/')` use `navigate(item.action_url)`, else `window.open(...)`. No prefix-matching heuristic is needed because the SPA router handles any in-app relative path; the only paths that should open a new tab are absolute http(s) URLs, which already fall into the else branch.

### DrawerDetail: Delete button always visible regardless of authorizedToDelete
- **Severity:** medium · **Category:** security · **Subsystem:** fe-components · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/overrides/DrawerDetail.tsx:106-113`
- **Evidence:** footer={   <>     <button       type="button"       onClick={() => setShowDelete(true)}       className="martis-btn-danger inline-flex items-center gap-1.5"     >       <TrashIcon size={14} />       {tAct('delete')}     </button>
- **Suggested fix:** The Table component checks `row._authorization?.authorizedToDelete` before rendering the delete button. The DrawerDetail footer does not. Add a guard: `const deleteDisabled = activeRecord?._authorization?.authorizedToDelete === false` and either hide the button or disable it when that is true. Without this, a user who can open a detail drawer can always click Delete, relying solely on the server-s …
- **Verifier:** Confirmed by reading the source. DrawerDetail.tsx lines 106-113 renders the Delete button with no guard on `activeRecord?._authorization?.authorizedToDelete`. The sibling surfaces that handle the same pattern all apply a guard: ResourceDetail.tsx:270 derives `const canDelete = auth?.authorizedToDele …
- **Verifier fix note:** Mirror the ResourceDetail.tsx pattern: derive `const canDelete = activeRecord?._authorization?.authorizedToDelete !== false` after line 48 where `activeRecord` is assigned, then wrap the delete button in `{canDelete && (...)}`. Optionally also guard the Edit button with `authorizedToUpdate` for consistency, since ResourceDetail does so at line 269/333.

### DrawerDetail: Edit button always visible regardless of authorizedToUpdate
- **Severity:** medium · **Category:** security · **Subsystem:** fe-components · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/overrides/DrawerDetail.tsx:114-121`
- **Evidence:** <button   type="button"   onClick={() => onEdit(recordId ? Number(recordId) || recordId : undefined)}   className="martis-btn-primary inline-flex items-center gap-1.5" >   <PencilSimpleIcon size={14} />   {tAct('edit')} </button>
- **Suggested fix:** Add `const editDisabled = activeRecord?._authorization?.authorizedToUpdate === false` and conditionally hide or disable the Edit button. The Table component already does this (line 597); the drawer should mirror the same authorization checks.
- **Verifier:** Confirmed by reading the full file. DrawerDetail.tsx has zero references to `_authorization`, `authorizedToUpdate`, or `authorizedToDelete`. Both the Edit button (lines 114-121) and the Delete button (lines 106-113) are rendered unconditionally regardless of the record's authorization payload.  The  …
- **Verifier fix note:** Both buttons need guards, not just Edit. Add `const canUpdate = activeRecord?._authorization?.authorizedToUpdate !== false` and `const canDelete = activeRecord?._authorization?.authorizedToDelete !== false`, then either hide or disable each button accordingly (matching the pattern in ResourceDetail.tsx:269-270). Also guard against the loading state: when `isLoading` is true, `activeRecord` is undefined, so the buttons should be disabled or omitted until the record resolves.

### CurrencyFieldDisplay renders 'NaN' when value is non-numeric string
- **Severity:** medium · **Category:** bug · **Subsystem:** fe-fields · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/CurrencyField.tsx:56-57`
- **Evidence:** const numVal = Number(value) const formatted = formatValue(numVal, ext)
- **Suggested fix:** Guard against NaN before calling formatValue: `if (value === null || value === undefined || value === '' || isNaN(Number(value))) return <span className="text-gray-400 dark:text-gray-500">—</span>`. The existing null/undefined check on line 51 does NOT cover strings like '"abc"' or the PHP fallback of an empty string when the column is nullable — Number('') is 0 not NaN, but Number('abc') is NaN.  …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/resources/js/components/fields/CurrencyField.tsx confirms the defect. Line 51 guards only null/undefined; line 56 calls Number(value) unconditionally. Number("abc") === NaN. Inside formatValue (lines 40-47), Intl.NumberFormat(...).format(NaN)  …
- **Verifier fix note:** The fix direction is correct. Add the NaN guard after line 56: `if (Number.isNaN(numVal)) return <span className="text-gray-400 dark:text-gray-500">—</span>`. Note also that Number("") === 0, so a nullable column returning an empty string silently displays 0.00 — a separate concern the suggested fix does not address. To handle both, also add `|| value === ''` to the guard on line 51.

### HasOneField.formatAggregate hardcodes 'pt-PT' locale and EUR currency — wrong for non-PT deployments
- **Severity:** medium · **Category:** convention · **Subsystem:** fe-fields · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/HasOneField.tsx:36-38`
- **Evidence:** return new Intl.NumberFormat('pt-PT', { style: 'currency', currency: 'EUR' }).format(agg.value)   }   return new Intl.NumberFormat('pt-PT', { maximumFractionDigits: 2 }).format(agg.value)
- **Suggested fix:** Replace the hardcoded locale with `undefined` (browser/OS locale) and derive the currency from the field schema or a global config variable rather than hard-wiring EUR. Minimally: `new Intl.NumberFormat(undefined, { style: 'currency', currency: 'EUR' }).format(agg.value)` removes the locale lock. The currency should come from the aggregate definition or a site-wide config token.
- **Verifier:** Lines 36 and 38 of /Users/lmoura/projects/martis/martis-package/resources/js/components/fields/HasOneField.tsx are confirmed in the source:    return new Intl.NumberFormat('pt-PT', { style: 'currency', currency: 'EUR' }).format(agg.value)   return new Intl.NumberFormat('pt-PT', { maximumFractionDigi …
- **Verifier fix note:** The locale fix is straightforward: replace `'pt-PT'` with `undefined` on both lines. The currency fix requires a small schema addition — the aggregate object from the server does not currently carry a currency code, so either the PHP HasOne::ofMany aggregate response should include a `currency` key when the column is monetary, or a fallback to a global config token (window.MartisConfig) should be used. Minimally, removing the locale lock is the priority; the EUR hardcode is secondary but should be noted as a follow-up.

### HasOneField ofMany pill uses native HTML title= and hardcoded Portuguese label — violates tooltip convention and i18n rule
- **Severity:** medium · **Category:** convention · **Subsystem:** fe-fields · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/HasOneField.tsx:183-184`
- **Evidence:** <span className="martis-ofmany-pill" title={`${ofMany.totalCount} registos no total`}>               1 de {ofMany.totalCount}
- **Suggested fix:** Replace the native `title=` with `data-pr-tooltip` (PrimeReact global Tooltip pattern used everywhere else). Translate both strings: `data-pr-tooltip={tMsg('ofmany_total_count', { count: ofMany.totalCount })}` and `{tMsg('ofmany_n_of_total', { n: 1, total: ofMany.totalCount })}`. The Portuguese literals 'registos no total' and '1 de' must not appear in component code — only in the pt_PT translatio …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/resources/js/components/fields/HasOneField.tsx:183-184 exactly matches the evidence in the report:  Line 183: `<span className="martis-ofmany-pill" title={`${ofMany.totalCount} registos no total`}>` Line 184: `1 de {ofMany.totalCount}`  Both v …
- **Verifier fix note:** The fix is straightforward: replace `title={`${ofMany.totalCount} registos no total`}` with `data-pr-tooltip={tMsg('ofmany_total_count', { count: ofMany.totalCount })}` plus `data-pr-position="top"`, and replace the hardcoded `1 de {ofMany.totalCount}` with `{tMsg('ofmany_n_of_total', { n: 1, total: ofMany.totalCount })}`. Add translation keys to the three locale files (en, pt_PT, pt_BR). The severity stays at medium because: the violation is cosmetic/convention (no data leak or functional breakage), but it is a two-pronged failure (tooltip pattern + i18n) in a visible surface, and the fix is trivial given `tMsg` is already in scope and `data-pr-tooltip` is used two lines below in the same component.

### SingleImageInput creates object URL in useMemo without ever revoking it — memory leak
- **Severity:** medium · **Category:** bug · **Subsystem:** fe-fields · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/ImageField.tsx:89-93`
- **Evidence:** const previewUrl = useMemo(() => {     if (currentFile) return URL.createObjectURL(currentFile)     if (existingImage) return existingImage.thumbnailUrl ?? existingImage.url     return null   }, [currentFile, existingImage])
- **Suggested fix:** Object URLs created with URL.createObjectURL() must be revoked with URL.revokeObjectURL() when no longer needed. A useMemo is not the right place to create them because the cleanup does not run when the memo recomputes. Use useEffect with a cleanup: `useEffect(() => { if (!currentFile) return; const url = URL.createObjectURL(currentFile); setPreviewUrl(url); return () => URL.revokeObjectURL(url);  …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/resources/js/components/fields/ImageField.tsx confirms both leak sites.  SingleImageInput (lines 89-93): `URL.createObjectURL(currentFile)` is called inside `useMemo`. When the memo dependency `currentFile` changes (user picks a different file …
- **Verifier fix note:** The suggested fix (useEffect with cleanup for SingleImageInput) is correct. For MultipleImageInput the fix is slightly different: revokeObjectURL must be called when an item is removed from the list (inside handleRemove, before filtering) and also when the component unmounts for any remaining new-file items. A useEffect that watches `items` and revokes URLs for items no longer present would work, but a simpler approach is to revoke immediately in handleRemove for items that have a file (not an existingImage, whose previewUrl is a server URL, not a blob URL).

### RepeaterField bulk-paste button and confirm-modal labels use Portuguese as defaultValue — English app renders Portuguese strings on missing translation key
- **Severity:** medium · **Category:** convention · **Subsystem:** fe-fields · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/RepeaterField.tsx:476, 561, 637, 646, 655, 672, 698, 700, 705, 709`
- **Evidence:** {tAct('paste_rows', 'Colar linhas')} {tAct('cancel', 'Cancelar')} {tAct('remove', 'Remover')} {t('repeater_confirm_title', 'Remover linha?')} t('repeater_paste_unknown_type', 'Tipo desconhecido.') t('repeater_paste_submit', 'Importar')
- **Suggested fix:** All i18n `defaultValue` fallbacks must be English. The fallback is shown verbatim when the translation key is missing from the active locale. Change them to English equivalents ('Paste rows', 'Cancel', 'Remove', 'Remove row?', 'Unknown type.', 'Import'). The Portuguese strings belong only inside the pt_PT/pt_BR translation files.
- **Verifier:** The finding is confirmed by direct code inspection.  `tAct` is `useTranslation('actions').t` (line 150) and `t` is `useTranslation('messages').t` (line 149). Both come from react-i18next. The i18n setup (`/Users/lmoura/projects/martis/martis-package/resources/js/lib/i18n.ts:125`) sets `fallbackLng:  …
- **Verifier fix note:** The fix is as described in the report: change the Portuguese defaultValue strings to English equivalents. Line 561 also has a separate instance: `t('repeater_add_named', { label: repeatables[0].label, defaultValue: 'Adicionar ${repeatables[0].label}' })` which uses a Portuguese interpolated template as its defaultValue — that one should become `'Add ${repeatables[0].label}'`. The line 700 confirm body has the same pattern: `defaultValue: 'Remover ${pendingRemoval.label}?'` should become `'Remove ${pendingRemoval.label}?'`. The severity is medium (not high) because the fallbackLng chain means this only surfaces when the English translation bundle is also missing the key, which limits the blast radius to edge cases rather than steady-state production use.

### TagFieldInput 'Add' button label is a hardcoded English string — not run through i18n
- **Severity:** medium · **Category:** convention · **Subsystem:** fe-fields · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/TagField.tsx:242`
- **Evidence:** Add {field.label}
- **Suggested fix:** Use the existing `tMsg` translation function: `{tMsg('tag_add', { label: field.label, defaultValue: `Add ${field.label}` })}`. The workspace i18n rule requires all user-facing strings to use translation keys in en, pt_PT, and pt_BR. This string renders in the form context and is directly visible to users.
- **Verifier:** Line 84 of TagField.tsx declares `const { t: tMsg } = useTranslation('messages')` inside `TagFieldInput`. Line 242, within the same function's JSX return, renders the button label as the bare template literal `Add {field.label}` — never passing through `tMsg`. Other strings in the identical componen …
- **Verifier fix note:** The suggested fix (`tMsg('tag_add', { label: field.label, defaultValue: ... })`) is correct. Additionally, the `data-pr-tooltip` string at line 220 (`Remove ${tag.title}`) has the same i18n violation and should be fixed in the same pass — e.g. `tMsg('tag_remove', { title: tag.title, defaultValue: `Remove ${tag.title}` })`.

### ToastContext safety-net clear() wipes all active toasts in queue
- **Severity:** medium · **Category:** bug · **Subsystem:** fe-pages-lib · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/contexts/ToastContext.tsx:110-112`
- **Evidence:** setTimeout(() => {   toastRef.current?.clear() }, life + 1000)
- **Suggested fix:** Replace toastRef.current?.clear() with a targeted remove by reference. Capture the PrimeReact message object returned by the show() call into a local ref and call toastRef.current?.remove(messageRef.current) inside the timeout instead of clear(). This limits the safety-net to the specific toast that triggered the timer and leaves other queued messages untouched.
- **Verifier:** Lines 110-112 call toastRef.current?.clear() inside a setTimeout set to life + 1000 ms (6000 ms). clear() is PrimeReact Toast's "remove every active message" method, not a targeted remove. The return value of toastRef.current?.show() on line 71 is discarded, so no per-message handle is ever stored.  …
- **Verifier fix note:** PrimeReact's Toast.show() returns the enqueued ToastMessage object. Capture it: const msg = toastRef.current?.show(...). Then in the timeout call toastRef.current?.remove(msg) instead of clear(). This limits the safety-net to the one toast that registered the timer and does not affect any other queued messages.

### Number::min() and max() accumulate duplicate validation rules on repeated calls
- **Severity:** medium · **Category:** bug · **Subsystem:** fields-base-textlike · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Number.php:28-44`
- **Evidence:** public function min(int|float $min): static {     $this->min = $min;     $this->extraRules[] = "min:{$min}";     return $this; }  public function max(int|float $max): static {     $this->max = $max;     $this->extraRules[] = "max:{$max}";     return $this; }
- **Suggested fix:** Each call appends a new rule to extraRules without removing any prior min:/max: entry. Calling ->min(5)->min(10) results in ['min:5', 'min:10'] — Laravel validates both, effectively enforcing min:10 with dead noise at min:5. Calling ->min(5)->max(3) creates an impossible constraint with no error at configuration time. Fix: strip any existing min:/max: rule before appending: $this->extraRules = arr …
- **Verifier:** The code at `/Users/lmoura/projects/martis/martis-package/src/Fields/Number.php:28-44` is exactly as described. Both `min()` and `max()` blindly append to `$this->extraRules[]` without removing any prior entry for the same constraint.  The parent class `Field.php` provides no deduplication anywhere  …
- **Verifier fix note:** The suggested fix is correct in principle. For completeness, the strip should also cover the `max:` prefix symmetrically. In the `min()` method: filter out any `$r` where `is_string($r) && str_starts_with($r, 'min:')` before appending. Same for `max()`. One additional edge case the reporter did not mention: if `->min()` or `->max()` is called inside a `dependsOn` closure (which re-runs on every sync request), the rule list grows by one entry per sync cycle — that path deserves a specific test as well.

### Password::fill() also uses raw $this->readonly instead of isReadonly()
- **Severity:** medium · **Category:** bug · **Subsystem:** fields-base-textlike · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Password.php:148`
- **Evidence:** public function fill(Model $model, mixed $value): void {     if ($this->readonly) {         return;     }
- **Suggested fix:** Replace `if ($this->readonly)` with `if ($this->isReadonly())` — same fix as Field::fill(). When a developer uses Password::make('password')->readonly(fn($r) => ...) on an update-only form, the hash is still written to the model.
- **Verifier:** Password::fill() at line 148 guards with `if ($this->readonly)`, reading the raw boolean property directly. Field::readonly() at line 428 only mirrors the value into `$this->readonly` when the argument is a bool — when a Closure is passed, `$readonlyResolver` is set but `$this->readonly` remains fal …
- **Verifier fix note:** Both Password::fill() and the base Field::fill() should replace `if ($this->readonly)` with `if ($this->isReadonly())`. Fixing only Password::fill() leaves the same bug latent in every other field subclass that relies on the base implementation.

### PasswordConfirmation doc-drift: docstring promises automatic 'confirmed' backend rule but none is ever added
- **Severity:** medium · **Category:** doc-drift · **Subsystem:** fields-base-textlike · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/PasswordConfirmation.php:11`
- **Evidence:** * The field is never persisted to the model (fill is a no-op). Backend  * validation relies on Laravel's `confirmed` rule applied to the paired  * `Password` field
- **Suggested fix:** Password::buildRules() never adds 'confirmed'. ResourceController::validateRequest() iterates fields independently and has no logic to link a PasswordConfirmation field back to its sibling Password field. The 'confirmed' rule must be added manually: Password::make('password')->rules(['confirmed']). Either (a) update the docstring to say 'the developer must add ->rules(["confirmed"]) to the Passwor …
- **Verifier:** The docstring on PasswordConfirmation.php:11-13 reads: "Backend validation relies on Laravel's `confirmed` rule applied to the paired `Password` field — this companion simply provides the second input and the UX we ship around it." This phrasing implies the framework or Martis wires up `confirmed` a …
- **Verifier fix note:** The docstring should be updated to explicitly state that the developer must add `->rules(['confirmed'])` to the paired Password field, or alternatively `Password::buildRules()` should be enhanced to auto-inject `'confirmed'` when a `PasswordConfirmation` sibling with a matching `getConfirms()` value is detected. The controller approach (scanning fields for a `PasswordConfirmation` instance and patching the paired field's rules) is also viable but more invasive. The docstring fix is the minimal safe correction.

### Slug reserved-word guard silently bypassed when reserved list contains mixed-case entries
- **Severity:** medium · **Category:** bug · **Subsystem:** fields-base-textlike · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Slug.php:84-88`
- **Evidence:** public function reserved(array $reserved): static {     $this->reserved = array_values(array_map(fn ($v) => (string) $v, $reserved));     return $this; }
- **Suggested fix:** reserved() stores strings verbatim. generate() (which uses Str::slug) always returns lowercase. The in_array($generated, $this->reserved, true) check at line 214 therefore fails when the developer passes ['Admin', 'Login'] because 'admin' !== 'Admin'. Fix: normalise the reserved list to lowercase at insertion time: $this->reserved = array_values(array_map(fn ($v) => strtolower((string) $v), $reser …
- **Verifier:** Confirmed by reading /Users/lmoura/projects/martis/martis-package/src/Fields/Slug.php in full.  reserved() at lines 84-88 stores the incoming array verbatim, applying only a (string) cast — no lowercasing.  generate() at lines 190-193 delegates to Str::slug(), which always returns a lowercase string …
- **Verifier fix note:** The suggested fix (normalise to lowercase at insertion time in reserved()) is correct and sufficient. An equally valid alternative is to change the in_array call to use strtolower($generated) against an already-lowercased list, but normalising at insertion is cleaner because it keeps getReserved() consistent and makes the stored list self-documenting.

### Image::resolveMultiple silently ignores thumbnailResolver and previewResolver closures
- **Severity:** medium · **Category:** implementation · **Subsystem:** fields-media · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Image.php:306-319`
- **Evidence:** return array_map(function (string $path) use ($disk): array {     $url = $disk->url($path);  // never uses $this->previewResolver     $thumbPath = $this->getThumbnailPath($path);     $thumbnailUrl = ($thumbPath !== null && $disk->exists($thumbPath))         ? $disk->url($thumbPath)  // never uses $this->thumbnailResolver         : $url;      return [         'path'         => $path,         'url'          => $url,         'name'         => $this->resolveDisplayName($path),         'thumbnailUrl' …
- **Suggested fix:** Mirror the single-mode resolve() logic (lines 268-279) inside resolveMultiple. Pass `$model` as a `use` capture and apply both `$this->previewResolver` and `$this->thumbnailResolver` when they are set: `$url = $this->previewResolver !== null ? (($this->previewResolver)($path, $model) ?? $disk->url($path)) : $disk->url($path);` and the equivalent for `thumbnailResolver`. Without this, calling `Imag …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/src/Fields/Image.php confirms the defect exactly as described.  Single-mode resolve() (lines 268-280) correctly applies both resolvers: - previewResolver: lines 268-270 check `$this->previewResolver !== null` and call it with `($path, $model)` …
- **Verifier fix note:** The suggested fix is correct in substance. One addition: the array_map closure must also capture $model alongside $disk — change `use ($disk)` to `use ($disk, $model)` — because both resolver closures receive ($path, $model) per their declared signatures (lines 51 and 60). Without capturing $model the closures cannot be invoked inside array_map even after the resolver checks are added.

### buildItemRules() not called in relationship controllers — MIME/size bypass for multi-file fields
- **Severity:** medium · **Category:** security · **Subsystem:** fields-media · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/HasManyController.php:515-548`
- **Evidence:** private function validateRequest(Request $request, array $fields, bool $isUpdate = false): ?IlluminateJsonResponse {     $rules = [];     $attributes = [];      foreach ($fields as $field) {         $fieldRules = $field->buildRules();         // ... strips 'required' on update ...         $rules[$field->attribute()] = $fieldRules;         // buildItemRules() is never called here     }      $validator = Validator::make($request->all(), $rules, [], $attributes);     // ...
- **Suggested fix:** All four relationship controllers (HasManyController, MorphManyController, HasOneController, MorphOneController) have their own private `validateRequest` copies that are missing the `buildItemRules()` block present in ResourceController lines 1658-1663. Extract the item-rules logic into a shared protected method on a base controller or a trait, and call it from every `validateRequest` implementati …
- **Verifier:** The code confirms the defect exactly as described.  `File::buildRules()` at /Users/lmoura/projects/martis/martis-package/src/Fields/File.php:503-517 shows that when `$this->multiple === true` the method returns only `['required'|'nullable'|'sometimes', 'array']` — it intentionally omits `file`, `mim …
- **Verifier fix note:** The suggested fix (extract shared logic into a trait or base controller method) is correct. One simpler alternative: add the `buildItemRules()` block verbatim after the `$rules[$field->attribute()]` assignment in each of the four `validateRequest` copies, matching the ResourceController pattern at lines 1657-1663. The trait approach is cleaner because the same duplication will recur whenever a new relationship controller is added. Either way, all four controllers must be updated atomically — patching only HasManyController still leaves the other three broken.

### BooleanGroup::requireAll() reads $this->options (always [] when options() was called with a Closure), making the constraint a no-op
- **Severity:** medium · **Category:** bug · **Subsystem:** fields-special · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/BooleanGroup.php:151-154`
- **Evidence:** /** @⭐ Sugar for `minChecked(count(options))`. Forces every flag on. */ public function requireAll(): static {     return $this->minChecked(count($this->options)); }
- **Suggested fix:** Change to `return $this->minChecked(count($this->getOptions()));`. When `options()` is called with a Closure, `$this->options` is set to `[]` (line 77) and `$this->optionsResolver` holds the closure. `requireAll()` then computes `minChecked(0)`, which applies no constraint at all. The fix makes it use `getOptions()` which resolves the closure first.
- **Verifier:** The defect is confirmed by directly reading the source.  `options()` at line 75-79 explicitly sets `$this->options = []` and stores the closure in `$this->optionsResolver` when called with a Closure. `requireAll()` at line 153 calls `count($this->options)`, which is `0` at that point. `minChecked(0) …
- **Verifier fix note:** The proposed fix is correct: change line 153 from `count($this->options)` to `count($this->getOptions())`. One additional consideration: `getOptions()` invokes the closure with `$this->safeRequest()`, which may return null at field-definition time (before any HTTP request context exists). The closure must therefore tolerate a null Request argument. This is already documented in the `options()` docblock (`@param \Closure(Request|null)`) and is not a new concern introduced by the fix, but the caller should be aware.

### KeyValue hardcoded English defaults ('Key', 'Value', 'Add Row') bypass the i18n system
- **Severity:** medium · **Category:** convention · **Subsystem:** fields-special · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/KeyValue.php:22-26`
- **Evidence:** protected string $keyLabel = 'Key'; protected string $valueLabel = 'Value'; protected string $actionText = 'Add Row';
- **Suggested fix:** Change the property defaults to `null` (making them `?string`) and resolve them in `extraAttributes()` with `__('martis::messages.key_label') ?? 'Key'`, etc. Add the translation keys `key_label`, `value_label`, `add_row` to `resources/lang/en/messages.php`, `pt_PT/messages.php`, and `pt_BR/messages.php`. The fallback hardcoded string is fine as a last resort but the translation system should be th …
- **Verifier:** All three properties on KeyValue (keyLabel, valueLabel, actionText) are typed as non-nullable string with hardcoded English defaults at lines 22-26 of /Users/lmoura/projects/martis/martis-package/src/Fields/KeyValue.php. The extraAttributes() method at lines 273-282 passes them directly to the front …
- **Verifier fix note:** The fix requires two coordinated changes: (1) change the three properties to ?string with null defaults and resolve via __() inside extraAttributes() mirroring Boolean, and (2) add key_label, value_label, and add_row to resources/lang/en/messages.php, pt_PT/messages.php, and pt_BR/messages.php. The suggested fix correctly identifies both steps but lists the translation keys without showing what values to add in pt_PT and pt_BR, which must be provided as part of the same commit to satisfy the project i18n rule (EN + pt_PT + pt_BR mandatory per memory entry).

### MagicLinkController::autoRegister() uses new Model([...]) without forceFill — relies on $fillable to include email/name/password, which may silently fail on custom user models
- **Severity:** medium · **Category:** security · **Subsystem:** http-auth-sso-ctrl · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/MagicLinkController.php:125-141`
- **Evidence:** protected function autoRegister(string $email): ?Authenticatable {     $userClass = $this->userClass();     if ($userClass === null) {         return null;     }      /** @var Model $user */     $user = new $userClass([         'email' => $email,         'name' => $email,         'password' => bcrypt(Str::random(40)),     ]);     $user->save();      return $user instanceof Authenticatable ? $user : null; }
- **Suggested fix:** Use `$user->forceFill([...])->save()` (as IdentityResolver::create() does at line 115) to bypass $fillable and guarantee the fields are written regardless of the consumer's model config. The current code silently creates a user with null email/name/password when the consumer model guards those columns — the row is saved but the email lookup on the next login fails, leaving the link in a permanentl …
- **Verifier:** The cited code at /Users/lmoura/projects/martis/martis-package/src/Http/Controllers/MagicLinkController.php:133-138 is confirmed. `autoRegister()` calls `new $userClass(['email' => ..., 'name' => ..., 'password' => ...])`, which internally invokes `Model::fill()` through the constructor (vendor/lara …
- **Verifier fix note:** The fix is correct as stated. Use `(new $userClass)->forceFill([...])->save()` to match the pattern already established in `IdentityResolver::create()` at line 115. Additionally, add a post-save assertion — after `$user->save()`, verify `$user->getAttribute('email') === $email` and return null (or throw) if it does not match, to prevent a silently-corrupted row from being returned as a valid Authenticatable. The `MagicLinkTest.php` suite also needs a test case that configures `auto_register = true`, uses a user model with `$guarded = ['*']` or a restricted `$fillable`, and asserts the returned user has the correct email attribute set.

### SsoController: on_no_role_match = 'callable' strategy is undocumented in the controller — hook fires regardless of strategy, and if the hook returns null + strategy is 'callable' the user is logged in with no roles
- **Severity:** medium · **Category:** security · **Subsystem:** http-auth-sso-ctrl · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/SsoController.php:92-108`
- **Evidence:** $strategy = (string) ($cfg['on_no_role_match'] ?? 'deny');  $hookResult = $this->manager->fireNoRoleMatch($identity, $provider); if ($hookResult instanceof RedirectResponse) {     return $hookResult; }  if ($strategy === 'deny') {     return redirect()         ->route('martis.login')         ->withErrors(['sso' => __('martis::messages.sso_no_role_match')]); } // strategy === 'guest' falls through with empty role set
- **Suggested fix:** SsoManager::onNoRoleMatchUsing docblock states the callback is only meant to fire when strategy is 'callable', but the controller fires it unconditionally for every strategy. More critically, if a consumer sets `on_no_role_match = 'callable'` but never registers a callback (or the callback returns null), the controller falls through to the 'guest' path and logs the user in without any role — the i …
- **Verifier:** Both files confirm the defect exactly as claimed.  SsoController.php lines 92-108: `fireNoRoleMatch()` is called unconditionally on every `$roles->isEmpty()` path, regardless of `$strategy`. There is no `if ($strategy === 'callable')` guard around the hook invocation.  SsoManager.php lines 201-208:  …
- **Verifier fix note:** The suggested fix is correct and sufficient. The safest single-line fix is to add, immediately after the `instanceof RedirectResponse` check: `if ($strategy === 'callable' && $hookResult === null) { return redirect()->route('martis.login')->withErrors(['sso' => __('martis::messages.sso_no_role_match')]); }`. An equally valid approach is to move the hook call inside a `if ($strategy === 'callable')` guard so it is never fired for `'deny'` or `'guest'` strategies, which also corrects the secondary issue (hook fires unconditionally for all strategies, not just 'callable' as documented). The severity stays medium rather than high because exploiting it requires the consumer to have set strategy='callable' without registering the callback — it is a misconfiguration-assisted bypass, not a direct external attack vector.

### ActionController.fields() returns action field schema without checking authorizedToSee
- **Severity:** medium · **Category:** security · **Subsystem:** http-misc · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ActionController.php:98-119`
- **Evidence:** public function fields(Request $request, string $resource, string $action): IlluminateJsonResponse {     // ...     $actionInstance = $this->findAction($instance, $action, $request);     if ($actionInstance === null) {         return JsonErrorResponse::notFound("Action [{$action}] not found.")->toResponse();     }     $fields = $actionInstance->fields($request);     $data = ['fields' => array_map(fn (FieldContract $f) => $f->toArray(), $fields)];     return JsonResponse::make($data)->toResponse( …
- **Suggested fix:** Add `if (! $actionInstance->authorizedToSee($request)) { return JsonErrorResponse::forbidden('...')->toResponse(); }` before calling `$actionInstance->fields()`. This aligns with the execute() method which already checks authorizedToSee at line 142.
- **Verifier:** Reading the actual code confirms the defect.  The `fields()` method at lines 98-119 calls `findAction()` (line 107), which at lines 282-293 iterates `$resource->actions($request)` with NO authorization filter — it simply matches on `uriKey()`. In contrast, `resolveActions()` (lines 271-279) wraps th …
- **Verifier fix note:** The fix is correct as stated. Add the `authorizedToSee` guard at line 112 (after the null check on `$actionInstance`), exactly as `execute()` does at line 142. No other changes needed. The `findAction` helper itself does not need to change — the caller is responsible for authorization, consistent with the existing pattern in the other three endpoints that use `findAction`.

### ActionController leaks raw exception message to HTTP clients in 500 responses
- **Severity:** medium · **Category:** security · **Subsystem:** http-misc · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ActionController.php:246,640`
- **Evidence:** // Line 246 in execute(): return JsonErrorResponse::serverError('Action failed: '.$e->getMessage())->toResponse();  // Line 640 in executePivot(): return JsonErrorResponse::serverError('Pivot action failed: '.$e->getMessage())->toResponse();  // JsonErrorResponse.serverError docblock (line 103-106): /** Build a 500 Internal Server Error response.  * Never exposes internal error details to the client.  */ public static function serverError(string $message = 'An unexpected error occurred.'): self
- **Suggested fix:** Do not pass $e->getMessage() to serverError(). Log the message (already done on the preceding Log::error() call at lines 231-236 and 629-634) and return a generic message: `JsonErrorResponse::serverError('Action execution failed.')->toResponse()`. The docblock explicitly states this method should not expose internal details.
- **Verifier:** Confirmed by reading both the ActionController and JsonErrorResponse source. At line 246 of ActionController.php, `JsonErrorResponse::serverError('Action failed: '.$e->getMessage())->toResponse()` is called inside a `catch (\Throwable $e)` block. The same pattern appears at line 640 for the pivot va …
- **Verifier fix note:** The fix is straightforward: drop the message argument at both call sites so the default generic string is used. `JsonErrorResponse::serverError()->toResponse()` is sufficient at lines 246 and 640. The `serverError()` docblock should also be amended to remove the "Never exposes internal error details" claim, or the implementation should be changed to hard-code the generic message and ignore any passed argument, to make the guarantee real rather than aspirational.

### ActionController.executePivot() does not verify the caller is authorized to view the parent record
- **Severity:** medium · **Category:** security · **Subsystem:** http-misc · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ActionController.php:544-550`
- **Evidence:** // executePivot() fetches the parent model with no authorization check: $modelClass = $resourceClass::model(); $parentModel = $modelClass::find($id); if ($parentModel === null) {     return JsonErrorResponse::notFound("Parent record [{$id}] not found.")->toResponse(); } // No: $resourceForParent->authorizedToView($request) check here. // The action's authorizedToSee($request) is checked at line 566, // but the parent record itself is accessed without policy enforcement.
- **Suggested fix:** After instantiating `$parentInstance = new $resourceClass($parentModel)`, add `if (! $parentInstance->authorizedToView($request)) { return JsonErrorResponse::forbidden('This action is unauthorized.')->toResponse(); }` before proceeding to resolve pivot columns and execute the action.
- **Verifier:** Reading the actual code confirms the defect.  In `executePivot()` (/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ActionController.php:546-571):  - Line 547: `$parentModel = $modelClass::find($id)` — the parent record is fetched. - Line 566: `$actionInstance->authorizedToSee($requ …
- **Verifier fix note:** The fix matches the suggested approach exactly, and the sibling controller already shows the correct pattern. After `$parentInstance = new $resourceClass($parentModel)` at line 571, insert: `if (! $parentInstance->authorizedToView($request)) { return JsonErrorResponse::forbidden('This action is unauthorized.')->toResponse(); }`. No other changes are needed.

### BelongsToMany attach: related record fetched from global scope, not relationship scope — IDOR on relatedId
- **Severity:** medium · **Category:** security · **Subsystem:** http-relationships · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/BelongsToManyController.php:278`
- **Evidence:** $relatedModel = $relatedModelClass::find($relatedId); // @phpstan-ignore-line if ($relatedModel === null) {     return JsonErrorResponse::notFound('Related record not found.')->toResponse(); }  // Authorization if (! $this->canAttach($request, $parentModel, $relatedModel, $ctx)) {
- **Suggested fix:** The attach, detach, and updatePivot actions all look up the related model with a global `::find($relatedId)` before calling `canAttach` / `canDetach` / `canUpdatePivot`. Because `canAttach` / `canDetach` default to `authorizedToUpdate` on the parent (not a model-level gate on the related record), a user who can update *any* parent can attach any related record that exists in the database — includi …
- **Verifier:** Confirmed by reading the full file. At line 278 (attach), line 359 (attachMany batch loop), line 482 (detach), and line 542 (updatePivot), the related record is retrieved with a bare `$relatedModelClass::find($relatedId)` against the global Eloquent scope — no relationship-scoped query, no tenant sc …
- **Verifier fix note:** The suggested fix is correct. The most robust approach is (a): before calling `canAttach`, re-run the `relatedModelClass` query through the same scoped builder used by `attachableIndex` — honouring `relatableQueryUsing` if set, or at minimum going through the relationship's `relatable()` query — so that IDs outside the permitted set return a 404 before authorization is even evaluated. Alternatively (b): after the bare `::find`, instantiate `new $relatedResourceClass($relatedModel)` and call `authorizedToView($request)` on it; this adds a model-level gate. Both fixes need to be applied to all four mutation methods: single `attach`, batch `attachMany`, `detach`, and `updatePivot`.

### BelongsToMany/MorphToMany index: sort column passed directly to orderBy without validating against sortable fields **[FIXED]**
- **Severity:** medium · **Category:** security · **Subsystem:** http-relationships · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/BelongsToManyController.php:82-84`
- **Evidence:** if (is_string($rawSort) && $rawSort !== '') {     $query->orderBy($rawSort, $direction->value); }
- **Suggested fix:** HasManyController::applySorting() and MorphManyController::applySorting() both validate the sort parameter against the resource's declared sortable field attributes before calling orderBy. BelongsToManyController::index() and MorphToManyController::index() do not: they call `$query->orderBy($rawSort, ...)` on any non-empty string. While Eloquent/PDO will parameterize the value, the column name its …
- **Verifier:** Confirmed by reading the actual code. BelongsToManyController.php:82-84 calls `$query->orderBy($rawSort, $direction->value)` with the only guard being `is_string($rawSort) && $rawSort !== ''`. There is no allowlist check against the resource's declared sortable fields.  HasManyController::applySorti …
- **Verifier fix note:** Both BelongsToManyController::index() and MorphToManyController::index() need the same allowlist guard used in HasManyController and MorphManyController: flatten the related resource's fields, filter by isSortable(), extract attributes into an array, and return without calling orderBy if the sort parameter is not in that array. The $relatedResourceClass variable is already in scope at both call sites, so no additional context is needed.

### replicateFields skips per-model field authorization (isAuthorizedForModel) by passing a keyless replica
- **Severity:** medium · **Category:** security · **Subsystem:** http-resource · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ResourceController.php:621-630`
- **Evidence:** $replica = $model->replicate(); $resReplica = new $resourceClass($replica); $fields = Field::filterForContext($resReplica->fieldsForCreate($request), FieldContext::CREATE); $values = []; foreach ($fields as $field) {     if ($field instanceof File) { continue; }     $resolved = $field->resolve($replica);     $values[$field->attribute()] = $resolved; }
- **Suggested fix:** Eloquent's replicate() strips the primary key, so any canSeeForModel callback that evaluates the model's key (e.g. owner-scoped policies) receives a null-key replica instead of the real record. Additionally, serializeModel() calls isAuthorizedForModel on the real model, but replicateFields never calls isAuthorizedForModel at all. Fix: before calling $field->resolve($replica), check `if (method_exi …
- **Verifier:** The defect is confirmed by direct code inspection. The `replicateFields` loop at lines 623-630 of ResourceController.php has no call to `isAuthorizedForModel`. The only skip inside the loop is for `File` instances. By contrast, `serializeModel` (line 1780) explicitly calls `isAuthorizedForModel($req …
- **Verifier fix note:** The fix should pass the original `$model` (not `$replica`) to `isAuthorizedForModel`, since the replica has a null primary key which breaks Gate policies that evaluate `$model->getKey()`. The check should be: `if (method_exists($field, 'isAuthorizedForModel') && !$field->isAuthorizedForModel($request, $model)) { continue; }` placed before `$field->resolve($replica)` at line 628, using `$model` (the real record fetched at line 604) as the second argument.

### per_page=0 or negative bypasses the ceiling guard — paginate(0) returns empty pages, paginate(-N) may throw on some engines
- **Severity:** medium · **Category:** bug · **Subsystem:** http-resource · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ResourceController.php:131-136`
- **Evidence:** $perPage = min(     (int) ($request->query('per_page', (string) $resourceClass::resolvedPerPage())),     (int) config('martis.pagination.max_per_page', 100), );  $paginator = $query->paginate($perPage);
- **Suggested fix:** min() only caps the ceiling. A caller passing per_page=0 gets LIMIT 0 (valid SQL, silently empty result), while per_page=-5 results in LIMIT -5 which raises an error on PostgreSQL. Add a floor: `$perPage = max(1, min(...))`. The same pattern applies to the relatable endpoint: resolveRelatableSearchResults only uses max(1,...) when $relatableSearchResults is set; when it is null, `min($requestPerPa …
- **Verifier:** The code at ResourceController.php:131-136 confirms the defect. `$perPage = min((int)$request->query('per_page', ...), 100)` applies only a ceiling; a user-supplied `per_page=0` yields `min(0,100)=0` and a `per_page=-5` yields `min(-5,100)=-5`. Both are passed without a floor to `$query->paginate($p …
- **Verifier fix note:** The fix for the primary index path is `$perPage = max(1, min((int)$request->query('per_page', ...), (int)config('martis.pagination.max_per_page', 100)));`. For the relatable endpoint, the `$cap === null` branch in `Resource::resolveRelatableSearchResults` should return `max(1, min($requestPerPage, 100))` to match the guarded branch. Severity stays medium: the `per_page=0` case is a silent bad response (not a crash or data leak), and the negative case only raises an engine error on PostgreSQL, not MySQL/SQLite.

### handleDatabaseError maps only MySQL vendor codes — unique-constraint and FK violations are silently downgraded to a generic error on Postgres/SQLite
- **Severity:** medium · **Category:** implementation · **Subsystem:** http-resource · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ResourceController.php:1395-1421`
- **Evidence:** $code = (string) ($e->errorInfo[1] ?? '');  $message = match ($code) {     '1048' => 'A required field is missing...',     '1062' => 'A record with this value already exists...',     '1364' => 'A required field was not provided...',     '1451' => 'This record cannot be modified...',     '1452' => 'The referenced record does not exist...',     default => 'A database error occurred.', };
- **Suggested fix:** errorInfo[1] is the MySQL-specific vendor code. On PostgreSQL the unique-constraint SQLSTATE is '23505' in errorInfo[0], and FK violations are '23503'/'23505'. On SQLite, constraint violations return code 19 in errorInfo[1]. Add SQLSTATE-based fallback: check errorInfo[0] for '23505' (unique) and '23503'/'23000' (FK) when the MySQL-specific code doesn't match. This ensures users on Postgres get th …
- **Verifier:** The code at ResourceController.php:1395 reads `$code = (string) ($e->errorInfo[1] ?? '');` and the match arms are all MySQL vendor codes ('1048', '1062', '1364', '1451', '1452'). There is no SQLSTATE-based fallback on `errorInfo[0]`. On PostgreSQL, unique-constraint violations surface as SQLSTATE '2 …
- **Verifier fix note:** The defect is also present in the 6 sibling controllers, each with their own copy of handleDatabaseError. ResourceController is the only one that even has the MySQL-specific 422 path for '1062'; the others return serverError for unique constraint violations on all drivers including MySQL. A shared trait or base-controller method with SQLSTATE-aware mapping would fix all sites at once.

### Bearer token compared with !== instead of hash_equals — timing-oracle attack possible
- **Severity:** medium · **Category:** security · **Subsystem:** mcp · **Confidence:** high
- **Location:** `src/Mcp/Transport/AuthenticatedStreamableHttpTransport.php:89, 135`
- **Evidence:** if ($request->getHeaderLine('Authorization') !== $expected) {
- **Suggested fix:** Replace both occurrences with `if (!hash_equals($expected, $request->getHeaderLine('Authorization'))) {`. PHP's !== short-circuits on the first differing byte, leaking timing information proportional to how many leading bytes of 'Bearer <token>' the attacker guesses correctly. hash_equals() runs in constant time regardless of where strings diverge. The fix must be applied in both listen() (line 89 …
- **Verifier:** Both cited occurrences exist exactly as described. Line 89 (inside the `listen()` wrapped handler) and line 135 (inside `createRequestHandler()`) both use `!==` to compare `$request->getHeaderLine('Authorization')` against the static `$expected = 'Bearer '.$this->token` string. The input is fully us …
- **Verifier fix note:** The fix must be applied to both occurrences: line 89 (the `$wrappedHandler` closure inside `listen()`, which is the live production path) and line 135 (the `createRequestHandler()` closure, used by unit tests). Argument order for `hash_equals` matters: the known-good secret should be the first argument and the user-supplied value the second, i.e. `!hash_equals($expected, $request->getHeaderLine('Authorization'))`. This matches the suggested fix exactly.

### Dashboard lock state cached and served stale on the /api/dashboards list endpoint
- **Severity:** medium · **Category:** security · **Subsystem:** menu-layout-dashboards · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/MetricController.php:41-46`
- **Evidence:** $payload = $cache->remember('dashboards', 'list:'.$userKey.':'.app()->getLocale(), function () use ($request): array {     $manager = app(MartisManager::class);     $dashboards = $manager->resolveDashboards($request);     return array_values(array_map(fn (DashboardContract $d) => $d->toArray(), $dashboards)); });
- **Suggested fix:** Dashboard::toArray() calls $this->lockPayloadNow() at line 298 of Dashboard.php, which evaluates the lock predicate and bakes the result into the serialized array. That serialized array is then stored in the cache under the user+locale key. If the user's plan is upgraded or downgraded, the cached payload still contains the old lock state until the TTL expires — a locked user who buys a plan upgrad …
- **Verifier:** The code confirms the defect. In MetricController::dashboards() (lines 41-46), the cache closure calls $d->toArray() on each DashboardContract instance. Dashboard::toArray() (Dashboard.php line 298) calls $this->lockPayloadNow(), which resolves the active request from the container and evaluates the …
- **Verifier fix note:** The show() endpoint already demonstrates the correct pattern: evaluate lockPayloadFor($request) before the cache block and short-circuit when non-null. The same guard should be applied per-dashboard inside the dashboards() closure, or the lock key should be stripped from the cached array and re-injected outside the cache boundary at response time. Alternatively, invalidate the 'dashboards' cache layer on plan/role change events. The show() guard is not sufficient on its own because the sidebar list drives the lock icon and click-intercept affordances independently of full-page navigation.

### Metric::resolve cache key has no user or tenant segment — metrics with per-user data leak across accounts
- **Severity:** medium · **Category:** security · **Subsystem:** metrics-rest · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Metrics/Metric.php:244-265`
- **Evidence:** $cacheKey = 'martis_metric_'.md5($this->uriKey().'_'.$range.'_'.$filters.'_'.app()->getLocale()); ... $key = md5($this->uriKey().'_'.$range.'_'.$filters.'_'.app()->getLocale());
- **Suggested fix:** Both cache paths (the direct cacheFor() path on line 244 and the MartisCache path on line 265) build the key from uriKey + range + filters + locale only. If any metric's calculate() method scopes results to the authenticated user (e.g. 'orders for my team'), the first user to request it writes the result, and every subsequent user with the same range/locale receives that result from cache. Fix: in …
- **Verifier:** Both cache key expressions in Metric::resolve() are confirmed in the source at lines 244 and 265 of /Users/lmoura/projects/martis/martis-package/src/Metrics/Metric.php and neither includes a user identifier:  Line 244: `$cacheKey = 'martis_metric_'.md5($this->uriKey().'_'.$range.'_'.$filters.'_'.app …
- **Verifier fix note:** The suggested fix (appending `$request->user()?->getAuthIdentifier()` to both key expressions) is correct and sufficient. A secondary improvement would be to document in the Metric base class docblock that calculate() implementations must not scope by user identity directly — all user-context constraints should flow through the filterScope mechanism, which is already included in the cache key via the $filters segment. This prevents the vulnerability class entirely rather than just widening the cache key.

### Client-side avatar size limit (5 MB) larger than server-side default (2 MB)
- **Severity:** medium · **Category:** bug · **Subsystem:** notif-pref-profile · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/Profile/AvatarSection.tsx:8`
- **Evidence:** const MAX_SIZE_MB = 5
- **Suggested fix:** The server enforces `config('martis.profile.avatar.max_size_kb', 2048)` (2 MB default) via `'max:'.$maxKb` in the Laravel validator. The frontend hardcodes 5 MB. Files between 2 MB and 5 MB pass the client guard but are rejected server-side with a 422, confusing users who see a misleading in-progress spinner before the error. Inject the server-configured value into `MartisConfig.profile.avatar.max …
- **Verifier:** The defect is confirmed by reading the actual code:  1. Client guard (AvatarSection.tsx:8): `const MAX_SIZE_MB = 5` — hardcoded to 5 MB. The check at line 40 is `file.size > MAX_SIZE_MB * 1024 * 1024`, so files up to 5 MB pass the client guard without any error shown to the user.  2. Server validato …
- **Verifier fix note:** The fix described is correct. In `app.blade.php` add `'max_size_kb' => (int) config('martis.profile.avatar.max_size_kb', 2048)` inside the `'avatar'` object (line 183-185). In `AvatarSection.tsx` import the config type, replace `const MAX_SIZE_MB = 5` with a derived constant from `config.profile?.avatar?.max_size_kb ?? 2048` (in KB), and update the comparison and the toast message accordingly. The `MAX_SIZE_MB` name should become `MAX_SIZE_KB` to match the unit the server config uses, avoiding a future unit-conversion mistake.

### NotificationController::markRead passes nullable fresh() to non-nullable serialize()
- **Severity:** medium · **Category:** bug · **Subsystem:** notif-pref-profile · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/NotificationController.php:115`
- **Evidence:** return new IlluminateJsonResponse(['data' => $this->serialize($notification->fresh())]);
- **Suggested fix:** `Illuminate\Database\Eloquent\Model::fresh()` returns `?static`. If the notification is concurrently deleted between `markAsRead()` and `fresh()`, the null is passed to `serialize(DatabaseNotification $notification)` which declares a non-nullable parameter, causing a PHP fatal `TypeError`. Fix: `$fresh = $notification->fresh() ?? $notification;` before passing to `serialize()`, or collapse to `$no …
- **Verifier:** The finding is confirmed by reading the actual code. At line 115, `$notification->fresh()` is called and its return value is passed directly to `serialize(DatabaseNotification $notification)` (line 203), which declares a non-nullable parameter. Laravel's `Eloquent\Model::fresh()` returns `?static`,  …
- **Verifier fix note:** Both proposed fixes are valid. The `refresh()` alternative (`$notification->markAsRead(); return new IlluminateJsonResponse(['data' => $this->serialize($notification->refresh())]);`) is the cleaner approach since `refresh()` returns `$this` and mutates the in-memory model with fresh DB state, avoiding the nullable return entirely. The null-coalescing approach (`$notification->fresh() ?? $notification`) also works but silently returns the pre-`markAsRead()` snapshot if the row was deleted, which may confuse the caller. A third option is to treat a deleted-after-mark as a 404: `$fresh = $notification->fresh(); if ($fresh === null) { return JsonErrorResponse::notFound()->toResponse(); }`.

### hasLastUsedColumn static cache leaks across users under long-running processes
- **Severity:** medium · **Category:** bug · **Subsystem:** notif-pref-profile · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Profile/TwoFactorService.php:306-321`
- **Evidence:** private function hasLastUsedColumn(Model $user): bool {     static $cached = null;     if ($cached !== null) {         return $cached;     }     ...     $cached = Schema::connection($user->getConnectionName())         ->hasColumn($user->getTable(), 'two_factor_last_used_at');     ...     return $cached; }
- **Suggested fix:** The PHP function-scope `static` variable is process-persistent under Laravel Octane, Swoole, RoadRunner, and FrankenPHP. In those runtimes the cached value from the first request is reused for every subsequent request in the same worker. If a consumer runs a database migration that adds or drops the `two_factor_last_used_at` column while the process is live, the stale `false` cache will silently d …
- **Verifier:** The code at TwoFactorService.php:306-321 is confirmed exactly as quoted. The `static $cached = null` variable is PHP function-scope static: it is process-persistent and is never reset between requests. The docblock at line 304 claims "Resolves once per request" but the implementation delivers "once  …
- **Verifier fix note:** The suggested fix (key on connection + table name) is correct and sufficient for the schema-drift scenario. A simpler and more idiomatic fix given the existing singleton/scoped pattern in MartisServiceProvider is to change the registration from `singleton` to `scoped`, which would reset the entire object (and therefore all its static-like state) between requests under Octane. However, `static` variables are not instance state -- they survive object destruction -- so changing to `scoped` alone does NOT fix this. The static variable must be changed to an instance property (e.g. `private ?bool $lastUsedColumnCache = null`) so that the scoped binding actually clears it between requests. Alternatively, key the static on `$user->getConnectionName().'.'.$user->getTable()` as suggested, which at least prevents cross-table leakage and makes the column-absent case correct after a migration on any future request (the key would not change between migrations, but at least multi-tenant or multi-table deployments would be correct). The cleanest fix is to convert to an instance property and register the service as `scoped`.

### Default manage-martis-cache gate allows all authenticated users to clear/disable caches
- **Severity:** medium · **Category:** security · **Subsystem:** sec-authz-sweep · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/MartisServiceProvider.php:325`
- **Evidence:** Gate::define('manage-martis-cache', fn ($user) => $user !== null);
- **Suggested fix:** The default is intentionally permissive (documented at line 311-316). Consider tightening the default to deny or to require a dedicated flag (e.g. config('martis.cache.require_admin', false)), with a clear upgrade note. At minimum ensure the docs prominently warn that cache clearing has side effects for all users and that the gate should be overridden in any multi-role deployment.
- **Verifier:** The gate definition at MartisServiceProvider.php:325 is confirmed verbatim: `Gate::define('manage-martis-cache', fn ($user) => $user !== null)`. This is the live default for every fresh Martis installation.  Reading the route file (routes/martis.php:181-263) shows the cache routes are nested inside  …
- **Verifier fix note:** The suggested fix (add a `config('martis.cache.require_admin', false)` flag or tighten the default) is reasonable but the simplest safe path is to flip the default to deny (`fn ($user) => false`) and document that consumers must explicitly open it. Alternatively, keep the permissive default but add a prominent runtime warning (logged at boot when `APP_ENV=production` and the gate has not been overridden) so the permissive posture is impossible to miss in production deployments. The `Gate::has()` guard at line 321 already makes the override mechanism correct; the only gap is that the default is too open for multi-role admin panels.

### Unsanitized sort column in MorphToManyController — arbitrary column enumeration **[FIXED]**
- **Severity:** medium · **Category:** security · **Subsystem:** sec-injection-sweep · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/MorphToManyController.php:85-86`
- **Evidence:** if (is_string($rawSort) && $rawSort !== '') {     $query->orderBy($rawSort, $direction->value); }
- **Suggested fix:** Same fix as BelongsToManyController: validate $rawSort against the sorted list of sortable attributes from Field::flattenLayoutFields() on $relatedResourceClass before calling orderBy(). ResourceController, HasManyController and MorphManyController all apply this guard; this controller pair missed it.
- **Verifier:** Lines 80-87 of MorphToManyController.php confirm the unguarded path verbatim:      $rawSort = $request->query('sort');   // line 80 — direct query string read     ...     if (is_string($rawSort) && $rawSort !== '') {         $query->orderBy($rawSort, $direction->value);   // line 86 — no allowlist   …
- **Verifier fix note:** The suggested fix describes BelongsToManyController as already fixed — it is not. BelongsToManyController has the same unguarded orderBy at its own line 83. Both controllers need the flattenLayoutFields allowlist applied. The fix pattern from HasManyController (lines 482-507) is the correct template to follow for both.

### LensRequest::withOrdering passes unvalidated sort column to orderBy()
- **Severity:** medium · **Category:** security · **Subsystem:** sec-injection-sweep · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Requests/LensRequest.php:72-73`
- **Evidence:** if ($this->sortColumn !== null && $this->sortColumn !== '') {     return $query->orderBy($this->sortColumn, $this->sortDirection->value); }
- **Suggested fix:** The $sortColumn value comes directly from $source->query('sort') (LensRequest.php line 114) and is never validated against a set of declared sortable fields. Because Lens::query() is abstract and implementors call $request->withOrdering($query) as a convenience, any authenticated user can pass any column name. Add an optional $allowedColumns parameter (list<string>) to withOrdering() and skip the  …
- **Verifier:** Confirmed by reading the code directly. LensRequest::fromRequest() at line 113-114 takes the raw `sort` query parameter and stores it unvalidated in $sortColumn. withOrdering() at lines 72-73 then passes $sortColumn directly to $query->orderBy() with no allowlist check. Contrast with ResourceControl …
- **Verifier fix note:** The suggested fix (adding an $allowedColumns list<string> parameter to withOrdering()) is correct in direction but incomplete on its own: lens implementors can opt out by simply not passing the list. The more robust fix mirrors what ResourceController does: LensController::resolveLensFields() already calls Field::flattenLayoutFields() and Field::filterForContext() (lines 383-391). Before calling LensRequest::fromRequest(), the controller should extract the sortable attribute names from those fields and either (a) validate $lensRequest->sortColumn against them immediately after fromRequest() — setting it to null if not in the list — or (b) add the allowedColumns guard inside withOrdering() while also having the controller inject the list. Option (a) is simpler and consistent with how ResourceController handles the same problem. The direction check is safe: SortDirection::tryFrom() at line 117 already coerces the direction to a typed enum value, so only the column name is the exposure.

### Raw exception message from arbitrary Throwable in ActionController action-execution response
- **Severity:** medium · **Category:** security · **Subsystem:** sec-secrets-sweep · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ActionController.php:246`
- **Evidence:** return JsonErrorResponse::serverError('Action failed: '.$e->getMessage())->toResponse();
- **Suggested fix:** Same pattern as the ResourceController delete path. Third-party action handle() methods can throw exceptions whose messages contain SQL snippets, internal service URLs, or stack trace fragments. Return a generic 'Action failed.' to the browser and keep `$e->getMessage()` only in the server-side log. Also applies to line 640 ('Pivot action failed: '.$e->getMessage()).
- **Verifier:** The finding is confirmed by direct code reading. Two pieces of evidence are definitive:  1. `JsonErrorResponse::serverError()` at /Users/lmoura/projects/martis/martis-package/src/Http/Resources/JsonErrorResponse.php:101 has an explicit docblock "Never exposes internal error details to the client" an …
- **Verifier fix note:** Both call sites already have a `Log::error()` call immediately above them that correctly logs `$e->getMessage()` server-side. The fix is only to remove the message from the response: change line 246 to `JsonErrorResponse::serverError('Action failed.')->toResponse()` and line 640 to `JsonErrorResponse::serverError('Pivot action failed.')->toResponse()`. No other changes needed. The `serverError()` docblock already documents this as the intended contract.

### MCP bearer token compared with non-timing-safe string equality
- **Severity:** medium · **Category:** security · **Subsystem:** sec-secrets-sweep · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Mcp/Transport/AuthenticatedStreamableHttpTransport.php:86-89`
- **Evidence:** $expected = 'Bearer '.$this->token;          $wrappedHandler = function (ServerRequestInterface $request) use ($vendorHandler, $expected) {             if ($request->getHeaderLine('Authorization') !== $expected) {
- **Suggested fix:** Replace the `!==` comparison with `hash_equals($expected, $request->getHeaderLine('Authorization'))`. Both the `listen()` path (line 89) and the `createRequestHandler()` path (line 135) must be fixed. `hash_equals` requires both strings to be the same length, so build the expected string the same way first, then compare. This prevents timing-oracle attacks where an attacker probes one character at …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/src/Mcp/Transport/AuthenticatedStreamableHttpTransport.php confirms the finding at both cited locations. Line 86-89 (listen() path) and lines 132-135 (createRequestHandler() path) both build `$expected = 'Bearer '.$this->token` and compare it  …
- **Verifier fix note:** Both sites must be fixed. The standard idiom `hash_equals($expected, $request->getHeaderLine('Authorization'))` is correct: hash_equals returns false immediately on length mismatch (so no length oracle is introduced), and compares in constant time otherwise. No padding or pre-hashing of both sides is needed. The fix is a one-line change at each of the two comparison sites (line 89 and line 135).

### No OAuth state parameter validation — CSRF on the SSO callback endpoint
- **Severity:** medium · **Category:** security · **Subsystem:** sso · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/SsoController.php:55-132`
- **Evidence:** public function callback(Request $request, string $provider): RedirectResponse {     $driver = $this->manager->driver($provider);     ...     $identity = $driver->resolveIdentity($request);     ...     Auth::guard($guard)->login($user, true);
- **Suggested fix:** The callback route is a plain GET with no CSRF token and no state parameter check of its own. Socialite stateful drivers do validate the `state` query parameter internally (comparing it against the session value stored during `redirect()`), which prevents CSRF logins when `stateless = false` (the default). However, the code does not explicitly verify that `stateless` mode is off, and when `statele …
- **Verifier:** The finding is real but its severity is overstated. AzureProvider.php lines 51-53 confirm that when `stateless = true` is set in config, the Socialite driver is switched to stateless mode before `$client->user()` is called. Socialite's stateless mode bypasses the OAuth `state` parameter check entire …
- **Verifier fix note:** The suggested fix is partially correct. Rejecting stateless mode outright would be over-restrictive. The appropriate fix is narrower: add a comment or runtime warning (e.g. a logged notice or a config validation assertion) when `stateless = true` is detected, informing the operator that no OAuth state check will be performed. Alternatively, document this clearly in docs/sso.md alongside the stateless option. The controller does not need to change; the gap is in developer guidance, not in the default code path.

### NativeAdapter role sync: pluck('id') assumes 'id' primary key — breaks UUID / custom PK models
- **Severity:** medium · **Category:** bug · **Subsystem:** sso · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Sso/PermissionAdapters/NativeAdapter.php:33`
- **Evidence:** $resolvedIds = $resolvedRoles->pluck('id')->filter()->all();
- **Suggested fix:** The pluck hardcodes the `id` column name. If the host app's Role model uses a UUID primary key named `uuid` or a custom key from `getKeyName()`, `pluck('id')` returns a collection of nulls and the `->filter()` produces an empty array. All existing roles are deleted from the pivot table and no new ones are inserted, silently stripping the user of every role on every SSO login. Fix: use `$resolvedRo …
- **Verifier:** The bug is confirmed by reading the code directly. In `/Users/lmoura/projects/martis/martis-package/src/Sso/PermissionAdapters/NativeAdapter.php`:  - Line 31: `$modelId = $user->getKey()` — correctly uses Eloquent's portable PK accessor for the user model. - Line 33: `$resolvedIds = $resolvedRoles-> …
- **Verifier fix note:** The fix is `$resolvedRoles->map(fn ($role) => $role->getKey())->filter()->all()` on line 33, consistent with how line 31 already handles the user model. No other changes needed in this file.

## Low (70)

### Dead ternary in RecordImpersonation: both branches of $target instanceof Model return the same value
- **Severity:** low · **Category:** bug · **Subsystem:** auth · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Auth/Listeners/RecordImpersonation.php:55`
- **Evidence:** $targetType = $target instanceof Model ? $target::class : $target::class;
- **Suggested fix:** Both branches of the ternary evaluate to $target::class — the instanceof check has no effect. The original intent was probably to use get_class($target) in one branch, or to produce a different value for non-Model Authenticatable implementations (e.g. a fallback string or a short class name). As written the check is dead code and creates a false impression of defensive handling. Replace with: $tar …
- **Verifier:** Line 55 of /Users/lmoura/projects/martis/martis-package/src/Auth/Listeners/RecordImpersonation.php reads exactly: `$targetType = $target instanceof Model ? $target::class : $target::class;`. Both branches of the ternary evaluate to the identical expression `$target::class`, so the `instanceof Model` …
- **Verifier fix note:** The suggested fix is correct: replace the entire ternary with `$targetType = $target::class;`. No behavioral change occurs — the fix only removes the misleading dead branch.

### RecordRoleChange::normaliseIds returns non-int/non-string scalar or object unvalidated into audit fields
- **Severity:** low · **Category:** bug · **Subsystem:** auth · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Auth/Listeners/RecordRoleChange.php:184-186`
- **Evidence:** if (! is_array($rolesOrIds)) {     return [$rolesOrIds]; }
- **Suggested fix:** When $rolesOrIds is not a Model, not a Collection, and not an array (e.g. a boolean, a float, or an arbitrary object from a future Spatie version), the value is wrapped in an array and returned directly without type validation. This value lands in the ActionEvent fields JSON column. Downstream code or audit-log UIs that assume list<int|string> for ids will behave unexpectedly. The array branch (li …
- **Verifier:** The defect is confirmed by reading the code. In normaliseIds() (lines 184-186), after excluding Model and Collection instances, any remaining non-array value is wrapped directly into an array with no type validation: return [$rolesOrIds]. By contrast, the array branch (lines 188-203) explicitly filt …
- **Verifier fix note:** The suggested fix is accurate. Replace lines 184-186 with: if (is_int($rolesOrIds) || is_string($rolesOrIds)) { return [$rolesOrIds]; } return []; This mirrors the guard already applied to each entry in the array branch and respects the declared list&lt;int|string&gt; return type. No broader change is needed.

### RecordImpersonation: dead ternary — both branches of instanceof-Model guard produce identical output
- **Severity:** low · **Category:** bug · **Subsystem:** authz-gates-impersonation · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Auth/Listeners/RecordImpersonation.php:55`
- **Evidence:** $targetType = $target instanceof Model ? $target::class : $target::class;
- **Suggested fix:** Both branches of the ternary expression return the identical value `$target::class`. The condition `$target instanceof Model` is evaluated but has no effect on the result. The original intent was likely to handle non-Eloquent Authenticatable implementations differently (e.g., storing a generic sentinel string or null for polymorphic morph-map safety). Simplify to `$targetType = $target::class;` an …
- **Verifier:** Line 55 of /Users/lmoura/projects/martis/martis-package/src/Auth/Listeners/RecordImpersonation.php reads exactly: `$targetType = $target instanceof Model ? $target::class : $target::class;`. Both branches of the ternary expression return the identical value `$target::class`. The condition is evaluat …
- **Verifier fix note:** The simplest correct fix is `$targetType = $target::class;`. If non-Model Authenticatables need distinct handling (e.g. a sentinel string for morph safety), add an explicit else branch at that point rather than relying on the dead condition.

### RequestScopedAbilityCache::lookup() is never called — cache is populated but never read, providing zero effect
- **Severity:** low · **Category:** implementation · **Subsystem:** authz-gates-impersonation · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Authorization/RequestScopedAbilityCache.php:55-67`
- **Evidence:** public function lookup(int|string|null $userId, string $ability, mixed ...$arguments): ?bool {     if ($userId === null) {         return null;     }      $key = $this->makeKey($userId, $ability, $arguments);     if ($key === null) {         return null;     }      return $this->cache[$key] ?? null; }
- **Suggested fix:** The handle() listener (line 69) faithfully populates the in-memory $cache array on every GateEvaluated event, but lookup() is never invoked from any production code path (confirmed by grepping the entire src/ tree — the method only appears in its own file). To short-circuit Gate evaluation the cache would need to be wired as a Gate::before() callback (or the Gate's 'before' stack via $gate->before …
- **Verifier:** lookup() has zero callers in the entire src/ tree (confirmed by grepping all PHP files for "lookup()"). The method is defined at RequestScopedAbilityCache.php:55 and is referenced only inside its own file. No Gate::before() callback is registered anywhere in the package (grep for "Gate::before" and  …
- **Verifier fix note:** Do not wire Gate::before() — that contradicts the documented observe-only design. The correct fix is to have the Resource layer (sidebar menu, schema authorization block, per-record authorization block, action visibility) call app(RequestScopedAbilityCache::class)->lookup($userId, $ability, $model) before a redundant $user->can() call and short-circuit when the cached value is non-null. Alternatively, document lookup() as a public API for host-app use and add a note that the package itself does not yet consume it internally.

### PlanRanker class docblock misrepresents fail-open vs fail-closed behavior for unknown plan tiers
- **Severity:** low · **Category:** doc-drift · **Subsystem:** authz-gates-impersonation · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Gates/PlanRanker.php:35-37`
- **Evidence:** * unknown plans rank  * `-1`, comparisons fail open (no lock), and the trait stays inert.
- **Suggested fix:** The class-level docblock conflates two distinct cases: (1) the REQUIRED plan tier is not in the rank table (lines 47-54: returns false = fail OPEN, user is NOT locked), and (2) the CURRENT user's resolved plan is not in the rank table (line 65: rank defaults to -1, which is less than every declared required rank, so the user IS locked = fail CLOSED). The docblock phrase 'unknown plans rank -1, com …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/src/Gates/PlanRanker.php confirms three distinct paths in isLockedFor():  1. Lines 47-53: required tier absent from rank table → returns false (fail-open). Docblock covers this correctly.  2. Lines 57-62: resolver not configured or returns nul …
- **Verifier fix note:** The suggested rewrite is accurate and sufficient. Additionally, the docblock should note case 2 (lines 57-62): when the plan resolver is not configured or returns null, the user is also locked (treated as rank -1 implicitly via the early true return). The revised docblock should cover all three paths: unknown required tier (fail-open), unconfigured resolver (fail-closed), and unknown user plan (fail-closed).

### clear() version increment is a non-atomic read-modify-write — lost bump under concurrent invalidation
- **Severity:** low · **Category:** bug · **Subsystem:** cache · **Confidence:** medium
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Cache/MartisCache.php:215-232`
- **Evidence:** foreach ($types as $t) {     $this->assertKnownType($t);      $current = $this->state($t);     $next = [         'version' => $current['version'] + 1,         'cleared_at' => $now->toIso8601String(),         'override' => $current['override'],     ];      $this->writeState($t, $next); }
- **Suggested fix:** Replace the PHP-side increment with a DB-side atomic update: `CacheState::query()->updateOrInsert(['type' => $t], [...]) ` using a raw expression for version: `DB::raw('version + 1')`. Or wrap the read+write in a `DB::transaction()` with a `lockForUpdate()` on the CacheState row to prevent the race. Two concurrent Artisan `cache:clear` calls (or two queue jobs) both reading version=N will both wri …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/src/Cache/MartisCache.php lines 215-232 confirms the race. `state($t)` (line 223) reads from the PHP in-memory array `$this->states`, which is hydrated from a single `SELECT` in `loadStates()`. The increment `$current['version'] + 1` (line 225 …
- **Verifier fix note:** The suggested fix is correct. The lightest-weight option is wrapping the read+write pair in a DB::transaction() with a lockForUpdate() on the CacheState row for the given type. Alternatively, updateOrInsert can be replaced with an UPDATE using DB::raw('version + 1') so the increment is fully DB-side. Note that updateOrInsert does not support raw expressions for individual columns directly — a raw increment requires using DB::table('martis_cache_state')->where('type', $t)->increment('version') combined with a separate update for cleared_at and override, all inside a transaction.

### writeState() always overwrites created_at on update, destroying audit trail
- **Severity:** low · **Category:** bug · **Subsystem:** cache · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Cache/MartisCache.php:473-482`
- **Evidence:** CacheState::query()->updateOrInsert(     ['type' => $type],     [         'version' => $next['version'],         'cleared_at' => $next['cleared_at'],         'override' => $next['override'],         'updated_at' => Date::now(),         'created_at' => Date::now(),     ], );
- **Suggested fix:** `updateOrInsert()` sends the full values array on both INSERT and UPDATE. The `created_at` should only be set on INSERT. Use `upsert()` with separate `$uniqueBy` and `$update` arrays, or issue a raw `INSERT ... ON DUPLICATE KEY UPDATE` without `created_at` in the update clause. The current code resets `created_at` to now() on every version bump, clearing the record of when the cache layer was firs …
- **Verifier:** The code at MartisCache.php:473-482 calls `CacheState::query()->updateOrInsert(['type' => $type], [..., 'created_at' => Date::now()])`. Reading the actual Laravel Query Builder implementation at vendor/laravel/framework/src/Illuminate/Database/Query/Builder.php:4342-4358 confirms that `updateOrInser …
- **Verifier fix note:** The suggested fix is sound. Replace `updateOrInsert()` with either (a) Eloquent's `upsert()` passing `created_at` only in the insert-column list and omitting it from the update-column list, or (b) a manual SELECT-then-INSERT/UPDATE pattern. Note that Laravel's `upsert()` on MySQL issues `INSERT ... ON DUPLICATE KEY UPDATE` and on SQLite/Postgres issues `INSERT ... ON CONFLICT DO UPDATE`, so the `created_at` exclusion from the update clause is portable. A simpler alternative that avoids the Query Builder entirely: `CacheState::firstOrCreate(['type' => $type])->update([...])` — `firstOrCreate` sets `created_at` only on INSERT, and the subsequent `update()` call never touches it.

### normalizedConfig() returns extension TTL raw without normalizeTtl() — zero/negative TTL bypasses 'no expiry' contract
- **Severity:** low · **Category:** implementation · **Subsystem:** cache · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Cache/MartisCache.php:353-356`
- **Evidence:** if ($raw === null) {     if ($extension !== null) {         return $extension;     }      return ['enabled' => false, 'ttl' => null]; }
- **Suggested fix:** Pass the extension TTL through normalizeTtl() before returning: `return ['enabled' => $extension['enabled'], 'ttl' => $this->normalizeTtl($extension['ttl'])];`. The `extend()` method accepts `?int $ttl`, so callers can pass 0 or a negative integer. The array shape path (line 342-346) and the legacy bare-int path (line 362-365) both normalize TTL, but this extension fast-path skips normalization. A …
- **Verifier:** The defect is confirmed by reading the code directly. Three paths exist in normalizedConfig():  1. Array shape (lines 342-346): ttl passes through normalizeTtl() — zero/negative becomes null (no expiry). 2. Bare-int/legacy path (lines 362-365): ttl passes through normalizeTtl() — zero/negative becom …
- **Verifier fix note:** The suggested fix is correct. The one-liner replacement for line 355 is: `return ['enabled' => $extension['enabled'], 'ttl' => $this->normalizeTtl($extension['ttl'])];`. No other callers need changing — the normalization contract is already satisfied on all other return paths within normalizedConfig().

### SsoMakeCommand provider name is only lowercased but not sanitized to valid identifier characters, producing invalid SQL column names for custom providers
- **Severity:** low · **Category:** bug · **Subsystem:** console · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Console/SsoMakeCommand.php:54,467,536`
- **Evidence:** $name = strtolower((string) $this->argument('provider'));         ...                 "                'role_column' => '{$name}_group_name',\n".         ...         $stub = str_replace('{{column_name}}', "{$name}_group_name", $stub);
- **Suggested fix:** A custom provider name like `my-provider` or `my provider` produces a column name `my-provider_group_name` or `my provider_group_name`, which is an invalid SQL identifier on every supported database. Add a sanitization step after `strtolower`: `$name = preg_replace('/[^a-z0-9_]/', '_', $name);` and validate that the result is non-empty and begins with a letter or underscore. Emit an error and retu …
- **Verifier:** The code at line 54 applies only `strtolower` with no further sanitization. For KNOWN_PROVIDERS (`azure`, `google`, `github`) this is irrelevant because the values are hardcoded clean identifiers. For the `--custom` path (lines 61-66), any string that is non-empty passes through — e.g. `my-provider` …
- **Verifier fix note:** The original suggested fix is correct in principle but the framing overstates the blast radius. Within the existing code, Laravel's Schema Builder and QueryBuilder quote column identifiers correctly, so hyphens do not break the generated migration or the role-mapping query. The genuine residual risks are: (1) spaces in the provider name can cause MySQL DDL errors even with quoting; (2) the unsanitized name is interpolated into a filesystem path (migration filename, line 523) — a name containing `/` or `..` would be a path traversal; (3) any future caller reading `role_column` from config and interpolating it into a raw SQL string would break. Sanitizing with `preg_replace('/[^a-z0-9_]/', '_', $name)` after `strtolower` and rejecting empty/leading-digit results is the right fix. This is developer-controlled CLI input (not a runtime user-controlled boundary), which keeps the severity low.

### ThemeMakeCommand::rewriteThemeNameInConfig regex silently fails when the theme block contains any nested array
- **Severity:** low · **Category:** bug · **Subsystem:** console · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Console/ThemeMakeCommand.php:117`
- **Evidence:** $pattern = "/('theme'\s*=>\s*\[)([^\]]*)( \])/s";
- **Suggested fix:** The character class `[^\]]` stops matching at the first `]` it encounters. For the canonical config (no nested arrays in the theme block) this works, but any user-extended config that adds a sub-array (e.g. `'accents' => ['blue', 'red']`) will cause the regex to capture only up to the inner `]` and miss the `name` key entirely. The config is silently left unchanged while the CSS and public/ files  …
- **Verifier:** The regex at line 117 is `"/('theme'\s*=>\s*\[)([^\]]*)(\])/s"`. The character class `[^\]]*` is greedy but stops the moment it sees any `]`, including the inner closing bracket of a sub-array. PHP testing confirms: with the canonical published config (all scalar values — `'default'`, `'allowToggle' …
- **Verifier fix note:** The bug is real but only affects user-extended configs that add sub-arrays to the `'theme'` block before the `'name'` key. The canonical config (`/Users/lmoura/projects/martis/martis-package/config/martis.php` lines 508-512) is entirely flat scalars, so no published install is currently affected. The simplest safe fix is to match the closing `]` that is indented at the same level as the opening one — for example anchoring on `\n    ]` — rather than the first `]` encountered. The suggested `ListEnvVarsCommand::captureBalancedExpression` approach is also valid but heavier than needed.

### Stale misleading comment in HasGate::lockPreset claims a method_exists guard exists for withBadge() but no guard is present
- **Severity:** low · **Category:** doc-drift · **Subsystem:** core-registry-manager-provider · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Concerns/HasGate.php:43, 136-140`
- **Evidence:** // Line 43 (class docblock): * side when `HasBadge` is absent (the `method_exists` guard below).  // Lines 136-140 (actual call site — no guard present): // so withBadge() is always available — the trait pair is the // documented contract, enforced at install time. $this->withBadge(     (string) ($badge['text'] ?? ''),     (string) ($badge['tone'] ?? 'neutral'), );
- **Suggested fix:** Either add the promised guard (`if (method_exists($this, 'withBadge')) { $this->withBadge(...); }`) to make any future HasGate-only class safe, or remove the misleading phrase 'the method_exists guard below' from the docblock and replace it with 'withBadge() is always available because every entity using this trait also uses HasBadge (enforced by convention)'.
- **Verifier:** The finding is confirmed by reading the code directly. Line 43 of the class-level docblock states "the `method_exists` guard below" but no such guard exists anywhere in the file. At lines 134-143, `lockPreset()` calls `$this->withBadge(...)` unconditionally after only an `isset($preset['badge'])` ch …
- **Verifier fix note:** The suggested fix is correct. The simplest path is to update only the class-level docblock at line 43: replace "the `method_exists` guard below" with "withBadge() is always available because every HasGate consumer also uses HasBadge (enforced by convention)". Adding an actual method_exists guard is not necessary given the convention is consistently followed, but it would make the code more defensive. Both options are valid; the docblock correction alone closes the doc-drift issue without changing behaviour.

### MartisServiceProvider publishes the action_events migration stub twice under the same 'martis-migrations' tag
- **Severity:** low · **Category:** implementation · **Subsystem:** core-registry-manager-provider · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/MartisServiceProvider.php:244-247, 288`
- **Evidence:** // First publish (line 244-247) — registers action_events under 'martis-migrations': $this->publishes([     __DIR__.'/../stubs/create_martis_action_events_table.php.stub' => database_path('migrations/'.date('Y_m_d').'_000001_create_martis_action_events_table.php'), ], 'martis-migrations');  // Second publish (line 287-292) — 'martis-migrations' bundle also includes action_events: $this->publishes([     __DIR__.'/../stubs/create_martis_action_events_table.php.stub' => database_path('migrations/'. …
- **Suggested fix:** Remove the standalone single-file publishes() call for action_events (lines 244-247 block that publishes only that one stub under 'martis-migrations'). The comprehensive bundle block at lines 287-292 already includes action_events alongside all other package migrations, making the earlier single-entry registration redundant.
- **Verifier:** Lines 245-247 register the action_events stub under 'martis-migrations' as a standalone single-entry call. Lines 287-292 register the same stub (identical source __DIR__.'/../stubs/create_martis_action_events_table.php.stub' and identical destination database_path('migrations/'.date('Y_m_d').'_00000 …
- **Verifier fix note:** Remove the standalone block at lines 245-247 (the single-entry 'martis-migrations' registration for action_events). The bundle at lines 287-292 already covers it with the same source and destination paths. No functional change — Laravel's tag merging means both registrations resolve to the same publish operation.

### EnvFilePatcher::remove() leaves an orphaned blank line when set() preceded the key with a blank separator line followed by a comment
- **Severity:** low · **Category:** bug · **Subsystem:** core-registry-manager-provider · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Support/EnvFilePatcher.php:62-70`
- **Evidence:** // set() writes: "\n\n# comment\nKEY=value\n" // remove() forward-scans, pops only ONE preceding comment line: foreach ($lines as $i => $line) {     if (preg_match('/^\s*'.preg_quote($key, '/').'=/', $line)) {         if ($i > 0 && isset($kept[count($kept) - 1]) && str_starts_with(trim($kept[count($kept) - 1]), '#')) {             array_pop($kept); // removes comment but not the blank line before it         }         continue;     }     $kept[] = $line; }
- **Suggested fix:** After popping the comment line, also pop any immediately preceding blank (empty or whitespace-only) lines from `$kept`. Example: `while (count($kept) > 0 && trim($kept[count($kept) - 1]) === '') { array_pop($kept); }` inserted after the existing `array_pop($kept)` call.
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/src/Support/EnvFilePatcher.php confirms the defect exactly as described.  set() (lines 33-42) writes the separator blank line unconditionally as part of $block = $tail."\n", then appends "# comment\n" followed by "KEY=value\n". When the file a …
- **Verifier fix note:** The suggested fix is correct as written. One addition worth considering: the while loop should also run when $i > 0 but no comment precedes the key (i.e. when set() was called with $comment = null, leaving only the blank separator line). In that case the existing guard at line 65 does not trigger at all, so the blank line is also never removed. The fix should be applied unconditionally after the key line is skipped (after the `continue`), or the while loop should be placed after the if-block so it runs regardless of whether a comment was found.

### McpConfigPatcher TOML inline-array parser splits by comma without respecting quoted strings, corrupting values that contain commas
- **Severity:** low · **Category:** bug · **Subsystem:** core-registry-manager-provider · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Support/McpConfigPatcher.php:271`
- **Evidence:** // parseTomlValue() for inline arrays: if (preg_match('/^\[(.*)\]$/', $raw, $m)) {     $items = array_map('trim', explode(',', $m[1]));     return array_values(array_filter(array_map(         fn (string $i) => $i === '' ? null : $this->parseTomlValue($i),         $items,     ), fn ($v) => $v !== null)); }
- **Suggested fix:** Replace the naive `explode(',', $m[1])` split with a parser that respects quoted strings. A simple approach: iterate character-by-character tracking whether the cursor is inside double quotes, and only split on commas that appear outside quotes. Alternatively, use a regex like `/(?:"[^"]*"|[^,])+/` to capture quoted tokens atomically before splitting.
- **Verifier:** The code at McpConfigPatcher.php:271 is confirmed exactly as cited: `$items = array_map('trim', explode(',', $m[1]));` splits TOML inline-array content on every comma without respecting quoted strings. A value like `["hello, world", "foo"]` would be split into three tokens (`"hello`, `world"`, `"foo …
- **Verifier fix note:** The suggested fix is appropriate. The simplest correct approach is to replace the naive explode with a character-by-character tokenizer that tracks whether the cursor is inside double quotes and only splits on commas outside quotes. The regex alternative `/(?:"[^"]*"|[^,])+/` works for simple quoted strings but fails on escaped quotes inside strings (e.g. `"he said \"hi, there\""`); since tomlEncodeValue already escapes inner double quotes with a backslash, a more robust approach would scan for `,` while skipping over `"..."` spans that handle `\"` as a non-terminating sequence.

### configuration.md 'Index / Default row actions' prose contradicts its own env var table and the actual config
- **Severity:** low · **Category:** doc-drift · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:593`
- **Evidence:** Line 593: "There are no per-action env toggles. Per-action visibility is controlled by Resource::defaultRowActions(Request $request): bool|array"  However the same file's env var table at lines 1290-1292 lists: | MARTIS_DEFAULT_ROW_ACTION_DELETE | true | | MARTIS_DEFAULT_ROW_ACTION_EDIT   | true | | MARTIS_DEFAULT_ROW_ACTION_VIEW   | true |  And the actual config at martis.php lines 1103-1105 has: 'view'   => env('MARTIS_DEFAULT_ROW_ACTION_VIEW', true), 'edit'   => env('MARTIS_DEFAULT_ROW_ACTION …
- **Suggested fix:** Remove the sentence "There are no per-action env toggles" from line 593 and add a row to the Index table documenting MARTIS_DEFAULT_ROW_ACTION_VIEW, MARTIS_DEFAULT_ROW_ACTION_EDIT, MARTIS_DEFAULT_ROW_ACTION_DELETE. The prose section at lines 579-600 should also include these keys in its code block (currently it omits the view/edit/delete sub-keys from the default_row_actions block shown at line 11 …
- **Verifier:** Confirmed by reading both files directly. docs/configuration.md line 593 says "There are no per-action env toggles." That statement is false. config/martis.php lines 1103-1105 define three env-backed per-action kill-switches: 'view' => env('MARTIS_DEFAULT_ROW_ACTION_VIEW', true), 'edit' => env('MART …
- **Verifier fix note:** Severity is low rather than medium because this is a pure documentation defect with no runtime impact. The env vars and config keys work correctly; only the prose misleads developers. The fix is: (1) delete line 593's "There are no per-action env toggles." sentence, (2) expand the code block at lines 577-584 to include the view/edit/delete sub-keys, (3) add three rows to the table at lines 587-591 documenting default_row_actions.view, default_row_actions.edit, and default_row_actions.delete.

### configuration.md profile 'sections' example omits 'sessions' which is present in the actual config default
- **Severity:** low · **Category:** doc-drift · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:834`
- **Evidence:** Docs (line 834): "'sections' => ['account', 'password', 'avatar', 'security'],"  Actual config (martis.php line 1276): "'sections' => ['avatar', 'account', 'password', 'security', 'sessions'],"  The 'sessions' section is backed by BrowserSessionsService (src/Profile/BrowserSessionsService.php) which lists and terminates other browser sessions.
- **Suggested fix:** Update the profile config example in configuration.md to include 'sessions' and match the actual default order: ['avatar', 'account', 'password', 'security', 'sessions'].
- **Verifier:** Directly confirmed by reading both files. configuration.md line 834 shows `'sections' => ['account', 'password', 'avatar', 'security']` while config/martis.php line 1276 shows `'sections' => ['avatar', 'account', 'password', 'security', 'sessions']`. Two genuine divergences: (1) `sessions` is absent …
- **Verifier fix note:** The fix should update configuration.md line 834 to match both the value set and the order from the actual default: `'sections' => ['avatar', 'account', 'password', 'security', 'sessions'],`. Severity is low (not medium) because this is a documentation example, not runtime behaviour. A developer who reads only the docs will produce a valid (but incomplete) config; the application will not break, they will simply not know `sessions` is available as an opt-in section.

### configuration.md env var table missing MARTIS_USER_ID_COLUMN_TYPE (added v1.12.2)
- **Severity:** low · **Category:** doc-drift · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:1214-1345`
- **Evidence:** config/martis.php line 44: 'user_id_column_type' => env('MARTIS_USER_ID_COLUMN_TYPE'),  The env var table in configuration.md (lines 1214-1345) is described as auto-generated by 'php artisan martis:list-env-vars' and claims to reflect '130 env vars in the current build'. MARTIS_USER_ID_COLUMN_TYPE appears in installation-guide.md (line 203) but is absent from the env var table in configuration.md.
- **Suggested fix:** Add 'MARTIS_USER_ID_COLUMN_TYPE' to the env var table in configuration.md with default '(no default)'. Also add a prose section under Storage or Installation documenting the four accepted values: bigint, uuid, ulid, string.
- **Verifier:** Direct evidence: 1. /Users/lmoura/projects/martis/martis-package/config/martis.php:44 — `'user_id_column_type' => env('MARTIS_USER_ID_COLUMN_TYPE')` — the env var is defined with no default value, available since v1.12.2 (per the surrounding comment block at lines 38-44). 2. grep for "MARTIS_USER_ID …
- **Verifier fix note:** The suggested fix is correct: add a row for MARTIS_USER_ID_COLUMN_TYPE with default "(no default)" to the env var table in configuration.md. Additionally, the prose on line 1207 states "130 env vars" but the table now has 152 rows — that count should be updated or the claim removed to avoid confusion. Adding a prose subsection under Storage or Installation documenting the four accepted values (bigint, uuid, ulid, string) would also align with the detail already present in installation-guide.md:203.

### configuration.md Dashboard section omits 'showWelcomeCard' from the config table despite it being a real env-driven key
- **Severity:** low · **Category:** doc-drift · **Subsystem:** docs-sync-package-vs-code · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/docs/configuration.md:547-563`
- **Evidence:** The 'Dashboard' section code block (line 549-555) and the table (lines 558-563) show only showGreeting, showWelcome, showMetrics, showResourceCards. It omits showWelcomeCard.  Actual config (martis.php line 747): 'showWelcomeCard' => env('MARTIS_DASHBOARD_SHOW_WELCOME_CARD', true),  The env var table at line 1285 does list MARTIS_DASHBOARD_SHOW_WELCOME_CARD, and the text above (line 312) references it, but the section's own table is incomplete.
- **Suggested fix:** Add 'showWelcomeCard' to the Dashboard section code block and table in configuration.md: | showWelcomeCard | bool | true | Show the animated WelcomeCard hero at the top of the dashboard. Override with MARTIS_DASHBOARD_SHOW_WELCOME_CARD=false.
- **Verifier:** Direct code inspection confirms the defect. In /Users/lmoura/projects/martis/martis-package/config/martis.php at line 747 the key 'showWelcomeCard' => env('MARTIS_DASHBOARD_SHOW_WELCOME_CARD', true) is a real, documented config entry with its own inline comment block (lines 726-730 describe it as co …
- **Verifier fix note:** Add 'showWelcomeCard' as a fifth entry to both the PHP code block (line 554, after showWelcome) and the table (line 563, after showWelcome row) in the Dashboard section. Suggested row: | `showWelcomeCard` | `bool` | `true` | Show the animated WelcomeCard hero at the top of the dashboard. Override with `MARTIS_DASHBOARD_SHOW_WELCOME_CARD=false`. |

### authentication.mdx has extra security sentence absent from pkg source
- **Severity:** low · **Category:** doc-drift · **Subsystem:** docs-sync-pkg-vs-mdx · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-docs/src/content/auth/authentication.mdx:39`
- **Evidence:** The mdx `email_verification_required` row ends with: `The response carries no user payload, so a swapped Login.tsx that ignores the flag cannot bypass the gate.` This sentence is absent from the corresponding pkg line at `/Users/lmoura/projects/martis/martis-package/docs/authentication.md:33`.
- **Suggested fix:** Port the sentence to the pkg source so both are identical. The sentence adds useful security context and should live in both.
- **Verifier:** Direct comparison of the two files confirms the drift. In /Users/lmoura/projects/martis/martis-docs/src/content/auth/authentication.mdx:39, the `email_verification_required` table row ends with the sentence "The response carries no user payload, so a swapped `Login.tsx` that ignores the flag cannot  …
- **Verifier fix note:** The sentence is accurate and adds useful security context. Port it to /Users/lmoura/projects/martis/martis-package/docs/authentication.md:33, appending it to the end of the `email_verification_required` description so both files are identical. Severity is low because this is a documentation-only drift with no runtime impact; the server-side gate itself is not affected.

### roles.mdx has truncated --promote flag description (missing 'Skips the manual assignRole step')
- **Severity:** low · **Category:** doc-drift · **Subsystem:** docs-sync-pkg-vs-mdx · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-docs/src/content/auth/roles.mdx:61`
- **Evidence:** The pkg source at `/Users/lmoura/projects/martis/martis-package/docs/roles.md:56` reads: `| --promote= | Email of the user to seed + promote to admin in the same call. Pass first to grab the lowest-id user. Skips the manual assignRole('admin') step. v1.8.15. |`. The mdx at line 61 reads: `| --promote= | Email of the user to seed + promote to admin in the same call. Pass first to grab the lowest-id user. v1.8.15. |` — the sentence 'Skips the manual assignRole step' is missing from the mdx.
- **Suggested fix:** Add the missing sentence back to the mdx: `Skips the manual assignRole('admin') step.`
- **Verifier:** Direct comparison of the two files confirms the drift. The package source at /Users/lmoura/projects/martis/martis-package/docs/roles.md line 56 contains the full cell text: "Email of the user to seed + promote to admin in the same call. Pass `first` to grab the lowest-id user. Skips the manual `assi …
- **Verifier fix note:** Append "Skips the manual `assignRole('admin')` step." to the `--promote=` row in /Users/lmoura/projects/martis/martis-docs/src/content/auth/roles.mdx line 61, before "v1.8.15.", to match the package source exactly.

### overrides.mdx has different wording for the `--frontend` troubleshooting hint
- **Severity:** low · **Category:** doc-drift · **Subsystem:** docs-sync-pkg-vs-mdx · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-docs/src/content/customization/overrides.mdx:805`
- **Evidence:** The pkg source at `/Users/lmoura/projects/martis/martis-package/docs/overrides.md:794` reads: `Any key that appears in martis:list-overrides but not in componentRegistry.keys() is a missing TSX file under resources/js/martis-extensions/ — the most common reason an override fails to resolve.` The mdx at line 805 reads: `Any key that appears in martis:list-overrides but not in componentRegistry.keys() is a missing registration in your extension bundle entry — the most common reason an override fai …
- **Suggested fix:** Minor wording divergence ('missing TSX file under resources/js/martis-extensions/' vs 'missing registration in your extension bundle entry'). The mdx wording is slightly more accurate for v1.9+ since registrations can come from `index.ts` imports and not only from auto-discovered TSX files. Sync the pkg to the mdx's wording.
- **Verifier:** Both files were read directly. The divergence is confirmed at the exact cited lines.  pkg doc (`martis-package/docs/overrides.md:794`): "missing TSX file under `resources/js/martis-extensions/`" mdx (`martis-docs/src/content/customization/overrides.mdx:805`): "missing registration in your extension  …
- **Verifier fix note:** The suggested fix direction is reversed. The pkg doc wording is correct for v1.9+ auto-discovery: the consumer only needs to place a TSX file in the right bucket; no manual registration call is needed. It is the MDX that should be corrected: replace "missing registration in your extension bundle entry" with "missing TSX file under `resources/js/martis-extensions/`" to match the pkg doc and the actual auto-discovery implementation in `index.ts.stub`.

### overrides.mdx has changed step 4 description in component command section
- **Severity:** low · **Category:** doc-drift · **Subsystem:** docs-sync-pkg-vs-mdx · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-docs/src/content/customization/overrides.mdx:629`
- **Evidence:** The pkg source at `/Users/lmoura/projects/martis/martis-package/docs/overrides.md:623` step 4 reads: `Shell stubs document the exact props the shell injects (collapsed state, mobile drawer callbacks, navigation payload from /api/navigation) so you can skip reading the source.` The mdx at line 629 reads: `For shell types, uses stubs that document the exact props the shell injects...` — The mdx is imprecise ('uses stubs' has no subject; it should be 'The command uses stubs...' or the pkg's origina …
- **Suggested fix:** The mdx wording introduced a grammatical fragment ('For shell types, uses stubs...'). Revert to the pkg's clearer prose ('Shell stubs document...') in both files.
- **Verifier:** Both files were read at the cited lines. The pkg source at martis-package/docs/overrides.md:623 reads: "Shell stubs document the exact props the shell injects (collapsed state, mobile drawer callbacks, navigation payload from /api/navigation) so you can skip reading the source." The mdx at martis-do …
- **Verifier fix note:** The mdx line 629 should be updated to match the pkg wording: replace "For shell types, uses stubs that document" with "Shell stubs document". No change needed to martis-package/docs/overrides.md:623 — it is already the correct form.

### differentials.mdx has wrong component name in code comment (RevenueGauge vs WelcomeCard)
- **Severity:** low · **Category:** doc-drift · **Subsystem:** docs-sync-pkg-vs-mdx · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-docs/src/content/reference/differentials.mdx:978`
- **Evidence:** The pkg source at `/Users/lmoura/projects/martis/martis-package/docs/differentials.md:972` reads: `# auto-discovery binds card:welcome-card to RevenueGauge.tsx).` The mdx at line 978 reads: `# auto-discovery binds card:welcome-card to WelcomeCard.tsx).` — the key is `card:welcome-card` but the pkg example uses a different component filename (`RevenueGauge.tsx`), while the mdx uses `WelcomeCard.tsx`.
- **Suggested fix:** The key `card:welcome-card` auto-derives from the component filename only when the filename IS `WelcomeCard.tsx`. The pkg comment that says `RevenueGauge.tsx` is therefore wrong — the key would be `card:revenue-gauge` for that filename. The mdx version (`WelcomeCard.tsx` → `card:welcome-card`) is internally consistent. Fix the pkg source to use `WelcomeCard.tsx` to match the mdx, or change both to …
- **Verifier:** Both files were read directly. The pkg source at /Users/lmoura/projects/martis/martis-package/docs/differentials.md:972 contains the comment `# auto-discovery binds card:welcome-card to RevenueGauge.tsx).` immediately above `php artisan martis:card WelcomeCard`. The `martis:card WelcomeCard` command …
- **Verifier fix note:** Fix the pkg source only: change `RevenueGauge.tsx` to `WelcomeCard.tsx` at /Users/lmoura/projects/martis/martis-package/docs/differentials.md:972. The mdx is already correct and needs no change.

### Abstract interface methods in Filter.php carry duplicate docblocks instead of {inheritdoc}
- **Severity:** low · **Category:** convention · **Subsystem:** enums-contracts · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Filters/Filter.php:60-72`
- **Evidence:** /**  * Apply the filter to the given query.  *  * @param  Builder<Model>  $query  */ abstract public function apply(Request $request, Builder $query, mixed $value): Builder;  /**  * The filter type identifier sent to the frontend.  */ abstract public function filterType(): FilterType;
- **Suggested fix:** Replace both docblocks with `/** {@inheritdoc} */`. The authoritative documentation lives in FilterContract::apply() and FilterContract::filterType().
- **Verifier:** Confirmed by reading both files directly. FilterContract.php (lines 26-30, 43-45) carries the authoritative docblocks for apply() and filterType(). Filter.php (lines 60-65, 67-70) re-declares both methods as abstract and attaches its own descriptive docblocks instead of `/** {@inheritdoc} */`. The w …
- **Verifier fix note:** Replace the docblock at Filter.php:60-64 and the docblock at Filter.php:67-69 each with `/** {@inheritdoc} */`. The @param Builder<Model> annotation on apply() does not need to be preserved in the abstract class because the contract already carries it and IDEs/PHPStan resolve it via the interface. If static analysis tooling requires the @param on the abstract re-declaration for type inference to flow to subclasses, keep the @param-only docblock rather than bare `{inheritdoc}`, but that is an edge case worth checking before applying the fix blindly.

### Abstract interface methods in Metric.php carry duplicate docblocks instead of {inheritdoc}; concrete cacheFor() also violates the rule
- **Severity:** low · **Category:** convention · **Subsystem:** enums-contracts · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Metrics/Metric.php:91-99, 221-226`
- **Evidence:** /**  * Calculate the metric value.  */ abstract public function calculate(Request $request): mixed;  /**  * The metric type identifier.  */ abstract public function metricType(): MetricType;  // at line 221: /**  * How long to cache the metric result. Return null to disable caching.  */ public function cacheFor(): ?DateTimeInterface
- **Suggested fix:** Replace all three docblocks with `/** {@inheritdoc} */`. The authoritative documentation lives in MetricContract::calculate(), MetricContract::metricType(), and MetricContract::cacheFor().
- **Verifier:** Confirmed by reading both files directly. MetricContract (/Users/lmoura/projects/martis/martis-package/src/Contracts/MetricContract.php) declares authoritative docblocks for calculate() at line 26, metricType() at line 31, and cacheFor() at line 53. Metric.php (/Users/lmoura/projects/martis/martis-p …
- **Verifier fix note:** Replace the three duplicate docblocks with `/** {@inheritdoc} */`. No behavioral change; the authoritative documentation already lives in MetricContract. Note that the abstract methods (calculate, metricType) are correctly flagged — being abstract does not exempt them from the `{@inheritdoc}` rule when the interface already documents them.

### Abstract interface methods in Resource.php carry duplicate docblocks instead of {inheritdoc}
- **Severity:** low · **Category:** convention · **Subsystem:** enums-contracts · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Resource.php:80-92`
- **Evidence:** /**  * Return all fields for this resource.  *  * @return list<FieldContract>  */ abstract public function fields(Request $request): array;  /**  * Return the fully-qualified Eloquent model class name.  */ abstract public static function model(): string;
- **Suggested fix:** Replace both docblocks with `/** {@inheritdoc} */`. The full documentation lives in ResourceContract per the project-local inheritdoc rule: interface owns the full docblock; implementing class (including abstract stubs) uses only `/** {@inheritdoc} */`.
- **Verifier:** Resource.php (line 17) implements ResourceContract. Lines 81-86 carry a full multi-line docblock on the abstract `fields()` stub, and lines 88-91 carry a full single-line docblock on the abstract `model()` stub. ResourceContract.php already owns complete docblocks for both methods (lines 29-34 for ` …
- **Verifier fix note:** Replace the docblock on both abstract stubs in Resource.php with `/** {@inheritdoc} */`. No change to ResourceContract.php needed — it already holds the authoritative documentation.

### GlobalSearch: redundant double-guard on item.url before navigation
- **Severity:** low · **Category:** bug · **Subsystem:** fe-components · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/GlobalSearch.tsx:304-309`
- **Evidence:** if (item.url) {   if ('url' in item && item.url) {     navigate(item.url)     onClose()   } }
- **Suggested fix:** The inner `if ('url' in item && item.url)` is always true when the outer `if (item.url)` is true. Collapse to a single guard: `if (item.url) { navigate(item.url); onClose() }`. The current nesting is dead logic but harmless — it just masks the intent.
- **Verifier:** Lines 304-309 of GlobalSearch.tsx contain the exact code cited. The outer `if (item.url)` already guards for a truthy url value. Inside that block, `'url' in item` is unconditionally true because every variant of the `PaletteItem` union (lines 83-88) declares a `url` field — `resource`, `action`, `v …
- **Verifier fix note:** The suggested collapse to `if (item.url) { navigate(item.url); onClose() }` is correct. No TypeScript type assertion is needed because the outer check already narrows `item.url` to a truthy string for all union variants that can reach this branch.

### Native title= tooltips in PreferencesMenu (convention violation, 3 occurrences)
- **Severity:** low · **Category:** convention · **Subsystem:** fe-components · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/PreferencesMenu.tsx:133,159,295`
- **Evidence:** title={t('preferences', 'Preferences')} // line 133 on trigger button title={t('reset_to_defaults', 'Reset to defaults')} // line 159 on reset button title={t('pick_brand_color', 'Pick a custom accent')} // line 251 on color label title={`${titleLabel} (custom)`} // line 219 on custom accent swatch title={label} // line 196 on accent swatch buttons
- **Suggested fix:** Replace all `title=` attributes with `data-pr-tooltip` + `data-pr-position` to use the MartisTooltip pattern. The trigger button at line 133, the reset button at line 159, each accent swatch, and the color picker label are all affected.
- **Verifier:** Verified by reading the actual file. PreferencesMenu.tsx contains 6 native title= attributes at lines 133, 159, 194, 219, 250, and 290. None of these elements use data-pr-tooltip. The MartisTooltip component (mounted globally in Layout.tsx line 211 and AuthFrame.tsx line 72) works via event delegati …
- **Verifier fix note:** The finding undercounts: there are 6 native title= occurrences (lines 133, 159, 194, 219, 250, 290), not 3. The cited line 295 is a closing div tag; the actual clear-brand-color title= is at line 290. Each title= should be replaced with data-pr-tooltip (plus data-pr-position where needed). Because MartisTooltip is global, no import is required in PreferencesMenu.tsx itself. The aria-label attributes already present on the trigger button (line 129) and color-picker input (line 261) can stay as-is for accessibility; only the visual tooltip mechanism changes.

### native window.confirm() used in BrowserSessionsSection — not i18n-safe and breaks in some environments
- **Severity:** low · **Category:** convention · **Subsystem:** fe-components · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/Profile/BrowserSessionsSection.tsx:97`
- **Evidence:** if (!confirm(t('sessions_revoke_others_confirm', {   defaultValue: 'Sign out of every other device? You will stay signed in here.', }))) {   return }
- **Suggested fix:** Replace `window.confirm()` with the existing `DeleteModal` component (or a similar `martis-modal-*` confirmation dialog) to keep the UX consistent with the design system. `window.confirm()` is also blocked in iframes (common in some admin dashboards) and cannot be themed or localised beyond the string it receives.
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/resources/js/components/Profile/BrowserSessionsSection.tsx line 97-101 contains exactly the reported `window.confirm()` call. A `DeleteModal` component exists at /Users/lmoura/projects/martis/martis-package/resources/js/components/DeleteModal/ …
- **Verifier fix note:** Wire in a `DeleteModal` (or a lightweight confirm variant) controlled by a local `showConfirm` state boolean. Set `open={showConfirm}`, pass `confirmMessage={t('sessions_revoke_others_confirm', ...)}`, fire the revoke logic in `onConfirm`, and cancel in `onCancel`. The `resourceLabel` prop can be set to an empty string or omitted if the modal variant is adjusted, since this is not a record-delete flow but a session-revoke flow. Consider adding a `variant='danger'` or reusing the existing `confirmMessage` prop to surface a custom message without showing the resource-label delete pattern.

### BrowserSessionsSection: i18n-hardcoded English strings in relativeTime() helper
- **Severity:** low · **Category:** convention · **Subsystem:** fe-components · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/Profile/BrowserSessionsSection.tsx:60-67`
- **Evidence:** function relativeTime(timestamp: number, locale = 'en'): string {   const seconds = Math.floor(Date.now() / 1000) - timestamp   if (seconds < 60) return 'just now'   if (seconds < 3600) return `${Math.floor(seconds / 60)} min ago`   if (seconds < 86400) return `${Math.floor(seconds / 3600)} h ago`   ... }
- **Suggested fix:** The strings 'just now', 'min ago', 'h ago' are hardcoded English. The function receives `locale` (from `i18n.language`) and uses it correctly for `Intl.DateTimeFormat`, but ignores it for the early branches. Use `Intl.RelativeTimeFormat` for all branches, or at minimum route these strings through the `t()` function in the calling component.
- **Verifier:** Lines 58-68 of BrowserSessionsSection.tsx confirm the defect exactly as reported. The `relativeTime` function accepts a `locale` parameter and correctly threads it to `Intl.DateTimeFormat` for timestamps older than 24 hours (line 64), but returns hardcoded English literals for the three short-durati …
- **Verifier fix note:** The cleanest fix is to replace the three early-return strings with `Intl.RelativeTimeFormat` calls using the `locale` argument — for example `new Intl.RelativeTimeFormat(locale, { numeric: 'auto' }).format(-Math.floor(seconds / 60), 'minute')`. This keeps i18n consistent with the existing `Intl.DateTimeFormat` path and avoids adding translation keys for what are effectively numeric expressions. If `Intl.RelativeTimeFormat` browser support is a concern for the target baseline, routing them through `t()` with interpolation keys is the fallback. Also note that `deviceLabel` (lines 45-55) has the same hardcoded-English pattern and should be fixed in the same pass.

### SecuritySection: passwordError default value contains Portuguese — violates English-in-code rule
- **Severity:** low · **Category:** convention · **Subsystem:** fe-components · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/Profile/SecuritySection.tsx:54`
- **Evidence:** setPasswordError(t('current_password_wrong', { defaultValue: 'Password incorrecta. Tente novamente.' }))
- **Suggested fix:** The `defaultValue` is an English fallback used when the translation key is missing — it should always be in English. Change to: `{ defaultValue: 'Incorrect password. Please try again.' }`. This conforms to the workspace rule that code (including i18n fallback strings) stays in English.
- **Verifier:** The translation key `current_password_wrong` does not exist in any of the three server-side lang files (`resources/lang/en/profile.php`, `resources/lang/pt_BR/profile.php`, `resources/lang/pt_PT/profile.php`). The i18n setup in `resources/js/lib/i18n.ts` fetches all translations from `/api/translati …
- **Verifier fix note:** The `defaultValue` fix alone is insufficient. The key `current_password_wrong` is missing from all three PHP lang files. Both fixes are required: (1) add `'current_password_wrong' => 'Incorrect password. Please try again.'` to `resources/lang/en/profile.php`, an appropriate translation to `pt_BR/profile.php`, and another to `pt_PT/profile.php`; (2) change the `defaultValue` in `SecuritySection.tsx:54` to English (`'Incorrect password. Please try again.'`) as the last-resort fallback if translations fail to load. Without step 1, even with an English `defaultValue`, the actual translation served to pt_BR and pt_PT users would still be missing.

### Native title= tooltip on hamburger menu button (convention violation)
- **Severity:** low · **Category:** convention · **Subsystem:** fe-components · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/Topbar.tsx:148`
- **Evidence:** <button   type="button"   className="martis-tb-menu-btn"   onClick={onToggleSidebar}   aria-label={t("open_sidebar", "Menu")}   title={t("open_sidebar", "Menu")} >
- **Suggested fix:** Remove the `title` prop and add `data-pr-tooltip={t('open_sidebar', 'Menu')}` + `data-pr-position="bottom"` instead. The native `title=` tooltip is explicitly forbidden by the workspace convention (same rule already applied to the adjacent collapse button).
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/resources/js/components/Topbar.tsx lines 142-152 confirms the finding exactly as reported. The hamburger button has `title={t("open_sidebar", "Menu")}` at line 148. The adjacent collapse button (lines 154-171) follows the correct pattern: `dat …
- **Verifier fix note:** The fix is correct as described: remove the `title` prop and add `data-pr-tooltip={t('open_sidebar', 'Menu')}` plus `data-pr-position="bottom"`. The collapse button in the same file (lines 160-163) is the direct template to follow.

### DrawerCreate/DrawerUpdate form items keyed by array index, breaking React reconciliation on field reorder
- **Severity:** low · **Category:** bug · **Subsystem:** fe-components · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/overrides/DrawerCreate.tsx:196-205`
- **Evidence:** {allFormFields.map((item, idx) => {   if (item.type === 'tab_group') {     return <TabsInput key={idx} ...   if (item.type === 'section') {     return <SectionInput key={idx} ...   if (item.type === 'panel') {     return <PanelInput key={idx} ...
- **Suggested fix:** The same issue exists in DrawerUpdate (lines 277-284). Use a stable key: for `tab_group`, `section`, and `panel`, use their `label` property; for scalar fields, use `field.attribute`. Index keys cause input state to persist in the wrong field when the server reorders fields (e.g. via a conditional `->when()`), and can cause controlled components to lose focus. DrawerDetail also uses `key={\`tg-${i …
- **Verifier:** The code at DrawerCreate.tsx:197-204 and DrawerUpdate.tsx:278-284 confirms the index-key pattern. For tab_group, section, and panel items, `key={idx}` is used, while scalar fields correctly use `key={field.attribute}` at line 208. The pattern is real.  However, the claimed severity scenario is overs …
- **Verifier fix note:** The fix suggestion (use label for structural items, field.attribute for scalar fields) is correct for DrawerCreate and DrawerUpdate. DrawerDetail does not need fixing because it is display-only with no controlled input state. The scenario involving PHP ->when() is not accurate: those methods filter fields at schema serialization time, they do not cause in-session reordering of the fieldsForCreate array.

### BelongsToFieldInput useEffect depends on JSON.stringify(value) — unstable serialisation of File objects crashes the effect
- **Severity:** low · **Category:** bug · **Subsystem:** fe-fields · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/BelongsToField.tsx:433`
- **Evidence:** }, [isMultiple, JSON.stringify(value)])
- **Suggested fix:** JSON.stringify on a File object returns '{}', so every call with a File value serialises identically. More critically, JSON.stringify inside a dependency array is an eslint-react-hooks violation (every render produces a new string, so the effect fires on every render regardless of whether value actually changed). Replace with a stable derived value: track the value array length and first id, or us …
- **Verifier:** Line 433 is confirmed: `}, [isMultiple, JSON.stringify(value)])`. The dependency on `JSON.stringify(value)` is present. However, the reporter's primary severity driver — "JSON.stringify on a File object returns '{}', so every call with a File value serialises identically" and "crashes the effect" —  …
- **Verifier fix note:** The "crashes the effect" claim and the File-object scenario are both invalid for this component. The actual issue is a lint anti-pattern: JSON.stringify in a dep array is opaque and triggers an eslint-react-hooks warning. The suggested fix (derive a stable key via .map(v => v.id).join(',') in a useMemo) is reasonable, but should be scoped to BelongsToValue arrays only, which the existing isBelongsToValue guard already ensures at runtime. The fix is a clean-up, not a correctness repair.

### IconField icon-picker buttons use native HTML title= for icon name tooltip
- **Severity:** low · **Category:** convention · **Subsystem:** fe-fields · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/IconField.tsx:302`
- **Evidence:** title={name}
- **Suggested fix:** Replace with `data-pr-tooltip={name} data-pr-position="top"` to align with the global PrimeReact Tooltip pattern.
- **Verifier:** The offending code is confirmed at /Users/lmoura/projects/martis/martis-package/resources/js/components/fields/IconField.tsx:302: the icon-picker grid buttons use `title={name}` (native browser tooltip) rather than `data-pr-tooltip={name} data-pr-position="top"`.   Evidence that this is a real viola …
- **Verifier fix note:** Replace `title={name}` at line 302 with `data-pr-tooltip={name} data-pr-position="top"`. The global MartisTooltip selector already covers the whole layout, so no additional Tooltip mount is needed. The clear button in the same component (lines 234-235) is the correct reference implementation.

### RepeaterField buttons use native HTML title= instead of data-pr-tooltip
- **Severity:** low · **Category:** convention · **Subsystem:** fe-fields · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/RepeaterField.tsx:334, 363, 376, 386, 473`
- **Evidence:** title={tAct('reorder', 'Reorder')}                   title={isCollapsed ? tAct('expand', 'Expand') : tAct('collapse', 'Collapse')}                   title={tAct('duplicate_row', 'Duplicate row')}                 title={tAct('remove', 'Remove')}               title={tAct('paste_rows', 'Paste rows (CSV/TSV/JSON)')}
- **Suggested fix:** Replace all `title={...}` tooltip attributes on interactive elements in RepeaterField with `data-pr-tooltip={...} data-pr-position="top"` to match the global PrimeReact Tooltip pattern used throughout Martis. The native title= attribute produces a browser-default tooltip that doesn't respect the design system's theme, positioning, or timing.
- **Verifier:** All five cited lines are confirmed by reading the file. Lines 334, 363, 376, 386, and 473 of RepeaterField.tsx all use the native HTML `title=` attribute on interactive elements (the reorder drag handle, collapse/expand button, duplicate button, remove button, and bulk-paste button). The sibling fie …
- **Verifier fix note:** Each `title={...}` on an interactive element should become `data-pr-tooltip={...} data-pr-position="top"`. The global Tooltip provider is already mounted above RepeaterField in the render tree (confirmed by the same pattern working in sibling fields), so no additional `<Tooltip />` import or mount is required inside RepeaterField. The non-interactive reorder drag `<span>` at line 334 can be treated the same way for consistency, though it is lower priority than the `<button>` elements.

### TagField remove-chip tooltip leaks 'undefined' when tag.title is null
- **Severity:** low · **Category:** bug · **Subsystem:** fe-fields · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/components/fields/TagField.tsx:220`
- **Evidence:** data-pr-tooltip={`Remove ${tag.title}`}
- **Suggested fix:** tag.title is typed as `string | null`. When it is null the tooltip renders 'Remove null'. Use `tag.title ?? String(tag.id)` or translate: `tMsg('tag_remove', { tag: tag.title ?? String(tag.id), defaultValue: `Remove ${tag.title ?? tag.id}` })`.
- **Verifier:** The bug is confirmed but the reporter's stated symptom is slightly wrong. At line 220, `data-pr-tooltip={\`Remove ${tag.title}\`}` interpolates `tag.title` directly. The `TagValue` interface (line 11) declares `title?: string | null`, so `tag.title` can be `null` (produces "Remove null") or `undefin …
- **Verifier fix note:** The tooltip produces "Remove null" (not "Remove undefined") when `tag.title` is explicitly null, and "Remove undefined" when the field is absent. Both are wrong. The fix `tag.title ?? String(tag.id)` is correct. The reporter incorrectly states the symptom as always 'undefined' — it depends on whether `title` is null vs missing. The i18n suggestion is a bonus improvement, not required to fix the defect.

### uploadRequest() unconditionally redirects on 401, missing publicProbes allowlist present in request()
- **Severity:** low · **Category:** bug · **Subsystem:** fe-pages-lib · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/lib/api.ts:291-294`
- **Evidence:** if (!res.ok) {   if (res.status === 401) {     redirectOnSessionExpiry()   }
- **Suggested fix:** Extract the publicProbes constant to module scope and apply the same guard used in request(): `if (res.status === 401 && !publicProbes.includes(path)) { redirectOnSessionExpiry() }`. The path parameter is already available in uploadRequest's closure so no signature change is needed.
- **Verifier:** The finding is confirmed by direct code inspection.  In `request()` (lines 159-185), the 401 guard is: ``` const publicProbes = ['/api/auth/user', '/api/preferences', '/api/navigation'] if (res.status === 401 && !publicProbes.includes(path)) {   redirectOnSessionExpiry() } ``` The allowlist is decla …
- **Verifier fix note:** The suggested fix is correct in principle -- extract `publicProbes` to module scope and apply the same guard in `uploadRequest()`. However, the claimed severity of "medium" is too high. Because `api.upload()` is only ever called for multipart file-field submissions (guarded by `hasFileValues()`), none of the three public probe paths (`/api/auth/user`, `/api/preferences`, `/api/navigation`) can reach `uploadRequest()` in any current call path. The inconsistency is a future-proofing / code hygiene issue rather than a present-day bug with observable consequences. Adjust to low severity.

### CacheAdmin uses native window.confirm instead of PrimeReact confirmation dialog
- **Severity:** low · **Category:** convention · **Subsystem:** fe-pages-lib · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/pages/CacheAdmin.tsx:85, 92`
- **Evidence:** if (!window.confirm(message)) return ... if (!window.confirm(t('cache_clear_all_confirm', 'Clear every Martis cache?'))) return
- **Suggested fix:** Replace both window.confirm calls with PrimeReact's confirmDialog() from primereact/confirmdialog. Add a single <ConfirmDialog /> mount to the component tree and convert the two callbacks to use the accept/reject pattern. This aligns with the design-system convention used everywhere else in the admin panel.
- **Verifier:** Both window.confirm calls are confirmed at exactly the cited lines (85 and 92) in /Users/lmoura/projects/martis/martis-package/resources/js/pages/CacheAdmin.tsx. CacheAdmin.tsx is the ONLY file in the entire resources/js tree that uses window.confirm. The project's established pattern for destructiv …
- **Verifier fix note:** The fix direction is correct (replace with a React modal) but the specific API is wrong. PrimeReact confirmDialog() is not used anywhere in this codebase. The correct fix is to follow the existing pattern: create a small inline ConfirmModal (or reuse DeleteModal with a custom confirmMessage prop) that renders via React portal, uses i18n keys, and accepts onConfirm/onCancel callbacks — matching DeleteModal.tsx and the other custom modal components in resources/js/components/.

### Hardcoded English fallback strings in buildOverrideProps bypass i18n system
- **Severity:** low · **Category:** convention · **Subsystem:** fe-pages-lib · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/resources/js/pages/ResourceIndex.tsx:459, 465, 471`
- **Evidence:** addToast('success', schema!.messages?.created ?? 'Record created successfully.') addToast('success', schema!.messages?.updated ?? 'Record updated successfully.') addToast('success', schema!.messages?.deleted ?? 'Record deleted successfully.')
- **Suggested fix:** Replace each hardcoded English fallback with a tMsg() call using the same translation keys already used elsewhere in these files (record_created, record_updated, record_deleted). The same pattern repeats in ResourceDetail.tsx (lines 152, 158, 164, 199, 439, 446, 452, 480), ResourceCreate.tsx (lines 380, 386, 392), and ResourceUpdate.tsx (lines 314, 321, 327).
- **Verifier:** The cited code at ResourceIndex.tsx lines 459, 465, and 471 was read directly. The three `addToast` calls inside `buildOverrideProps` use `schema!.messages?.created ?? 'Record created successfully.'` (and the updated/deleted analogues) as their final fallback. The `messages` property on the resource …
- **Verifier fix note:** The suggested fix (replace each hardcoded string with `tMsg('record_created')`, `tMsg('record_updated')`, `tMsg('record_deleted')`) is correct. No additional changes are needed: the keys already exist in all locales and tMsg is already in scope. The same fix applies to the analogous lines in ResourceDetail.tsx, ResourceCreate.tsx, and ResourceUpdate.tsx cited by the reporter, but those were not part of this adversarial check.

### buildRules() emits duplicate 'required' when rules(['required',...]) is combined with auto-detection
- **Severity:** low · **Category:** bug · **Subsystem:** fields-base-textlike · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Field.php:1388-1429`
- **Evidence:** if ($this->isRequired()) {     $rules[] = 'required'; } ... $merged = array_values(array_merge($rules, $extraRules, $contextRules));
- **Suggested fix:** The isRequired() auto-detection (rulesHaveRequired()) causes 'required' to be prepended AND the literal 'required' from $extraRules to also be present in the merged output. Fix: after building $merged, deduplicate the string rules: $merged = array_values(array_unique($merged)); — or, preferably, skip adding 'required' at line 1393 when it is already in $extraRules: if ($this->isRequired() && !in_a …
- **Verifier:** The defect is real and confirmed by direct code inspection.  Execution path: when a developer writes `Text::make('email')->rules(['required', 'email'])`, the following happens: - `$this->extraRules` = `['required', 'email']` - `isRequired()` falls through to `rulesHaveRequired()` (Field.php:607), wh …
- **Verifier fix note:** The cleaner fix is the guard approach proposed by the reporter: at Field.php line 1392, change `if ($this->isRequired())` to `if ($this->isRequired() && !in_array('required', $this->extraRules, true))`. This avoids emitting the duplicate at the source. The `array_unique` alternative (on `$merged` after the merge) would also work but could silently collapse distinct `Rule` object instances that happen to stringify identically, making it riskier as a blanket deduplicate. The guard on `$extraRules` is precise and surgical. Note: the same guard does NOT need to check `$this->rulesResolver` output because that path (line 1415-1419) overwrites `$extraRules` with the closure result, meaning the closure-returned `'required'` string is still deduplicated if the guard checks `$extraRules` after the resolver runs. To cover both paths, the guard should be applied after line 1420 (after the resolver is resolved), or checked against both `$this->extraRules` and the resolved `$extraRules`.

### rulesHaveRequired() has a tautological condition — the second OR branch is dead code
- **Severity:** low · **Category:** bug · **Subsystem:** fields-base-textlike · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Field.php:626`
- **Evidence:** if (is_string($rule) && (str_starts_with($rule, 'required') || $rule === 'required')) {
- **Suggested fix:** str_starts_with($rule, 'required') is true for every string that starts with 'required', including 'required' itself — the subsequent || $rule === 'required' is therefore never reachable. Simplify to: if (is_string($rule) && str_starts_with($rule, 'required')) { — no behaviour change, but the dead branch signals a possible past intention to also check object rules with string representation, which …
- **Verifier:** Line 626 of /Users/lmoura/projects/martis/martis-package/src/Fields/Field.php reads: `if (is_string($rule) && (str_starts_with($rule, 'required') || $rule === 'required'))`. The second disjunct `$rule === 'required'` is unreachable: `str_starts_with('required', 'required')` evaluates to `true` in PH …
- **Verifier fix note:** The suggested fix is correct and safe: replace the condition with `is_string($rule) && str_starts_with($rule, 'required')`. No behavior change. The dead branch should not be misread as evidence of a missing object-rule path — that case is already handled by the `is_object()` branch immediately below.

### Image::getThumbnailPath produces a trailing dot when the stored path has no extension
- **Severity:** low · **Category:** bug · **Subsystem:** fields-media · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Image.php:412-424`
- **Evidence:** protected function getThumbnailPath(string $path): ?string {     // ...     $ext = pathinfo($path, PATHINFO_EXTENSION);  // '' when no extension     // ...     return $prefix.$filename.'_thumb.'.$ext;  // 'uploads/photo_thumb.' when ext='' }
- **Suggested fix:** Guard the extension concatenation: `return $prefix.$filename.'_thumb'.($ext !== '' ? '.'.$ext : '');`. This mirrors the same pattern already used in `generateStorageFilename` (File.php line 228). While Image validation enforces known image extensions making this unreachable in the normal flow, a model record with a legacy or manually-set path value without an extension would produce a trailing-dot …
- **Verifier:** The code at Image.php:423 exactly matches the report: `return $prefix.$filename.'_thumb.'.$ext;` with no guard when `$ext === ''`. The parallel guard already present in File.php:228 (`($ext ? '.'.$ext : '')`) confirms this was missed here.  However, the practical impact is significantly lower than t …
- **Verifier fix note:** The fix is valid: guard the concatenation as `$prefix.$filename.'_thumb'.($ext !== '' ? '.'.$ext : '')` to match File.php:228. That said, severity stays at low rather than invalid because the bug is a genuine code inconsistency — the absence of the guard is a latent defect even though all current callers neutralise it via `$disk->exists()`. A future caller that skips the exists-check (e.g. a direct thumbnail-URL assertion in a test or a new resolver variant) would expose the trailing dot. The fix costs one line and closes the gap.

### Image::generateThumbnailWithGd has no case for BMP even though 'bmp' is in acceptedTypes
- **Severity:** low · **Category:** bug · **Subsystem:** fields-media · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Image.php:448-454`
- **Evidence:** $src = match ($mime) {     'image/jpeg' => @imagecreatefromjpeg($sourcePath),     'image/png'  => @imagecreatefrompng($sourcePath),     'image/gif'  => @imagecreatefromgif($sourcePath),     'image/webp' => @imagecreatefromwebp($sourcePath),     default      => false,  // BMP ('image/bmp' / 'image/x-bmp') falls here };
- **Suggested fix:** Add a BMP case to both the source-load match and the output match: `'image/bmp', 'image/x-bmp' => @imagecreatefrombmp($sourcePath)` (available since PHP 7.2). Also add `'image/bmp', 'image/x-bmp' => imagebmp($thumb)` to the output match. Without this, uploading a BMP image when `thumbnail()` is configured silently skips thumbnail generation — the GD match returns `false`, the `$src === false` guar …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/src/Fields/Image.php line 67 confirms `'bmp'` is in the default `acceptedTypes`. The `generateThumbnailWithGd` method (lines 448-454) loads the source image with a match on MIME type that covers jpeg/png/gif/webp but has no case for 'image/bmp …
- **Verifier fix note:** The fix is correct as described: add `'image/bmp', 'image/x-bmp' => @imagecreatefrombmp($sourcePath)` to the source-load match, and add a corresponding `'image/bmp', 'image/x-bmp' => imagebmp($thumb)` case to the output match. Note that `imagecreatefrombmp()` was introduced in PHP 7.2, so it is safe for the PHP 8.2+ requirement. It is also worth adding a transparency/alpha preservation block for BMP analogous to the existing PNG block if alpha-channel BMPs need to be supported, though standard BMP files do not carry transparency.

### isShowCreateRelationButton() uses raw request() instead of safeRequest() in BelongsTo, BelongsToMany, MorphTo
- **Severity:** low · **Category:** implementation · **Subsystem:** fields-relation · **Confidence:** medium
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/BelongsTo.php:580`
- **Evidence:** $declared = $this->showCreateRelationButton instanceof \Closure     ? (bool) ($this->showCreateRelationButton)(request())     : $this->showCreateRelationButton;
- **Suggested fix:** Replace request() with $this->safeRequest() in isShowCreateRelationButton() in BelongsTo (line 580), BelongsToMany (line 360), and MorphTo (line 226). This method is called from extraAttributes() -> toArray(), so if a non-HTTP context (artisan command, queue job, test without HTTP bootstrap) ever serializes a field with a Closure-based showCreateRelationButton, it will throw rather than returning  …
- **Verifier:** The code at BelongsTo.php:580, BelongsToMany.php:360, and MorphTo.php:226 each call the bare `request()` Laravel global inside the Closure branch of `isShowCreateRelationButton()`. The parent class `Field` defines `safeRequest()` at Field.php:456, which wraps `app('request')` in a try/catch Throwabl …

### HasMany::resolve and MorphMany::resolve call relationship method without method_exists guard
- **Severity:** low · **Category:** bug · **Subsystem:** fields-relation · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/HasMany.php:354`
- **Evidence:** if ($this->showOnIndex) {     return $model->{$this->relationship}()->count(); }
- **Suggested fix:** Add a method_exists guard identical to the one BelongsToMany::resolve uses on lines 394-396: if ($this->showOnIndex) { $method = $this->relationship; if (method_exists($model, $method)) { return $model->{$method}()->count(); } }. Apply the same fix to MorphMany::resolve (line 343 in MorphMany.php). Without the guard a misconfigured relationship or a wrong model class causes a BadMethodCallExceptio …
- **Verifier:** The finding is confirmed by direct code inspection.  HasMany::resolve at line 353-355 of /Users/lmoura/projects/martis/martis-package/src/Fields/HasMany.php:   if ($this->showOnIndex) {       return $model->{$this->relationship}()->count();   } No method_exists guard is present.  MorphMany::resolve  …
- **Verifier fix note:** The suggested fix (add method_exists guard identical to BelongsToMany::resolve lines 394-396) is correct and should be applied to both HasMany::resolve and MorphMany::resolve. validateRelationship already contains the right check but is never called from the production pipeline — it is test-only dead code with respect to runtime protection. Consider whether validateRelationship should be wired into a resource boot/registration step for early-fail behaviour instead of relying on a silent null fallback at resolve time.

### MorphMany::resolve calls relationship method without method_exists guard
- **Severity:** low · **Category:** bug · **Subsystem:** fields-relation · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/MorphMany.php:342-344`
- **Evidence:** if ($this->showOnIndex) {     return $model->{$this->relationship}()->count(); }
- **Suggested fix:** Parallel fix to HasMany: wrap the call with method_exists($model, $this->relationship) before invoking the dynamic method. BelongsToMany (lines 394-396) already demonstrates the correct guard pattern.
- **Verifier:** The cited code is real. MorphMany::resolve at lines 342-344 calls $model->{$this->relationship}()->count() with no method_exists guard. If the relationship method is absent on the model, PHP throws a fatal BadMethodCallException at runtime.  The default constructor path calls $this->onlyOnDetail() a …
- **Verifier fix note:** The suggested fix (wrap with method_exists before invoking) is correct and should be applied to both MorphMany::resolve and HasMany::resolve for consistency - HasMany has the same gap and was incorrectly described as already fixed. BelongsToMany::resolve lines 394-397 is the reference implementation to follow.

### Tag::extraAttributes silently drops relationSearchable: false
- **Severity:** low · **Category:** bug · **Subsystem:** fields-relation · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Tag.php:312-322`
- **Evidence:** return array_filter([     ...     'relationSearchable' => $this->relationSearchable, ], fn (mixed $v): bool => $v !== null && $v !== false && $v !== '');
- **Suggested fix:** The filter predicate removes any value that is strictly false. For boolean UI flags like relationSearchable this is wrong when the developer explicitly calls ->relationSearchable(false) — the key must still reach the frontend so the TSX can disable search. Either remove relationSearchable from the filtered set (always include it) or narrow the filter to only strip null/empty-string: fn ($v) => $v  …
- **Verifier:** The PHP-side data-contract defect is real and confirmed at Tag.php:322. The array_filter predicate `fn ($v) => $v !== null && $v !== false && $v !== ''` strips `relationSearchable` from the serialized payload whenever the developer calls `->relationSearchable(false)`, because `false` is literally on …
- **Verifier fix note:** The fix is still warranted before any TSX wiring lands. The predicate at Tag.php:322 should be narrowed to `fn ($v) => $v !== null && $v !== ''` so that explicit `false` values for boolean flags survive serialization. Alternatively, pull `relationSearchable` outside the filtered set and always include it (as MorphTo.php does at line 470). The reporter's secondary note about `withPreview`, `displayAsList`, and `showCreateRelationButton` also being stripped when false is correct but harmless in the same way — their TSX consumers branch on truthiness, and `false` from the server is equivalent to the key being absent (both evaluate to falsy). Only `relationSearchable` has the true-by-default / false-by-explicit-call semantic that makes the omission meaningful once the TSX is wired.

### Country::extraAttributes() sends the full 195-entry country list on every field serialization, with no caching
- **Severity:** low · **Category:** perf · **Subsystem:** fields-special · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Fields/Country.php:311-318`
- **Evidence:** protected function extraAttributes(): array {     return array_filter([         'countries' => self::countryList(),         'showFlags' => $this->showFlags,         'filterPlaceholder' => $this->filterPlaceholder,     ], fn ($v) => $v !== null); }
- **Suggested fix:** Cache the country list in a static property so it is constructed only once per process: `protected static ?array $cachedCountryList = null;` and in `countryList()` add `if (self::$cachedCountryList !== null) { return self::$cachedCountryList; }` before the array literal. Additionally, for pages with multiple Country fields the payload bloat is multiplied — consider moving the list to a dedicated e …
- **Verifier:** Confirmed by direct code inspection. `Country::countryList()` at line 78 returns a hardcoded 197-entry array literal with no static property, no memoization, and no caching of any kind — `grep -n "static"` on the file returns only method signatures, never a static property. `extraAttributes()` at li …
- **Verifier fix note:** The static-property memoization suggested (`protected static ?array $cachedCountryList = null`) is the right fix for the allocation cost within a single process. However, the secondary suggestion — "move the list to a dedicated endpoint or ship it only on forms" — would require a contract change between the PHP serializer and the TSX component (the frontend currently reads `field.countries` from the schema payload). That is a larger refactor worth a separate proposal, not bundled with the caching fix. Also note that `resolveCountryName()` (line 285) and `resolveCountryFlag()` (line 299) each call `self::countryList()` in a linear scan; memoizing the list helps those too, but a keyed lookup map (`array_column(..., null, 'value')`) would reduce their O(n) scan to O(1) at the same time.

### 2FA bypass: SSO callback and magic-link consume never set martis_two_factor_passed — a user with 2FA enabled is logged in fully without a challenge
- **Severity:** low · **Category:** security · **Subsystem:** http-auth-sso-ctrl · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/SsoController.php:119-131`
- **Evidence:** // SsoController.php lines 119-131 — no martis_two_factor_passed write: Auth::guard($guard)->login($user, true); $request->session()->put('martis_sso_provider', $provider); return redirect()->intended($redirectTo);  // MagicLinkController.php lines 106-112 — same gap: Auth::guard($guard)->login($user); $request->session()->regenerate(); return redirect($home);  // EnsureTwoFactorChallenge::requiresChallenge() lets them through because // session()->get('martis_two_factor_passed') returns null, a …
- **Suggested fix:** After `Auth::guard()->login($user)` in both SsoController::callback() and MagicLinkController::consume(), check whether the user has 2FA enabled and set the flag accordingly: ```php if (app(TwoFactorService::class)->isEnabled($user)) {     $request->session()->put('martis_two_factor_passed', false); } else {     $request->session()->put('martis_two_factor_passed', true); } ``` This mirrors exactly …
- **Verifier:** The finding is real, but the claimed severity (critical — full 2FA bypass) is overstated. Here is what the code actually shows:  SsoController.php lines 119-131 and MagicLinkController.php lines 106-112: confirmed — neither sets `martis_two_factor_passed` in the session after `Auth::guard()->login() …
- **Verifier fix note:** The fix is still correct — both SsoController::callback() and MagicLinkController::consume() should write `martis_two_factor_passed` (false when 2FA is enabled, true otherwise) after login, mirroring AuthController::login() lines 130-131. This removes the strict/truthy asymmetry and gives AuthController::user() the `=== false` value it needs to return `two_factor_pending: true` on SPA bootstrap. The security impact is limited to a momentary partial-render race, not a bypass, so severity is low rather than critical.

### TwoFactorService::hasLastUsedColumn() uses a process-level static cache — incorrect result when tests switch user models or when two different user tables are used in the same process
- **Severity:** low · **Category:** bug · **Subsystem:** http-auth-sso-ctrl · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Profile/TwoFactorService.php:306-321`
- **Evidence:** private function hasLastUsedColumn(Model $user): bool {     static $cached = null;     if ($cached !== null) {         return $cached;     }      try {         $cached = Schema::connection($user->getConnectionName())             ->hasColumn($user->getTable(), 'two_factor_last_used_at');     } catch (Throwable) {         $cached = false;     }      return $cached; }
- **Suggested fix:** Cache by connection+table key rather than a single boolean: ```php static $cache = []; $key = $user->getConnectionName().':'.$user->getTable(); if (array_key_exists($key, $cache)) {     return $cache[$key]; } try {     $cache[$key] = Schema::connection($user->getConnectionName())         ->hasColumn($user->getTable(), 'two_factor_last_used_at'); } catch (Throwable) {     $cache[$key] = false; } re …
- **Verifier:** The code exactly matches the quoted evidence. At /Users/lmoura/projects/martis/martis-package/src/Profile/TwoFactorService.php:306-321, `hasLastUsedColumn()` uses `static $cached = null` — a single PHP process-level boolean that is written once on first call and never keyed by connection or table. T …
- **Verifier fix note:** The suggested fix (keying the static cache by `$user->getConnectionName().':'.$user->getTable()`) is correct and worthwhile as a defensive improvement. However, the additional suggestion to expose a `resetCacheForTesting()` method is only needed if tests ever exercise a confirmed-user valid-OTP path with different table schemas in the same process. The current test suite does not reach `hasLastUsedColumn` at all (verifyForUser returns early for unconfirmed users), so no test harness change is immediately required. Apply the key-based cache fix; defer the testing helper until a test actually exercises this path.

### NotificationController.markRead() passes potentially-null result of fresh() to a non-nullable typed parameter
- **Severity:** low · **Category:** bug · **Subsystem:** http-misc · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/NotificationController.php:115`
- **Evidence:** // Line 108-115: $notification = $user->notifications()->where('id', $id)->first(); if (! $notification instanceof DatabaseNotification) {     return JsonErrorResponse::notFound()->toResponse(); } $notification->markAsRead(); return new IlluminateJsonResponse(['data' => $this->serialize($notification->fresh())]);  // serialize() is declared as: protected function serialize(DatabaseNotification $notification): array
- **Suggested fix:** Either re-read from the database with a fresh query instead of calling fresh(), or add a null guard: `$fresh = $notification->fresh(); if ($fresh === null) { return new IlluminateJsonResponse(['data' => $this->serialize($notification)]); } return new IlluminateJsonResponse(['data' => $this->serialize($fresh)]);`. The race condition is rare (a concurrent delete between markAsRead and fresh()) but t …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/src/Http/Controllers/NotificationController.php line 115 is exactly as described. After `$notification->markAsRead()` on line 113, the code immediately calls `$notification->fresh()` and passes the result directly to `serialize(DatabaseNotific …
- **Verifier fix note:** The simplest safe fix is to skip the re-fetch entirely and pass the already-loaded `$notification` directly: `return new IlluminateJsonResponse(['data' => $this->serialize($notification)]);`. The `read_at` field will still be set correctly because `markAsRead()` updates the model's attribute in memory as well as persisting it. Alternatively, if the fresh DB state is strictly required, add a null guard: `$fresh = $notification->fresh(); return new IlluminateJsonResponse(['data' => $this->serialize($fresh ?? $notification)]);`. Using the in-memory model is preferable as it eliminates the extra query and the race entirely.

### handleDatabaseError in BelongsToManyController/MorphToManyController uses MySQL-specific error codes only
- **Severity:** low · **Category:** implementation · **Subsystem:** http-relationships · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/BelongsToManyController.php:797-807`
- **Evidence:** $message = match ($code) {     '1062' => 'A record with this value already exists.',     '1451' => 'This record is referenced by other records.',     '1452' => 'The referenced record does not exist.',     default => 'A database error occurred.', };
- **Suggested fix:** The HasMany/MorphMany/HasOne/MorphOne handleDatabaseError methods also include error codes 1048 and 1364. The BelongsToMany and MorphToMany versions omit them. More importantly, all six controllers use MySQL-specific numeric error codes (errorInfo[1]). On PostgreSQL the relevant codes are SQLSTATE strings like '23505' (unique violation) and '23503' (FK violation). On SQLite they are different agai …
- **Verifier:** The code confirms both sub-claims. At BelongsToManyController.php:797 and MorphToManyController.php:719, handleDatabaseError reads `$e->errorInfo[1]` (the MySQL driver-specific vendor code) and matches only '1062', '1451', '1452'. The four other relationship controllers (HasManyController:591, Morph …
- **Verifier fix note:** The fix has two independent parts. First, add '1048' and '1364' arms to BelongsToManyController::handleDatabaseError and MorphToManyController::handleDatabaseError to match the other four controllers (trivial copy). Second, for cross-driver portability across all six controllers, branch on $e->errorInfo[0] (SQLSTATE) alongside $e->errorInfo[1]: PostgreSQL unique violation is SQLSTATE '23505', FK parent missing is '23503', NOT NULL is '23502'; SQLite unique is '23000'. A shared trait or base-class method would eliminate the duplication across all six identical private implementations.

### MorphToMany attachableIndex: relatableQueryUsing closure receives no form-draft (feature gap vs BelongsToMany)
- **Severity:** low · **Category:** implementation · **Subsystem:** http-relationships · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/MorphToManyController.php:162-165`
- **Evidence:** // Apply relatableQueryUsing closure $relatableClosure = $field->getRelatableQueryClosure(); if ($relatableClosure !== null) {     $relatableClosure($request, $query); }
- **Suggested fix:** BelongsToManyController passes a third `$formDraft` argument when the closure arity is >= 3 (v1.8.2 feature). MorphToManyController always calls the closure with 2 arguments, silently ignoring any arity-3 closure. Developers who move a BelongsToMany field to MorphToMany will get a silent regression. Apply the same ReflectionFunction arity check and form-draft collection logic used in BelongsToMany …
- **Verifier:** The code confirms the discrepancy exactly as described. In /Users/lmoura/projects/martis/martis-package/src/Http/Controllers/MorphToManyController.php lines 161-165, the relatable closure is called with exactly two arguments ($request, $query) and there is no arity check and no form-draft collection …

### Inline-create depth guard is permanently inert — X-Martis-Inline-Create-Depth is never sent by the frontend
- **Severity:** low · **Category:** bug · **Subsystem:** http-resource · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ResourceController.php:808`
- **Evidence:** if ($request->header('X-Martis-Inline-Create-Depth', '0') !== '0') {     return JsonErrorResponse::validation([], 'Inline create only supports one level of depth.')->toResponse(); }
- **Suggested fix:** The frontend InlineCreateModal.tsx never sends X-Martis-Inline-Create-Depth (verified by grep over all resources/js). The header default of '0' means the condition is always false and the depth guard never fires. Either (a) make the frontend send the header with an incrementing depth counter, or (b) remove the dead guard and enforce depth at the schema layer by already stripping showCreateRelation …
- **Verifier:** The header `X-Martis-Inline-Create-Depth` is confirmed to be absent from all frontend code. A full grep over `resources/js/` returns zero hits for the string, while the only non-controller reference is in a test file (`ReplicateInlineCreateTest.php:202`) that manually injects the header to exercise  …
- **Verifier fix note:** The schema layer at `inlineCreateSchema` (ResourceController.php lines 669-676) already enforces depth by stripping `showCreateRelationButton` from `belongs_to`/`morph_to` fields before the schema is returned to the frontend. `BelongsToField.tsx` gates both the button and the `InlineCreateModal` on `showCreateRelationButton === true`, so the nested create UI is never available inside an inline create modal. The server-side header guard is dead code but the depth constraint itself is met. The cleanest fix is option (b) from the suggestion: remove the dead header guard from `inlineCreateStore` and add a code comment to `inlineCreateSchema` documenting that stripping `showCreateRelationButton` is the intentional depth enforcement point. No frontend changes are needed.

### DocLookup::search() with limit=0 returns 1 result instead of 0
- **Severity:** low · **Category:** bug · **Subsystem:** mcp · **Confidence:** high
- **Location:** `src/Mcp/DocLookup.php:80`
- **Evidence:** return array_slice($rows, 0, max(1, $limit));
- **Suggested fix:** max(1, $limit) prevents returning an empty result set, so callers passing limit=0 to request 'no results' (e.g. for testing whether a query matches at all) will unexpectedly receive one result. If the intent is to prevent the tool from being called with a nonsensical limit, validate at the boundary instead: `if ($limit <= 0) { $limit = 5; }` or throw/return [] for limit=0. The current safeguard is …
- **Verifier:** The code at DocLookup.php:80 does contain `array_slice($rows, 0, max(1, $limit))`, confirming the literal claim: passing `limit=0` returns up to 1 result instead of 0. However, the severity is overstated. The only caller is `Tools::searchDocs()` (Tools.php:88), which itself is only ever reached via  …
- **Verifier fix note:** The reported fix ("return [] for limit=0") would silently discard results for any misconfigured agent call rather than returning a safe fallback. A better approach, if hardening is desired, is to validate at the boundary in `Tools::searchDocs()` with a clamp (e.g. `$limit = max(1, min(50, $limit))`) and document that 0 is not a valid input. The `max(1, $limit)` guard in `DocLookup::search()` can remain as a last-resort defensive clamp, or be replaced by an early return of [] for limit<=0 if the caller-side validation is added. The inconsistency with the @param default is real but trivial.

### Unsanitized user-supplied slug interpolated into error response in Tools::readDoc
- **Severity:** low · **Category:** security · **Subsystem:** mcp · **Confidence:** high
- **Location:** `src/Mcp/Tools.php:59`
- **Evidence:** 'error' => "No documentation page found for slug `{$slug}`. Call martis_doc_list to see available slugs."
- **Suggested fix:** DocLookup::read() validates the slug through safeSlug() and returns null for any slug that fails the regex. However, Tools::readDoc() passes the original unvalidated $slug variable to $this->docs->read(), and then interpolates that same original $slug (not the validated result) into the error JSON when read() returns null. A caller supplying a slug with backticks, Unicode surrogates, or null bytes …
- **Verifier:** Confirmed by reading both files. DocLookup::read() calls $this->safeSlug($slug) at line 41 and reassigns to a local $slug, so the sanitized value never propagates back to the caller. When safeSlug() rejects the input it returns null, read() returns null, and Tools::readDoc() at line 59 interpolates  …
- **Verifier fix note:** The suggested fix is correct. The simplest safe approach: add $safe = preg_replace('/[^A-Za-z0-9._-]/', '', $slug) before the null check and use $safe in the error string. Alternatively make safeSlug() package-internal (change private to internal/public) and reuse it — but that changes the DocLookup API surface. The preg_replace approach is self-contained in Tools::readDoc() and requires no API change.

### HealthServer binds on the same public host without any authentication
- **Severity:** low · **Category:** security · **Subsystem:** mcp · **Confidence:** high
- **Location:** `src/Mcp/Transport/HealthServer.php:54, 79-93`
- **Evidence:** public function start(): void {     $this->socket = new SocketServer("{$this->host}:{$this->port}", [], $this->loop);     ... }  private function handle(ServerRequestInterface $request): Response {     ...     return new Response(200, ['Content-Type' => 'application/json'], (string) json_encode($payload)); }
- **Suggested fix:** HealthServer inherits its host from McpServeCommand::maybeStartHealthServer(), which copies the MCP host (line 136 of McpServeCommand.php). When --host=0.0.0.0 is used, the health endpoint is bound publicly with no auth and no CORS restriction, disclosing version, transport, uptime, and operational state to any peer on the network. Two mitigations: (1) HealthServer should accept an optional bearer …
- **Verifier:** Both cited files were read and the claim is confirmed by code.  HealthServer.php:54 creates a SocketServer on `{$this->host}:{$this->port}` with no authentication layer. The handle() method at lines 66-94 returns the full payload (version, transport, uptime_s, tool_count, status) to any caller that  …
- **Verifier fix note:** The primary gap is in maybeWarnOnPublic(), not in HealthServer itself. The warning should fire when the health port is active and host is 0.0.0.0, independently of whether the MCP token is configured — because setting a token silences the current warning while leaving the health endpoint exposed. A secondary improvement would be passing an optional token to HealthServer and applying the same bearer-token check as AuthenticatedStreamableHttpTransport. Given the low sensitivity of the exposed data (version, uptime, tool count), adding a configurable token parameter (defaulting to the MCP token) and patching the warning condition are sufficient; mandatory auth is not warranted for an opt-in diagnostic endpoint.

### HasPolicy static cache uses self:: in trait body — flushPolicyCache() on one consuming class does not flush caches for other consumers
- **Severity:** low · **Category:** bug · **Subsystem:** menu-layout-dashboards · **Confidence:** medium
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Concerns/HasPolicy.php:37, 61-88, 97-100`
- **Evidence:** protected static array $resolvedHasPolicyPolicies = []; ... if (array_key_exists($key, self::$resolvedHasPolicyPolicies)) {     $cached = self::$resolvedHasPolicyPolicies[$key]; ... self::$resolvedHasPolicyPolicies[$key] = $instance; ... public static function flushPolicyCache(): void {     self::$resolvedHasPolicyPolicies = []; }
- **Suggested fix:** In PHP, a static property declared in a trait is copied per consuming class. self:: inside a trait method resolves to the class that inlined the trait at compile time. Because each consumer (Dashboard, Tool, Resource) gets its own copy of $resolvedHasPolicyPolicies, calling Dashboard::flushPolicyCache() only clears the Dashboard copy. Tests that call flushPolicyCache() on one class and then test a …
- **Verifier:** The PHP mechanics are confirmed real. Live PHP execution proves that `self::` inside a trait method binds to the per-consuming-class copy of a static property: flushing `A::flush()` leaves `B`'s cache intact. `HasPolicy` is used by `Dashboard` (src/Dashboards/Dashboard.php:28) and `Tool` (src/Tools/ …
- **Verifier fix note:** The fix scope is narrower than the reporter stated. Resource is unaffected (it owns its own cache independently). Only Dashboard and Tool share the trait. A shared flush helper such as a static `HasPolicy::flushAll()` that maintains a registry of consumers is one option, but the simpler and more practical fix for tests is a base test helper that calls both `Dashboard::flushPolicyCache()` and `Tool::flushPolicyCache()` together whenever either is needed. In production no fix is required since PHP-FPM resets static state per request.

### MenuItem::resolveDashboardItem does not include the dashboard icon in the serialized payload when icon() returns a value
- **Severity:** low · **Category:** implementation · **Subsystem:** menu-layout-dashboards · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Menu/MenuItem.php:324-342`
- **Evidence:** protected function resolveDashboardItem(Request $request): ?array {     ...     return array_merge([         ...         // Dashboard does not expose icon() in the contract — only override         // when the consumer explicitly set one on the menu item.         'icon' => $this->icon,         ...     ], $this->meta); }
- **Suggested fix:** Dashboard::icon() was added in v1.11.4 and is now part of the concrete class (not the DashboardContract interface). resolveDashboardItem() deliberately falls back only to $this->icon (the MenuItem-level override) and ignores $dashboard->icon(). The comment says 'Dashboard does not expose icon() in the contract' — but the method exists on the concrete class. The Sidebar uses the dashboard icon corr …
- **Verifier:** Code confirms the defect. In resolveDashboardItem (MenuItem.php:324-342), the variable $dashboard passes an `instanceof Dashboard` guard at line 328, so it is guaranteed to be the concrete Dashboard class. Dashboard::icon() exists at Dashboard.php:159-162 and is not part of DashboardContract, but th …
- **Verifier fix note:** The suggested fix using `method_exists($dashboard, 'icon')` is unnecessarily defensive since the instanceof Dashboard guard already guarantees the method exists. A cleaner fix is `'icon' => $this->icon ?? $dashboard->icon()` directly at line 338 — no method_exists check needed. This exactly mirrors the pattern in resolveToolItem at line 314.

### ProgressResult percentage calculation treats negative target as zero — silently renders 0% instead of an error or correct percentage
- **Severity:** low · **Category:** bug · **Subsystem:** metrics-rest · **Confidence:** medium
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Metrics/ProgressResult.php:68`
- **Evidence:** $percentage = $this->target > 0     ? round(($this->current / $this->target) * 100, 1)     : 0;
- **Suggested fix:** The guard `$this->target > 0` is correct for the division-by-zero case but also silently returns 0% when target is negative (e.g. a metric tracking deficit reduction toward -100). The practical risk is low (negative targets are unusual) but could confuse developers. Consider throwing an InvalidArgumentException in the constructor when target < 0, or at minimum documenting that target must be a pos …
- **Verifier:** The cited code is confirmed exactly as reported. Line 68 of ProgressResult.php reads `$percentage = $this->target > 0 ? round(($this->current / $this->target) * 100, 1) : 0;`. The constructor (lines 20-24) accepts any `float|int` for `$target` with no validation guard, so a negative value passes thr …
- **Verifier fix note:** The guard `$this->target > 0` correctly prevents division by zero for target=0, but it overloads that protection to also swallow negative targets silently. The cleanest fix is a constructor guard: `if ($target < 0) { throw new \InvalidArgumentException('ProgressResult target must be >= 0.'); }` This makes the contract explicit and gives developers an immediate, actionable error instead of a confusing 0% in production. If negative targets are ever a desired use-case (e.g. deficit-reduction metrics), the percentage formula itself would need to change (use abs($target) or a signed-ratio approach), which is a separate design decision the team should make explicitly rather than inheriting the current silent behaviour.

### TrendMetric and PartitionMetric interpolate column names directly into DB::raw without quoting — SQL injection and reserved-word breakage **[FIXED]**
- **Severity:** low · **Category:** security · **Subsystem:** metrics-rest · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Metrics/TrendMetric.php:148-153`
- **Evidence:** $expression = $function === AggregateFunction::Count     ? DB::raw('count(*) as aggregate')     : DB::raw("{$function->value}({$column}) as aggregate"); ... ->select(DB::raw("DATE_FORMAT({$dateColumn}, '{$dateFormat}') as date_key"), $expression)
- **Suggested fix:** The $column and $dateColumn values come from method callers (i.e. package consumers writing their own TrendMetric subclasses). They are not parameterized: DB::raw("{$function->value}({$column}) as aggregate") interpolates the column name into the SQL string. While the attack vector requires the developer to pass an externally-controlled string (unlikely but not impossible with dynamic column selec …
- **Verifier:** The code at TrendMetric.php:146-153 and PartitionMetric.php:80-82 does interpolate column names directly into DB::raw() strings without quoting. However, the severity claimed ("high / SQL injection") is overstated for two reasons:  1. No user-controlled input reaches $column or $dateColumn. Both val …
- **Verifier fix note:** The fix direction is correct: wrap $column and $dateColumn with DB::getQueryGrammar()->wrap() before interpolating into DB::raw(). The same applies to PartitionMetric::aggregate() line 82. However this is a developer-ergonomics/portability fix, not a security patch. The $groupBy column in PartitionMetric line 86 is passed directly to the Eloquent builder's ->select() and ->groupBy() (not inside DB::raw()), so the query grammar wraps it automatically — no fix needed there.

### TrendResult::toArray latestValue uses end() ?: 0 which coerces float 0.0 and a legitimate last-value of false to int 0
- **Severity:** low · **Category:** bug · **Subsystem:** metrics-rest · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Metrics/TrendResult.php:111`
- **Evidence:** $data['latestValue'] = end($this->values) ?: 0;
- **Suggested fix:** end() returns false for an empty array but also returns 0 (int) or 0.0 (float) for a legitimately zero last value; the ?: operator then replaces any falsy result — including 0.0 — with 0. The intent is to default to 0 only when the array is empty. Use `$last = end($this->values); $data['latestValue'] = $last !== false ? $last : 0;` instead. This correctly emits 0.0 as a real value and only falls b …
- **Verifier:** Line 111 of TrendResult.php confirms the exact code cited: `$data['latestValue'] = end($this->values) ?: 0;`. The `?:` operator replaces any falsy result from `end()` -- including a legitimate last value of `0` or `0.0` -- with the integer `0`. The reporter's description of the mechanics is accurate …
- **Verifier fix note:** The suggested fix (`$last = end($this->values); $data['latestValue'] = $last !== false ? $last : 0;`) is correct. The motivation about preserving `0.0` float precision is technically accurate in PHP but has no observable effect in the JavaScript consumer, since `JSON.parse` maps both `0` and `0.0` to the same JS number. The real value of the fix is the empty-array case: without it, `showLatestValue()` on an empty series renders `"0"` instead of nothing, because `false ?: 0` produces `0`, which passes the `displayValue !== undefined` guard in TrendCard.tsx (line 23). An alternative that also omits the key for an empty array: `if ($this->values !== []) { $data['latestValue'] = end($this->values); }` -- this is arguably more expressive of the intent.

### MartisNotification::level accepts arbitrary string — missing NotificationLevel enum
- **Severity:** low · **Category:** convention · **Subsystem:** notif-pref-profile · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Notifications/MartisNotification.php:39`
- **Evidence:** public readonly string $level = 'info',
- **Suggested fix:** The docblock on line 25 references `NotificationLevel::Success` but no such enum exists. The TSX renderer expects the finite set `{info, success, warning, danger}`. An invalid level (e.g. `'error'`) produces an unmatched CSS class `is-error` and falls through to the `default: InfoIcon` case. Per the project convention (enums for finite value sets), create a `Martis\Enums\NotificationLevel` enum wi …
- **Verifier:** Confirmed by direct code inspection. MartisNotification.php:39 declares `public readonly string $level = 'info'` with no enum constraint and no runtime validation. The codebase has 39 enums in src/Enums/ — none named NotificationLevel — confirming the enum simply does not exist despite the docblock  …
- **Verifier fix note:** The suggested fix is correct: create Martis\Enums\NotificationLevel with cases Info, Success, Warning, Danger and update both the constructor and the static make() factory to accept NotificationLevel instead of string. The docblock example on line 25 already uses the correct syntax (NotificationLevel::Success) so it will be accurate once the enum exists. Also update NotificationController.php:206 which currently reads the level as a raw string from the stored payload — it should map the stored string back to the enum case (or validate it) when constructing the response DTO sent to the TSX renderer.

### ProfileResource hardcodes 'users' table in unique-email rule
- **Severity:** low · **Category:** bug · **Subsystem:** notif-pref-profile · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Profile/ProfileResource.php:55`
- **Evidence:** 'email' => ['required', 'email', 'max:255', Rule::unique('users', 'email')->ignore($user->getAuthIdentifier())],
- **Suggested fix:** Consumers with a custom auth user table (e.g. `admins`) will have the uniqueness check run against `users`, not their actual table. This either blocks valid saves (if the same email exists in `users` for a different user) or silently allows duplicates (if the actual table has a constraint but the wrong table is queried). Derive the table from the model: replace `'users'` with `(new (config('auth.p …
- **Verifier:** The hardcode is confirmed at /Users/lmoura/projects/martis/martis-package/src/Profile/ProfileResource.php:55 — `Rule::unique('users', 'email')->ignore($user->getAuthIdentifier())`. The string `'users'` is literal; no dynamic table resolution exists in this class. However, the class is explicitly des …
- **Verifier fix note:** The suggested fix is correct in principle. Prefer `Rule::unique(config('auth.providers.users.model', \App\Models\User::class))` so Eloquent resolves the table name automatically, matching however the consumer has configured their auth provider. The `'email'` column argument can also be dropped because Eloquent's unique rule already defaults to the column named after the rule key. No need to also resolve the email column dynamically unless a consumer is known to rename it.

### assert() used as production type guard in TwoFactorService — no-op with zend.assertions=-1
- **Severity:** low · **Category:** implementation · **Subsystem:** notif-pref-profile · **Confidence:** medium
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Profile/TwoFactorService.php:46, 66, 92, 107, 124, 155, 175`
- **Evidence:** assert($user instanceof Model); $user->two_factor_secret = encrypt($secret);
- **Suggested fix:** PHP documentation recommends `zend.assertions = -1` in production, which compiles out all `assert()` calls. If a non-Model Authenticatable is passed (theoretically possible because the signature accepts the interface), all property accesses after the assert become dynamic property reads/writes that silently fail or write to ephemeral stdClass properties. Replace each `assert($user instanceof Model …
- **Verifier:** The cited code is confirmed. TwoFactorService's seven public methods all declare `Authenticatable $user` but immediately follow with `assert($user instanceof Model)` before doing property reads/writes and calling `$user->save()`. The call sites (ProfileController::resolveUser at line 259-268 and Two …
- **Verifier fix note:** The reporter's fix is correct: replace each `assert($user instanceof Model)` with an explicit `if (!$user instanceof Model) { throw new \InvalidArgumentException(...); }` and tighten the parameter type to `Model&Authenticatable $user` (matching the already-correct signature of the private `verifyAndTrack()`). This eliminates the zend.assertions dependency and makes the contract self-documenting. No severity change needed — keep it low.

### abort(500, ...) with internal config error message on email verification route
- **Severity:** low · **Category:** security · **Subsystem:** sec-secrets-sweep · **Confidence:** medium
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/EmailVerificationController.php:96`
- **Evidence:** abort(500, 'Auth user model is not configured.');
- **Suggested fix:** The second argument to `abort()` becomes the exception message and in some Laravel error-handler configurations (particularly with debug=true or custom error pages) can be included in the HTTP response body. In a production admin panel this is a configuration error message, not a real secret, but it reveals internal structure. Use `abort(500)` without a message and instead throw a logged exception …
- **Verifier:** The cited code at line 96 does exist exactly as reported:      abort(500, 'Auth user model is not configured.');  The second argument to Laravel's `abort()` helper is passed as the `$message` parameter to `HttpException`, which stores it as the exception message. In Laravel's default debug-off produ …
- **Verifier fix note:** The fix is correct: replace `abort(500, 'Auth user model is not configured.')` with a logged exception followed by `abort(500)`. For example: `report(new \RuntimeException('Martis: auth user model is not configured — check auth.guards and auth.providers in config.'));` followed by `abort(500);`. This keeps the diagnostic information in the server log while emitting no message in the HTTP response under any configuration. The concern is real but only materialises when APP_DEBUG=true or with a custom exception renderer — standard production Laravel suppresses the second argument of abort(500).

### Raw exception message from arbitrary Throwable surfaced in 500 JSON response body
- **Severity:** low · **Category:** security · **Subsystem:** sec-secrets-sweep · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Http/Controllers/ResourceController.php:460`
- **Evidence:** return JsonErrorResponse::serverError($e->getMessage() ?: 'Error deleting record.')->toResponse();
- **Suggested fix:** The delete catch-all path deliberately forwards `$e->getMessage()` to the client (justified in the v1.8.2 comment) so 'the operator sees what actually broke'. This is acceptable for an admin panel where only authenticated staff reach it, but the message can still disclose DB schema, file paths, or third-party service internals. Replace with a stable user-facing string (e.g. 'Error deleting record. …
- **Verifier:** The code at ResourceController.php:460 does forward `$e->getMessage()` to the JSON response body, exactly as described. The same pattern appears in ActionController at lines 246 and 640. However, the route definition in routes/martis.php:181-467 places the destroy endpoint inside a triple-layered pr …
- **Verifier fix note:** The pattern is an intentional UX decision for admin operators, not an unauthenticated information-disclosure vulnerability. If the team wants to tighten it anyway, replace the raw `$e->getMessage()` in the JSON body with a stable string such as 'Error deleting record.' and keep the full message exclusively in the `Log::error` context array (where it already also appears at line 456). The same optional hardening applies to ActionController lines 246 and 640. This is a code-quality improvement, not a security fix.

### AzureProvider Graph API call has no HTTP timeout — SSRF amplification and hung requests
- **Severity:** low · **Category:** security · **Subsystem:** sec-secrets-sweep · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Sso/Providers/AzureProvider.php:164-166`
- **Evidence:** $response = Http::acceptJson()             ->withToken($accessToken)             ->get($url);
- **Suggested fix:** Add `->timeout(10)` (or a config-driven value) to the `Http::` chain. Without a timeout, a slow or unresponsive Graph endpoint will hold a PHP-FPM worker for the default cURL limit (often 0 = forever), enabling a slow-loris-style denial-of-service via the SSO callback. Additionally, the `$url` in `fetchAppRoleAssignments` and `fetchGroupMemberships` is constructed from the hard-coded `graph.micros …
- **Verifier:** The code at /Users/lmoura/projects/martis/martis-package/src/Sso/Providers/AzureProvider.php lines 164-166 confirms the finding verbatim: `Http::acceptJson()->withToken($accessToken)->get($url)` has no `->timeout()` modifier. A search across the entire package src/ directory finds no global Http tim …
- **Verifier fix note:** Add `->timeout(config('martis.auth.sso.providers.azure.graph_timeout', 10))` to the Http chain in `graphCall()`. The "SSRF amplification" label in the original report is misleading — there is no SSRF vector here because the base URL is hard-coded to graph.microsoft.com and $externalId is urlencode()'d. The actual risk is FPM worker exhaustion under a slow upstream, not data exfiltration or internal host probing. Severity should be low, not medium.

### Graph API OData filter $filter=resourceId eq {guid} is unquoted — invalid OData and potential injection
- **Severity:** low · **Category:** security · **Subsystem:** sso · **Confidence:** high
- **Location:** `/Users/lmoura/projects/martis/martis-package/src/Sso/Providers/AzureProvider.php:118-122`
- **Evidence:** $url = sprintf(     'https://graph.microsoft.com/v1.0/users/%s/appRoleAssignments?$filter=resourceId eq %s',     urlencode($externalId),     $resourceId, );
- **Suggested fix:** OData GUID literals must be written as `guid'<value>'` (or just bare in beta) and the value must be URL-encoded. The current code interpolates `$resourceId` directly from config without URL-encoding or OData-escaping. If an operator misconfigures the env value with spaces or OData-significant characters (single quotes, ampersands), the filter will either fail silently (returning no roles, effectiv …
- **Verifier:** The code at lines 118-122 confirms that `$resourceId` is interpolated directly into the OData `$filter` query string without `urlencode()`. However, the security framing in the report is substantially overstated for two reasons.  First, `$resourceId` is sourced exclusively from `$this->config('resou …
- **Verifier fix note:** The reporter's `guid'<value>'` OData syntax recommendation is incorrect for Graph API v1.0, which uses OData v4 bare GUID literals. The correct fix is simply to apply `urlencode()` to `$resourceId` in the query-string position (matching what is already done for `$externalId` in the path segment), and optionally add a UUID format guard with a regex or `Str::isUuid()` to catch misconfigured env values early with a clear RuntimeException rather than a silent empty-roles return. No OData quoting change is needed.
